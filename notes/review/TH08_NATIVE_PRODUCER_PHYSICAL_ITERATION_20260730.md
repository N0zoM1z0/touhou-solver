# TH08 Native Producer / Physical Iteration Audit

Date: 2026-07-30 (Asia/Singapore)

Workspace: `/home/pentester/coding/codex_ida/th08`

Branch/HEAD: `main` / `eec2560`

Connected IDB identity: TH08 1.00d patched analysis input, image base
`0x00400000`, file size `840704`, SHA-256
`ec101fcff80b77e717d43b54e326375487af19661bb7c8d11a19ee5e0fbf928b`;
the retained one-byte no-life-decrement proof reconciles it to the clean
physical target

Executable identity: expected shipped `th08.exe` SHA-256
`330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`;
pending fresh verification

Latest retained evidence cutoff: Lunatic Stage-5 run
`lunatic_route2_stage5_unattended_20260730_083416`

Scope:

- revalidate the ten native producer-root component classes;
- connect timeline allocation and immediate initial-VM termination to the
  ordered generational lifetime ledger;
- identify the smallest action-authority change that can improve physical
  hit count;
- pass focused/dual-platform gates;
- run one complete original-game Lunatic stage without fail-close; and
- retain first-hit causality, resources, Bombs, timing, and replay evidence
  when naturally produced.

Non-actions:

- no THPRAC or exact-spell activation;
- no screenshot-based runtime sensing;
- no Bomb bit `0x02`;
- no mid-stage stop after hits;
- no promotion from static/offline parity alone.

## Evidence Labels

- **Observed:** shipped instructions/dataflow, deterministic execution, or
  retained native/physical evidence.
- **Inferred:** supported by observed facts but not directly witnessed at
  runtime.
- **Hypothesized:** candidate explanation or intervention awaiting a
  differentiating gate.

## Live Summary

- The current live controller has no complete predictive future-body producer.
- A full implementation of every root class before any physical feedback
  would repeat the slow iteration pattern the user rejected.
- The implementation target is therefore the smallest causally useful
  producer/event slice that reaches current hit windows, followed by an
  immediate whole-stage physical falsifier.

## Pending Checks

- [x] reconcile IDB and expected executable identity;
- [x] inventory concrete addresses/layouts for ten root requirements;
- [x] trace allocation/initial-VM semantics through current source;
- [x] mine retained Stage-5 first-hit evidence for the narrowest reachable
      producer class;
- [x] implement and differentially verify that class;
- [x] pass Linux/Windows gates;
- [x] run and retain one complete physical stage;
- [x] compare the canonical first hit and aggregate hits to a
      control-equivalent baseline;
- [x] retain checkpoint and next experiment.

## Evidence Update

- **Observed / IDA revalidated:** `enemy_spawn_from_timeline`
  `0x0042A4E0` uses template/manager root `0x0057D2F0`; the ordinary pool
  starts one `0x53D0` record later at `0x005826C0`. Main VM state begins at
  enemy `+0x07F8`. The scheduler's 16-byte timeline-state table is rooted at
  `0x00F5A0C0`; its PC is state `+0x0C`.
- **Observed / authority correction:** fixed pointer cells are not the bytes
  of the runtime ECL image, auxiliary VM heaps, callback pointees, or
  run-state resource pointee. The root inventory now labels these layouts
  revalidated but partial; only ordinary template/pool, slot motion/flags/
  lifecycle bytes, and shared RNG currently satisfy a whole minimum
  requirement.
- **Implemented offline:** native-order timeline spawn allocation now selects
  the first inactive slot, emits `allocate`, invokes an explicit initial-VM
  executor, emits `retire` on exact return `-1`, and re-scans for later
  same-update requests. The integrated simulator feeds timeline spawn
  requests into the generational ledger.
- **Observed differential:** four product/oracle cases agree, including
  allocate/retire/reallocate in one update and pool exhaustion. Retained
  payload SHA-256 is
  `d5db9e32cf029249b17f3e99cf8b7782856bbdb128acf52e5f7538f827c2ac17`.
- **Observed Stage-5 offline screen:** on 500 deterministic empty-kernel rows
  within 240 frames of a hit from `20260729_161313`, enabling
  `losing_control_reserve` changes seven actions, improves reserve deficit on
  all seven, and preserves the complete fresh/local/terminal hard vector on
  all 500. It does not change the canonical first-hit window and is not proof
  of prevented contact.
- **Physical candidate:** enable the already parity-tested losing-state
  reserve only after Boolean exhaustion, with
  `--no-losing-control-reserve` as the exact rollback. This is a physical
  falsification experiment, not a viability certificate.
- **Iteration-method correction:** the retained offline-iteration audit and
  first-hit perspective are now repository documents. After this one changed
  Stage-5 transition gate, parser/model/event work moves to a
  content-addressed, fail-closed one-frame causal executor and same-input
  native replay differential. Aggregate hits remain a workload metric; the
  fresh attempt's first contact is the canonical research unit.
- **Observed platform gates:** complete Linux discovery passes 1,297 tests in
  14.197 seconds. The exact Windows UNC workload reaches terminal `OK` with
  1,297 tests in 32.864 seconds and three existing skips. WSL interop returned
  before the Windows process/report stream completed; the exact process was
  monitored to terminal output rather than treating launch return as test
  completion. Both retained differentials render LF-normalized-identically
  on Linux and Windows.
- **Observed physical preflight failure / CE-0199:** run
  `lunatic_route2_stage5_unattended_20260730_124635` verified executable,
  patch, route, difficulty, team, and Stage 5, then emitted
  `time_scale_authority_unknown` at frame 1 and terminated before any
  decision. Its five rows and zero hits are not survival evidence. Cleanup
  completed.
- **Corrected launch contract:** the same immutable intervention must rerun
  with `--diagnostic-continue-root-only-scale`. This prevents fail-close but
  explicitly remains an unknown-direction constant-root proxy with no hard
  scale/model authority. No trace observer, THPRAC, Bomb, or hit-stop is
  added.
- **Observed prelaunch scope defect:** the first corrected command was
  rejected before starting `th08.exe` because the practice supervisor coupled
  scale continuation to two unrelated trace observers. That restriction is
  removed and protected by a no-observer Stage-5 regression. The following
  prelaunch retry exposed the same duplicated restriction in the shared agent
  contract. Run `20260730_125502` had already launched/patched the game and
  navigated to the verified Stage-5 selection; it rejected before final
  confirmation, agent arm, or any decision and then cleaned up. Both layers
  now permit independent explicit continuation. Focused Linux/Windows
  supervisor discovery passes 33/33 and agent/hotkey discovery passes 19/19.
  Complete discovery passes 1,298 tests in 14.387/32.480 seconds with three
  existing Windows skips.
- **Observed complete physical run / CE-0200:** run `20260730_130219`
  completes Stage 5 over frames `2..42835`, 12,281 decisions, 12 hits, hard
  no-Bomb, two automatic dialogue pulses, `route_complete`, and exact cleanup.
  First contact is frame 2,220 at `(8.000, 422.343)` after `up_left_fast`;
  nine of 12 contacts have a boundary contributor. Raw 461,720,403-byte
  trace SHA-256 is
  `fd54f277da5bc32b75c874af7dffae8c279d6f3cadfdb3848211477e76b602c1`.
- **Rejected activation claim:** the run emitted zero corridor policy
  records/queries and zero reserve guidance/selections. The configured
  losing-state reserve was physically inert. Aggregate 12 and earlier first
  contact are not a favorable result.
- **Next offline gate:** separately version an unguided boundary reserve and
  replay the canonical first-hit plus all-hit windows. Require a changed
  pre-contact action, strictly reduced reserve deficit, zero hard-vector
  regressions, and bounded timing before another physical run. This is the
  first end-to-end test of the retained offline-first iteration protocol.

## Completion And Supersession

This audit's physical intervention finished as CE-0200: the reserve was
configured but physically inert. It is not the next experiment.

Subsequent work established an accepted replay-save flow, a content-addressed
Stage-5 replay, exact replay/manager input alignment, one atomic but partial
native byte root, and an original-engine all-36 action portfolio. The six
short-fence local candidates all hit four to six frames later. The retained
next milestone is an explicit native restore-and-step executor, not another
unguided physical repeat or another full all-36 replay-prefix sweep. See
`../architecture/NATIVE_REPLAY_CAUSAL_WIND_TUNNEL_AND_REPLAY_SAVE_CONTRACT_20260730.md`.
