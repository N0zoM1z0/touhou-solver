# G5 Shipped Runtime ECL Identity Stage-5 Result

Date: 2026-07-28

Status: **physical gate accepted for immutable Stage-5 runtime instruction
bytes only**. Source completeness, reachable auxiliary paths, future geometry,
planner feasibility, publication, and physical action authority remain
`none`.

This result closes the physical gate fixed by
`notes/research/g5/G5_SHIPPED_RUNTIME_ECL_IDENTITY_PHYSICAL_CONTRACT_20260728.md`.

## Implementation Boundary

The default-off `RuntimeEclIdentityService`:

- reads and verifies the static candidate before the gameplay loop;
- triggers only for the exact executable, route 2, Lunatic, and native
  Stage-5 route index `5`;
- attempts one action-neutral capture after the current input issue;
- makes exactly four process-memory reads on success;
- normalizes only the revalidated loader relocation sites;
- flushes one visible terminal result immediately; and
- never retries a failed attempt or exposes a result to an action consumer.

The supervisor and hotkey command path propagate the explicit static path and
SHA-256. The strict offline audit streams the raw trace and validates the
fixed physical identity, exact one-row semantics, adjacent cadence, route
completion, and hard no-Bomb masks.

The contract originally used route index `4` while being drafted. This was
corrected before implementation or physical execution: Stage 4A and 4B use
native indices `3` and `4`, while Stage 5 uses index `5`. Both the practice
menu mapping and retained Stage-5 traces independently establish this. A
human stage label must never be converted to native identity by subtracting
one.

## Automated Gates

- Focused runtime-image primitive tests: 6 passed.
- Focused identity service and audit tests: 6 passed.
- Agent hotkey tests: 15 passed.
- Practice supervisor tests: 23 passed.
- Complete Linux discovery: 957 passed.
- Complete Windows UNC discovery: 957 passed, with the three existing
  platform skips.
- Focused Ruff and `git diff --check`: passed.

## Physical Workload

Run:
`lunatic_route2_stage5_unattended_20260728_212622`

- executable SHA-256:
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`;
- Sakuya/Remilia route 2, Lunatic, native Stage-5 route index 5;
- frames `1..40984`, 12,100 decisions;
- nine native hit edges at
  `[2038, 4202, 7998, 11923, 12768, 13735, 23000, 29569, 36025]`;
- hard no-Bomb passed over every decision;
- automatic transitions, `route_complete`, accepted compact artifacts,
  supervisor exit zero, exact key release, and no residual game/controller
  process.

The raw trace is local and ignored:

- bytes: `503555113`;
- SHA-256:
  `c00fc8379526f2f998df984d10a1e3c076bd339583386ae95821ce33f1948d27`.

## Exact Runtime Observation

The one result row was emitted on decision/snapshot frame `1`, gameplay epoch
`0`:

- runtime base: `0x0B1E0048`;
- runtime image length: `47224`;
- subroutines/timelines: `90/1`;
- read count: `4`;
- capture wall time: `0.2083000727 ms`;
- complete transaction wall time: `2.9960999964 ms`;
- next decision: frame `3`, delta `2`;
- relocated runtime SHA-256:
  `f2d69f5e207efed65bc5fc6e3925a86608e235a5a47c82f83b5c246c7c19e2a8`;
- normalized runtime SHA-256:
  `3148f45faf78bd8211a956edcdc353be73d2781995d3dadd36bdca8132f8fe19`;
- static decoded SHA-256:
  `3148f45faf78bd8211a956edcdc353be73d2781995d3dadd36bdca8132f8fe19`;
- first differing offset: none;
- status: `exact_match`.

The strict audit regenerated byte-identically:

- report:
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260728_212622.runtime_ecl_identity_audit.json`;
- SHA-256:
  `f8c88c2c68a8a28eec4f8981a9474295c5fce1aed3bc96f35bd55c9f5c4145d5`.

## Conclusion And Authority

**Observed:** for this shipped executable and immutable Stage-5 image, the
complete relocated runtime ECL image normalizes byte for byte to the decoded
`artifacts/decoded/ecldata5.ecl` payload.

**Inferred:** the retained decoded file is an exact instruction-byte key for
captured Stage-5 main and auxiliary VM PCs under this executable identity.

**Not established:** that a VM path reaches an event before its decision
deadline, that all source contexts are observed, that operands and geometry
are complete, or that any action is physically safe. Those remain separate
fail-closed gates.

The next G5 checkpoint is a fixed contract for one auxiliary instruction/path
event class. Unsupported control flow, locals, timing, scheduler state,
emission dependencies, or source branches remain `UNKNOWN`.
