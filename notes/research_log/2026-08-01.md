# TH08 Research Log — 2026-08-01

## Ordinary causal future and physical latency gate

- **Observed native:** shipped `ecl_eval_float` case `10048` at `0x0042067F`
  calls angle-to-player using the current player and enemy `+0x2D88`; case
  `10069` reads source motion angle `+0x2D94`. Ordinary source semantics v11
  retains affine player-aim coefficients and residual uncertainty for all
  reached future direct-fire events.
- **Implemented:** future projection v4 conditions aimed births separately on
  every selected action's exact active/held/pending pickup path. A named exact
  action authority can no longer be widened by local preflight or fresh issue
  recertification. Early kill remains an objective only inside that hard set.
- **Observed physical:** Stage-5 `20260801_092136` completed 15 hits,
  first 1863, while an all-action terminal probe at frame 1847 consumed
  796.649 ms and reached issue 70 frames after snapshot 1777. This directly
  validates computation/publication latency as a physical state missing from
  the offline wind tunnel.
- **Implemented and re-run:** exact terminal work was reduced to a three-action
  subset and reused; the causal held witness consumes the remaining complete
  horizon. Stage-5 `20260801_093817` completed 20 hits, first 1873. The new
  witness was physically effective seven times, but changed from safe to
  unsafe at frame 1767 and no exact planning-lag transition could issue an
  alternative before the hit. Different-RNG totals are observational; this
  retained safe-to-unsafe sequence is the causal next gate.
- **Integrity:** both runs passed hard no-Bomb, saved verified Route-2 Lunatic
  Stage-5 replays, and left no game/controller/supervisor process running.

## Computation-delay predecessor and physical action conversion

- **Implemented:** delayed issue branches explicitly retain active input and
  old pending evolution during computation. At issue, held complete input is
  no-write; another complete mask replaces pending and ranges over pickup
  delay 0..6. The H80 certificate is indexed by final observed issue age and
  consumes action-conditioned future births. Fresh hostile-body geometry
  intersects every row before the final age read; incomplete coverage, phase
  1/2, terminal expiry, and nonpositive clearance fail closed.
- **Observed deterministic:** packed binary32 delayed paths match scalar
  native-order movement bit for bit. The f817/833/835/850/910 retained chain
  has complete q=0..24 tables and exact branch counts. Elapsed times are
  120/112/104/269/103 ms; only f833 retains `right_fast@q24`, so the later
  saturated roots are not accidentally reopened.
- **Observed physical sequencing:** Stage-5 `111208` first activated the table
  but a three-action scan took 1,282 ms and reached q81 after safe rows ended
  at q49. Incremental evaluation in `112918` made 11 scans effective; its
  first pressure root showed `right_fast` safe only q15..24 but evaluated
  third at q59. A no-authority local proposal now orders independent exact
  action computations. Reusing that immutable proposal removes a second
  stale-root planning pass; fresh issue still recertifies every local action
  and only the final exact q row can issue input.
- **Observed final gate:** `lunatic_route2_stage5_unattended_20260801_115838`
  completed 18 hits, 10 nonspell, zero Bombs. Eleven of 21 delayed scans were
  effective; median issue age was 25 frames and median delayed computation
  342.9 ms, versus 43/494.8 in ranking-only `114613`. Replay slot 7 decoded
  Route 2/Lunatic with an empty Bomb list and SHA-256
  `00d8cd9839a5f972b1be144e3b7314e5c4e5afca473bcfbb974e9568d446fcfb`;
  cleanup was complete. Hit totals are different-RNG observations, while the
  exact authority conversion and latency reduction are mechanism evidence.
- **Remaining:** CE-0250's zero-transition blocker is closed, not ordinary
  NMNB. Exact rows can still be empty/expired, and one effective direction is
  not retained as a recursive lease. Next build versioned terminal-
  continuation authority, then rotate physical validation to Stage 3 or 4A.

## Versioned terminal-continuation lease

- **Implemented:** an effective delayed final-age row now retains its exact
  action, content-addressed projection version and digest, original root,
  issue/pickup support, binary32
  branch positions, fresh geometry seam, and H80 expiry. Every compatible
  fallback root renews held/no-write first. If that is unsafe, recovery
  directions are computed as independent delayed predecessors; local ranking
  cannot widen either authority. Native spell-active and stage-route are
  reread at final issue; capture-to-issue context changes fail closed.
- **Observed deterministic:** the retained Stage-5 f1456/f1469 `left_fast`
  witness predicts the f1470 player position bit-exactly. The old f1479
  `right_fast` choice has a held-action mismatch and would now be rejected
  without a new predecessor. The f817/833/835/850/910 gate still has empty
  actual issue rows; f833's isolated `right_fast@q24` does not authorize its
  observed q0 root. Held/no-write base rows share one exact path and no longer
  repeat 74 equivalent issue-age trajectories.
- **Status:** focused Linux and Windows tests pass. Physical promotion remains
  pending a rotated Stage-3 or Stage-4A trial with hard no-Bomb.

## Physical lease falsifier and native phase correction

- **Observed physical:** Stage-4A `20260801_130742` completed 21 hits, 12
  nonspell, zero Bombs, accepted replay, and cleanup. It created six leases
  and renewed 34, but effective no-write consumption remained zero. Three
  revocations were fresh-geometry changes and three were pipeline/position
  mismatches. Aggregate hits are different-RNG observations.
- **Observed native mismatch:** f3191 published `down_right` from a q17
  delayed predecessor, but f3192 position retained one final
  `down_right_fast` movement step. Native priority 9 movement reads current
  input before priority 17 publishes that frame's new input, so observed
  publication and physical movement are one frame out of phase.
- **Implemented:** TH08 pipeline roots use publication-to-motion lag 1;
  branches separately retain published actions and movement actions. Lease
  capture conditions native current input on publication history and position
  on the shifted movement history. The physical f3191→3192 witness now
  matches bit-exactly.
- **Implemented geometry authority:** lease v2 retains the active/future-body
  AABB set consumed by its exact certificate. Fresh bodies may preserve the
  lease only when their uncertainty/robustness-expanded future envelopes are
  contained frame-by-frame in that old set; pointer identity is irrelevant,
  removals are harmless, and any uncontained envelope fails closed.
- **Observed deterministic:** 186 Windows focused tests pass. The retained
  delayed chain still keeps every actual f817/833/835/850/910 row empty; no
  lease can be created there. Rotate next to Stage 3 physical.

## Rotated Stage-3 result and timeline-fraction correction

- **Observed physical:** Stage-3 `20260801_134853` completed with one
  nonspell hit at f7935, zero Bombs, Power 128 throughout, accepted replay
  SHA-256
  `9e2a1272623843f39ebd5c68ee5deec02f36731ba79ac7af0fad67f4d91c71ad`,
  and cleanup. The earlier Stage-3 workload had five hits, but the different
  roots make that count observational.
- **Observed mechanism audit:** all 4,725 eligible ordinary decisions lacked
  a future policy. No continuation lease was created, renewed, selected, or
  effective; early kill observed targets but could not rank inside an exact
  set. Of 1,214 source attempts, 701 failed on timeline-2 fractional time and
  465 crossed the native clock bracket. All 49 attempts in f7695..7935 before
  the hit failed on the fraction. The run therefore measures fallback, not
  recursive authority.
- **Observed native/correction:** at exact unit scale, native timer
  `0x00447421` increments integer elapsed and preserves the fraction; timeline
  scheduling compares only the integer. Source semantics v12 now accepts any
  finite fraction under its existing exact-unit precondition and reduces the
  root to the observed integer clock. Nonfinite and nonunit roots remain
  `UNKNOWN`. Focused Linux tests pass; repeat Stage 3 after Windows gating.

- **Observed Stage-3 v12 repeat:** `20260801_141250` completed four hits at
  `[3181,7956,8414,25932]`, three ordinary and spell 50, with zero Bombs,
  accepted replay SHA-256
  `c1b99e9215e4bd044f574c449a2064837afa531c062cb5709ba559c7371a7d11`,
  and cleanup. Power fell 128 to 93. The preceding one-hit root differs.
- **Observed activation:** 692/1,261 source roots completed; 596 were
  submitted, 595 policies completed, and 265 exact ordinary issues were
  effective. Four delayed issues created four leases. All four leases were
  revoked by fresh-body containment, and effective no-write consumption was
  zero. Early kill applied twice.
- **Observed first-hit gap:** exact authority ended at f1406; all 103 decisions
  in f2941..3181 were `future_policy_unavailable`, even though 14 source roots
  completed in that window. First hit f3181 was a local robust-set exhaustion
  with four-frame warning. Source v12 is physically validated for delivery,
  but persistent recursive authority and hit reduction are not.

## Parent cross-workload falsifier and artifact retention

- **Observed Stage-4A:** parent `5fc6d92` run `20260801_142851` completed 18
  hits/10 nonspell, first hit 833, Power 128 to 1, zero Bombs, accepted replay
  SHA-256 `569b79af83389e3112a47f17afc1d45e349998b61f87f4d41d907c9284e123c8`,
  and cleanup. The direct `130742` baseline was 21/12 with first hit 819. Nine
  leases were created, none effective; seven revoked on fresh geometry.
- **Observed Stage-5:** parent run `20260801_143952` completed 18 hits/10
  nonspell, first hit 1888, Power 128 to 2, zero Bombs, accepted replay SHA-256
  `bc20e857701f9be40a7cef3b1a491d25977ac689d65a810b07b1fe2228d81a0f`,
  and cleanup. Direct baseline `115838` was exactly 18/10 with first hit 1728.
  Seven leases were created, none effective; fresh geometry dominated the six
  recorded revocations. Different-RNG hit totals are observational.
- **Inferred general blocker:** the Stage-3 one-hit run was fallback luck. In
  all rotated workloads, the lease's restarted fresh linear H80 envelope can
  diverge under native timed motion before the old causal witness is consumed.
  Add active hostile source trajectories and recertify only the newly observed
  seam; incomplete source/topology coverage remains fail closed.
- **Storage retention:** retired `archive/raw/` (36 GB, no live authority) was
  removed after compact evidence and current replay-capable raw retention were
  verified. Active Stage 3/4A/5 now keep exactly two raw bundles each. A
  fail-closed pruning tool requires complete compact reports, run note,
  accepted hard-no-Bomb status, and archived replay before deleting anything.
- **Implemented source v13/lease v3:** every active contact-enabled hostile
  source now publishes its captured ECL/native-motion AABB trajectory from the
  projection root. A lease accepts only that version. Fresh capture and issue
  checks condition the immutable proof at the stable native observation frame
  instead of restarting a new linear H80 future; unstable or uncontained
  observations still revoke fail closed.
- **Observed retained witness:** f469/f474 lease `stay` had 74 frames remaining
  and a compatible pipeline at f475/f482. The old geometry check passed its
  first three invented future frames and first failed at f478 on pointer
  `0x5826c0`, after native deceleration changed the restarted linear slope.
  This identifies a recertification-model mismatch, not an observed body that
  had already left the causal proof.
- **Verification:** affected Linux source/lease/controller/capture/retained
  tests pass. Windows affected patterns pass 149 tests; a broader 65-test
  ordinary pattern had only the preserved unrelated factorized-report worker
  mismatch (`8` observed versus legacy test `16`).

## Source-v13 / lease-v3 Stage-3 physical falsifier

- **Observed setup:** the first launch `20260801_152254` intentionally stopped
  at f1 because the diagnostic root-only unit-scale continuation flag was
  omitted; it made zero gameplay decisions and is not a physical result. The
  corrected run `20260801_152531` used the same diagnostic workload contract
  as prior successful ordinary trials.
- **Observed physical:** `152531` completed six hits at
  `[1255,1991,7576,8339,23938,25481]`, four nonspell and two spell, with zero
  Bombs, accepted replay SHA-256
  `82eb705e36ff9e3056965e58934277200ce8dc2a160d96d48386694afe130db8`,
  and cleanup. Different RNG prevents a causal total-hit comparison with v12
  `141250` (four hits/three nonspell).
- **Observed authority:** 653/1,190 source roots completed, 267
  prepublication issues and 13 delayed issues were effective. Ten leases were
  created and three renewed; 13 later decisions captured a lease and two
  selected it, but effective lease consumption was still zero.
- **Observed false revocations:** all six capture-geometry revocations had
  zero contact-enabled bodies. Three action-change revocations preserved the
  same direction/focus after removing the trace suffix and SHOT bit. One
  selected lease passed the pipeline and body seam but was rejected solely by
  the redundant fresh-local transaction test. These ten structural seams, not
  scalar reserve, are the next correction.
- **Implemented lease v4:** exact ordinary prefix/delayed/global calculations
  consume only currently contact-enabled bodies; future enables remain under
  source-v13 authority. Decorated action labels and SHOT-only auto-confirm
  writes preserve movement identity, while direction/focus/Bomb remain exact.
  A missed renewal issue-age row falls back to the compatible held old lease.
  Stable contact-body seam containment remains hard; the local recertificate
  is diagnostic because it contains no fresher bullet observation.
- **Verification:** 10 lease, 91 live-controller, source, prepublication, and
  retained-chain Linux tests pass; the same 10 lease, 91 controller, and 3
  retained-chain tests pass through the Windows UNC loader. Physical Stage 3
  must show nonzero effective v4 lease consumption before promotion.

## Lease-v4 physical mechanism promotion and source-horizon handoff

- **Observed physical:** Stage-3 `20260801_155718` completed seven hits at
  `[1263,2136,6695,7711,13011,13860,25791]`, five ordinary and two spell,
  hard no-Bomb, accepted replay SHA-256
  `e100140171749ef039f13634afc2df369298a967af043bc89d8490440eaf43ee`,
  and cleanup. Different-RNG totals are observational.
- **Observed lease mechanism:** one create and one renewal yielded 31
  captures, 30 selections, and 29 effective held/no-write issues over
  f1725..1783. Every issue retained `down_left_fast`, emitted no physical
  transition, and passed fresh contact-body containment. The only revocation
  was finite terminal expiry. Lease v4 therefore passes its narrow physical
  persistence gate.
- **Observed global failure:** all five ordinary hits had zero effective
  prepublication, delayed, or lease authority in the prior 240 frames.
  `future_policy_unavailable` dominated. Common source roots were complete
  only up to a truncated causal prefix, ending below H268 at reached main ECL
  `0x05`/`0x06` or captured movement state 2. The first full-H268 root before
  f1263 arrived at f1249, too late for causal solve/publication.
- **Storage retention:** after validating compact reports and the archived
  replay, pruning removed the retired accepted Stage-3 `141250` raw/log
  bundle (350,778,699 bytes). Its compact evidence and replay remain. The two
  newest accepted Stage-3 replay-capable raws are retained; incomplete and
  unaccepted bundles remain fail-closed from automatic deletion.
- **Next:** use shipped IDA/native evidence to close those reached source
  semantics exactly. Preserve UNKNOWN for unreconstructable state, then
  repeat the deterministic horizon/authority gate before rotating Stage 4A.

## Source-v14 reached-semantics correction

- **Observed shipped code:** main ECL `0x05/0x06` at
  `enemy_ecl_vm_step@0x004184B0` share the captured-local loop/set semantics
  already used for auxiliary contexts. Stage-3 reached masks are `0x04` and
  `0x01`, respectively.
- **Observed motion root:** `enemy_motion_update@0x00422C40` consumes state-2
  displacement `+0x2DC4`, start `+0x2DD0`, easing bits 14..16, timer
  fraction/current `+0x2DE0/+0x2DE4`, and duration `+0x2DE8`. The prior
  capture read previous `+0x2DDC` as elapsed; retained state-3 roots commonly
  exposed `-999`, proving the field error was material beyond state 2.
- **Implemented:** projection/source v14 captures and advances the complete
  state-2 root, fixes state-3 timer current, and lowers main `0x05/0x06`.
  Missing, nonfinite, invalid, or legacy timed roots fail closed; no velocity
  reconstruction is admitted. Corrected IDA comments are retained at
  `0x00422E80`, `0x00422F8F`, and `0x00423045`.
- **Verification:** source 34, native snapshot 15, capture 4, retained chain
  3, pre-exhaustion 14, controller 91, import smoke, and 147 Windows focused
  tests pass. Old state-2/3 payloads cannot be uplifted. Stage 3 is the next
  physical full-H268 delivery gate.

## Source-v14 physical delivery result

- **Observed physical:** Stage-3 `20260801_163434` completed four hits at
  `[1253,2141,8884,16134]`, three ordinary and one spell, hard no-Bomb,
  accepted replay SHA-256
  `0c65e8835b7cd9c6837cc0cbf37575552dc86e660d11d1e6c0cc7cc057bda80e`,
  and cleanup. Different-RNG 4/3 versus v13 7/5 is observational only.
- **Observed mechanism:** complete roots rose 645→801 and full-H268 roots
  47→58; the targeted main `0x05`/`0x06` and state-2-motion failures vanished.
  Prepublication/delayed/lease authority was effective on 316/7/5 issues,
  with three creates and four renewals. This promotes source-v14's narrow
  semantic correction.
- **Observed remaining mismatch:** every ordinary hit's prior 240 frames had
  zero exact authority. The first full root before f1253 was f1223, submitted
  at f1230; its 2352.884 ms solve completed at f1371, after the hit and
  expiry. Newly reached exact ECL/transform semantics must move source
  completeness upstream across the roughly 141-frame publication pipeline.
- **Storage:** compact evidence and replay were verified before the Stage-3
  `152531` raw/log bundle was retired, reclaiming 350,708,909 bytes. The two
  newest accepted replay-capable Stage-3 raws remain.
