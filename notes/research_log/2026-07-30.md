# TH08 Research Log — 2026-07-30

## Corrected Power-0 Lunatic route completed without aborting on absent C5 authority

- **Observed physical scope:** original Game Start selected Lunatic
  Sakuya/Remilia from Power 0. Run
  `lunatic_route2_fullrun_unattended_20260730_002115` completed all six route
  stages through native Final-B unload at frame 229,992. It retained 60,877
  decisions, 74 hit edges, zero Bomb actions/masks, zero foreground
  interruptions, zero JSON decode errors, and exact controller/game cleanup.
- **Stage result:** Stage 1/2/3/4A/5/Final B took
  `3/4/6/21/17/23` hits. Stage 4A is the largest current physical workload.
  The first fresh-attempt hit is Stage-1 nonspell frame 2,022. The model
  already reported a committed-prefix collision for 44/74 contacts; exact
  overlap evidence exists for 25 bullets, two lasers, and two enemy bodies.
- **Power/resource boundary:** stage entry Power was `0/6/7/5/10/5`.
  Stage 1 reached only Power 7 and ended at Power 1. All continuation after
  the first hit is contaminated by the no-life-decrement patch and respawn
  state, so later resource values are workload evidence rather than an NMNB
  route.
- **C5 reachability:** the corrected non-aborting continuation preserved the
  entire route, but the route history never reached the C5 target. Final B
  observed spell 174 for 299 decisions and spell 178 for 311 decisions, then
  completed without any decision in 182, 186, or 190. All sampled native
  scale roots were unit. The strict v3 C5 report has zero authority rows and
  correctly fails.
- **Evidence:** the ignored raw JSONL is 2,009,399,974 bytes with SHA-256
  `ffe52f97e959a92ec0adb06e418c17e2a97c8e2209f0978a1249f1b66e8a69d0`.
  The retained strict C5 report hashes to
  `47337406a0aaa4ff6a819b2614f94907613b951d44ae058bf3472634517fe7d8`.
  Compact session, summary, dossier, comparison, regressions, deaths CSV, and
  run note are retained. No new native replay was created.
- **Authority:** this is accepted physical whole-route/no-Bomb evidence and
  validates non-aborting evidence collection. It is not exact C5 delivery,
  clean stage survival, a clean resource route, or Lunatic NMNB.

## CE-0189 and dossier v4 separate live spell coverage from hit attribution

- **Observed report ambiguity:** the v3 spell table rendered both an observed
  zero-hit spell and a statically reachable absent spell as `Runtime 0`.
  That made spell 178 and 182/186/190 look equivalent despite exact decision
  evidence showing otherwise.
- **Correction:** dossier schema v4 records
  `observed`, `observed_decision_count`, `first_decision_frame`, and
  `last_decision_frame` independently of `hit_count`. Markdown now reports
  Observed/Decisions/Hits as separate columns. The current raw trace was
  replayed offline to regenerate its compact dossier and run note.
- **Stage-unit rule at that checkpoint:** the authorized C5-2 physical
  falsifier was the complete original-game Stage-6B workload. It required no
  THPRAC, precise-spell selection, or operator-time activation. Restore
  auto-stop was disabled, so exact C5 delivery remained a nested gate and
  never shrank the physical research unit below the whole stage.
- **Validation:** focused dossier, full-route-supervisor,
  practice-supervisor, and Ruff checks pass. Complete Linux discovery passes
  1,140 tests in 13.404 seconds; the exact Windows UNC suite passes 1,140 in
  29.833 seconds with the three existing skips.

## Whole-stage C5-2 physically passes exact scale delivery but fails survival

- **Observed physical scope:** original-game Lunatic Stage 6B run
  `lunatic_route2_stage6b_finalb_scale_delivery_20260730_020015` completed
  frames `1..76050` with 18,332 decisions, 22 hit edges, zero Bomb masks,
  normal `route_complete`, and exact game/controller cleanup. The canonical
  fresh-attempt hit is nonspell frame 7,412. Every hit follows global
  viability exhaustion, so the stage remains a hard survival failure.
- **Observed exact source:** one coherent spell-190/sub44 transaction
  captured manager frame 75,811 while the controller decision and expected
  source frame were 75,810. The source has exact runtime ECL identity,
  quarter scale, Bomb zero, one out-of-pool spell owner, zero ordinary-pool
  sources, player phase 3, and stable predeath baseline 7.
- **Observed causal consumption:** the one-frame transaction lag means no
  offset-zero authority can be sampled without backfilling. The first exact
  authority row is offset 1. Across 111 exact decisions through offset 238,
  every sampled root is quarter scale and no fallback, fresh hit, or Bomb is
  present.
- **Observed restore bracket:** callback 18 writes unit scale at relative
  offset 239. Controller cadence does not sample that frame. Offset 240
  observes a unit native root together with
  `scene_inactive/status=terminal_unload`; this closes the physical restore
  interval without inventing an active post-spell decision.
- **Evidence:** the ignored 634,344,375-byte raw JSONL hashes to
  `cbad986f0bb627d88135e2a4ae31c48389b6e030657ad77e557b882585aedcfc`.
  Strict schema v4 passes all 20 checks and hashes to
  `53cddd1162769010dbc467bf9d295e90e5389db3ffb4fce3d1c73c42076b08ec`.
  Tracked LF-normalized session SHA-256 is
  `c1d6dbcd8bb1ebf644cb05da6159bab748e8a9fa5f45b1faecea72fe85f641f2`.
- **Authority:** SEM-SCALE-C5 exact live delivery is physically observed only
  for this contaminated source-local restore interval. The 22-hit stage,
  phase 3/predeath 7 source, experimental pre-target transport, and absence
  of a clean Power-0 late-spell history forbid clean survival, stage-wide
  source, Extra, or NMNB promotion. The ordered program advances to
  `SEM-MODE`.

## CE-0190 corrects the asynchronous capture and terminal-restore report contract

- **Rejected v3 assumption:** the original strict report required a sampled
  exact authority row at offset zero and an active exact unit-scale row at
  restore. The physical transaction completed one manager frame after its
  decision root, and the unit root appeared only with native terminal unload.
  v3 therefore rejected valid causal evidence. The original Windows-CRLF
  render hashes to
  `b1aa5a0c27082303a19c7d90f7aba42661006c5892e351b68c3e0a80f65aa2b5`;
  the tracked LF-normalized artifact hashes to
  `9d3652466596f3a49cd67d0ff317f31685c92d23a6640bdf5ed9d47bd9d5dca7`.
- **Correction:** schema v4 derives origin and restore offset from the
  accepted source schedule, admits only zero/one-frame coherent capture lag,
  forbids retroactive offsets, and accepts a unit
  `root_only_context_mismatch` only when a same-frame terminal-unload record
  closes the bracket. Missing unload, late/negative capture, root/context
  mismatch, fallback, hit, and Bomb cases remain rejected.
- **Runtime completion:** the controller's delivery-complete helper now uses
  the accepted schedule's observed unit root instead of hard-coded offset
  240. A synthetic restore-at-offset-2 test protects the schedule-derived
  rule while the retained C4 offset-240 fixture still passes.
- **IDA revalidation:** `player_update` priority 9 precedes
  `enemy_manager_update` priority 11; the manager counter increments before
  per-enemy ECL execution and callback 18 writes the global gameplay scale
  during that ECL step. The IDB now names/types base `0x017CE758` as
  `g_game_timing_state: Th08GameTimingState`. The comment at `0x00424FB4`
  records the physical one-frame source capture and offset-239/240 terminal
  restore bracket. These are evidence-backed Phase-1 database changes.
- **Validation before full checkpoint:** focused scale-authority discovery
  passes 13 tests and focused strict-report discovery passes 10. Complete
  Linux discovery passes 1,143 tests in 13.481 seconds. The exact Windows UNC
  suite passes 1,143 in 29.925 seconds with the three existing skips. Ruff,
  `py_compile`, `git diff --check`, compact JSON parsing, the 22-row death
  ledger, raw ignore rules, and deterministic strict-report byte comparison
  also pass. No native target was rebuilt; these are quick correctness and
  report-integrity gates, not performance evidence.

## The 22-versus-19 Stage-6B aggregate does not justify rollback

- **Comparison boundary:** C5-1 at code checkpoint `19fa70b` and C5-2 at
  `3f0e56f` are route/team/difficulty/stage and practice-entry matched, both
  hard no-Bomb and complete, but they are not same-seed, paired,
  same-history, or same-first-hit-state trials.
- **Canonical witnesses:** C5-1 first contacts at frame 8,085 near the lower
  boundary in an observed bullet overlap; C5-2 first contacts earlier at
  frame 7,412 near the upper boundary in a modeled committed-prefix
  collision. Both precede spell 190 and follow global viability exhaustion.
- **Source diff:** between `19fa70b` and `3f0e56f`, the changed live files add
  source `player_phase`, admit the contaminated quarter-scale trigger,
  preserve evidence without new writes on authority loss, broaden the
  explicit full-route launch scope, and make restore auto-stop explicit.
  The local/corridor/native viability implementations and pre-target mask
  ranking are unchanged. The C5 source path is not active before either
  canonical hit.
- **Timing observation:** both runs have decision cadence median/p95 `3/4`;
  local-plan median/p95 is `17.429/30.474 ms` for C5-1 and
  `17.394/30.751 ms` for C5-2. This does not support a general cadence
  regression.
- **Disposition:** the extra three post-respawn hits are coupled through
  different position, Power, damage, timing, and phase histories. Aggregate
  worsening alone is not a causal code witness. No rollback or repeat is
  justified; the ordered semantic program advances to `SEM-MODE`.

## SEM-MODE-A retains latent geometry and separates native flag gates

- **Observed native semantics:** IDA instructions and update order were
  revalidated for player priority 9 and enemy-manager priority 11. Player
  focus byte `+3` changes immediately; secondary-character byte `+5` changes
  only after transition counter `+8` reaches 7. Active enemy flag `0x100`
  mirrors that byte into flag `0x800`; the later `0x830` manager gate precedes
  independent contact bit `0x04` and player-shot-damage bit `0x40` checks.
- **IDA database retention:** `0x0040BC40` is now
  `player_secondary_character_active`; `0x0042C420` is now
  `enemy_update_secondary_character_gate`. Strong comments were added at
  `0x0044B1D9`, `0x0044B42C`, `0x0042C568`, and `0x0042CF47`.
- **Decoder correction:** first-64 capture with
  `include_contact_disabled=True` now retains active, finite,
  nonnegative-size geometry blocked by flags `0x10`, `0x20`, or `0x800`.
  Strict current-contact decode is unchanged. This closes the permanent
  latent-geometry loss part of CE-0176.
- **Pure model and sensing:** `th08_enemy_mode.py` applies the exact
  bit-`0x100 -> 0x800` sync and exposes separate contact/damage eligibility.
  Runtime diagnostic observations and decision traces now retain player
  `+3/+5/+8`.
- **Offline evidence:** every 9-step binary focus history from three
  adversarial initial states matches an independent scalar recurrence. The
  CE-0176 fixture reproduces the seven-update gate opening corresponding to
  retained frame `10065 -> 10075`, and a decoder fixture retains raw flags
  `0x0100194D`.
- **Validation:** focused tests, Ruff, `py_compile`, and `git diff --check`
  pass. Complete Linux discovery passes 1,152 tests in 13.467 seconds;
  the exact Windows UNC suite passes 1,152 tests in 29.934 seconds with the
  three existing skips.
- **Authority boundary:** the broad state read is not yet a frame-bracketed
  player-mode/enemy transaction, and the projection is not wired into live
  hazard recurrence. Latent geometry remains conservative; unfocused combat
  authority remains disabled. No physical run was performed for this
  offline checkpoint.
- **Design and next gate:** the formal contract, IDA record, native-replay
  validation method, and SEM-MODE-B through F plan are retained in
  `notes/architecture/TH08_ACTION_CONDITIONED_PLAYER_ENEMY_MODE_SEMANTICS_20260730.md`.

## SEM-MODE-B preflight brackets whole-stage player/enemy mode evidence

- **Observed implementation:** a bounded diagnostic transaction now reads
  active input, player `+0..+11`, Bomb active/index, the existing first-64
  enemy prefix under one stable manager frame, and the same player/input/Bomb
  state afterward. It accepts only equal brackets with no retained
  bit-`0x100 -> 0x800` mismatch.
- **Bounded non-fail-close behavior:** crossed player or enemy updates retry
  at most twice. Exhaustion records `enemy_frame_unstable`,
  `player_or_input_changed`, or `enemy_mode_sync_mismatch`; gameplay
  continues and no mode-conditioned action is selected.
- **Whole-stage delivery:** `--trace-enemy-mode-transitions` is default-off
  and propagates through the hotkey contract plus original-game practice and
  full-route supervisors. It is enabled from stage/route entry, not by spell
  ID or operator timing. Existing `stop-after-hits=0` and hard no-Bomb
  arguments remain intact.
- **Reviewable evidence:** each capture retains both player observations,
  raw pointer/flags pairs for observed mode-sensitive bodies, frame bracket,
  attempts, read time, and `action_authority=false`. The streaming compact
  report preserves raw SHA-256, status/timing/stage counts, adjacent
  secondary transitions, and before/after body sets.
- **Deterministic falsifiers:** focused fixtures cover coherent flags
  `0x0100114D`, crossed player and enemy updates, exhausted retries, Bomb
  callback focus parity, and a synthetic `10065 -> 10075` transition. Report
  integrity refuses an absent/true action-authority marker.
- **Validation:** focused discovery, Ruff, `py_compile`, and
  `git diff --check` pass. Complete Linux discovery passes 1,162 tests in
  12.964 seconds; exact Windows UNC discovery passes 1,162 tests in 30.139
  seconds with three existing skips.
- **Authority boundary and next gate:** this is code/offline evidence only.
  Diagnostic reads/retries may perturb cadence; invalid-geometry or slots
  beyond the first 64 are outside the bracket; no physical survival claim is
  added. Run one complete original-game Lunatic Stage 5 from entry without
  fail-close, retain compact/raw evidence and cleanup, and use a compatible
  native replay afterward if the game produced one.

## CE-0191 preserves whole-stage evidence across root-only scale failure

- **Observed failed attempt:** run
  `lunatic_route2_stage5_unattended_20260730_035718` verified the shipped
  executable, no-life patch, Lunatic difficulty, route 2, and selected Stage
  5. It entered gameplay but emitted `time_scale_authority_unknown` at frame
  1 and terminated before any decision. It has zero hits only because no
  control interval occurred; it is not a survival or SEM-MODE sample.
- **Cleanup and evidence:** the five-row raw JSONL hashes to
  `da31073f043a6a615830d3410769174993ce30273becf44e1c1648117f0e5dad`;
  the compact session hashes to
  `560cce7dd93dea844c84bf673f4aa8ad5988c408d806fac6b42652d6b2083e24`.
  Keys were released and no game/controller/supervisor process remained.
- **Correction:** default hard fail-close is preserved. A default-off
  `--diagnostic-continue-root-only-scale` flag is accepted only with the
  whole-stage enemy-mode observer. Each decision repeats its freshly
  observed root over the finite 120-frame consumer horizon and records
  unknown-direction/no-authority provenance. Exact Final-B authority is
  mutually exclusive.
- **Formal boundary:** this proxy omits future scale writers and is neither a
  hard certificate nor survival authority. Its only purpose is retaining a
  complete stage-level native mode trace despite hits. The compact mode
  report now audits fallback configuration, roots, fallback labels, and
  `hard_authority=false`.
- **Validation:** focused fallback/launcher/report tests, Ruff,
  `py_compile`, and `git diff --check` pass. Complete Linux discovery passes
  1,167 tests in 14.404 seconds; exact Windows UNC discovery passes 1,167
  tests in 30.150 seconds with three existing skips.
- **Next gate:** checkpoint the correction and failed attempt, then retry
  exactly one original-game Lunatic Stage 5 with both explicit flags from
  stage entry. Do not stop after hits and do not treat any result as an
  observer-off comparator.

## SEM-MODE-B physically observes stage-wide player/enemy mode synchronization

- **Observed physical scope:** original-game run
  `lunatic_route2_stage5_unattended_20260730_041408` exercises code
  checkpoint `60ae5b9`, selects Lunatic Sakuya/Remilia Stage 5, and reaches
  `route_complete` over frames `1..42463` with 11,879 decisions. The
  supervisor continues through ten hit edges and verifies hard no-Bomb over
  every input, flag, and action.
- **Observed mode evidence:** 11,763 captures are coherent (`99.023%`);
  116 crossed observations are excluded with explicit status. The compact
  report retains 1,455 focus-input edges, 299 adjacent coherent
  secondary-character transitions, and 308 unique mode-sensitive
  pointer/raw-flags pairs. Frames `394 -> 397` and `444 -> 447` show stable
  body sets changing only by native enemy bit `0x800` in opposite
  directions.
- **Observed timing:** bounded capture costs 2.238 ms mean, 4.599 ms p95,
  and 10.965 ms maximum. It is a cadence-perturbing observer. Ten root-only
  scale observations use the explicit constant-current-root diagnostic proxy
  and retain no hard or survival authority.
- **Failure routing:** CE-0191 is physically revalidated only for preserving
  the whole-stage diagnostic unit. CE-0192 retains all ten hits; the
  canonical fresh-attempt contact is nonspell frame 3,519 with
  committed-prefix clearance `-2.766`. The `10 versus 12` aggregate
  comparison is not causal evidence for improvement or regression.
- **Evidence and cleanup:** ignored raw JSONL is retained locally at
  482,944,752 bytes, SHA-256
  `773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`;
  ignored launch log SHA-256 is
  `895586a05926072d22148b17b839243d570121ada0728fc611ee4d8778af5668`;
  compact mode-report SHA-256 is
  `545677cead65e312942408cbaa977694be6416b7afbaea663802462c0775dfd9`.
  The supervisor selected no-save, no new compatible replay was found, and
  no game/controller/supervisor process remained.
- **Provenance correction:** current dossier output now says
  `practice-mode respawn`, not `thprac respawn`; this run used only the
  original-game stage-selection script and no THPRAC.
- **Deterministic report check:** independent regeneration from the ignored
  raw JSONL is byte-identical for the compact mode report, dossier, deaths
  CSV, and regressions report.
- **Checkpoint validation:** focused dossier/mode-report discovery passes
  18/7 tests; Ruff, `py_compile`, and `git diff --check` pass. Complete Linux
  discovery passes 1,167 tests in 14.371 seconds. The exact Windows UNC
  discovery command exits zero on the same suite.
- **Authority and next gate:** SEM-MODE-B shipped-runtime occurrence is
  complete. No strategy is promoted. Continue at SEM-MODE-C with a causal,
  observation-compatible player/enemy mode recurrence before damage,
  differential, or whole-stage promotion.

## SEM-MODE-C adds an exact offline pickup/cadence mode transition

- **Observed IDB provenance:** the connected IDB input hashes to
  `ec101fcff80b77e717d43b54e326375487af19661bb7c8d11a19ee5e0fbf928b`,
  while the shipped disk executable hashes to
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
  Replacing only shipped file offset `0x4C4FA`/VA `0x0044D0FA` from `0xFF`
  to `0x00` reproduces the IDB hash exactly: research `push 0` replaces
  shipped `push -1` before `add_player_lives`. Critical SEM-MODE ranges at
  `0x0044B1C0`, `0x0044B410`, `0x0042C960`, and `0x0042CF30` byte-match the
  shipped image.
- **IDA database change:** instruction `0x0044D0F9` now has a provenance
  comment recording both hashes, the single-byte no-life-decrement patch,
  its purpose, the matching SEM-MODE ranges, and the warning that patched
  life behavior is not shipped semantics.
- **Offline recurrence:** `th08_enemy_mode.py` composes the existing exact
  local input-pipeline branches with priority-9 player `+3/+5/+8`, then
  priority-11 enemy bit-`0x800` projection. It emits contact and
  player-shot-damage body identities separately for every physical step.
- **Recursive transition seam:** one decision transition enumerates every
  declared cadence, returns an exact successor active/held/pending root and
  mode state, and can be called again at the next decision. No-write
  preserves and decrements older remaining-delay support. Hidden remaining
  delays merge only after complete base observation, elapsed physical
  updates, active input, held desired input, and exact mode state agree.
- **Clock and source boundary:** player mode advances on physical priority-9
  updates, not an unconditional enemy-manager clock. Supplied per-frame
  body/flag schedules are exogenous contact-gate-epoch inputs; the model
  overwrites only `0x800` and does not predict births, ECL flag writes,
  motion, geometry, scheduler freezes, or immutable hazard versions.
- **Focused validation:** Ruff and `py_compile` pass. Fifteen focused tests
  pass, covering an independent scalar mode oracle, pickup-shifted body
  gates, pending no-write, hidden-support merging, recursive cadence
  successors, observation separation, and CE-0176 `10065 -> 10075`.
  Complete Linux discovery passes 1,175 tests in 14.858 seconds. The exact
  Windows UNC suite passes 1,175 tests in 30.329 seconds with three existing
  skips.
- **Authority and next gate:** this is offline transition authority, not a
  global survival solve, exact-version publication, live action, or NMNB
  result. Damage identities remain diagnostic and unfocused combat remains
  disabled. Continue SEM-MODE-C integration with exact body/flag/geometry
  version and physical-update clock, then SEM-MODE-D/E. Do not repeat the
  unchanged Stage-5 observer workload.

## SEM-MODE-C physically replays the local recurrence post hoc

- **Observed static gate:** revalidated `player_update` at `0x0044C390`.
  Instructions `0x0044C4C6..0x0044C4CB` invoke the relevant input/mode update
  only when player phase byte `+0` is neither `1` nor `2`. IDA instruction
  `0x0044C4C6` now has a material comment recording this phase boundary, the
  retained Stage-5 recurrence result, and the warning that manager frame is
  not a universal player-update clock.
- **Retained implementation:** added
  `scripts/analysis/th08_enemy_mode_recurrence_report.py` and six focused
  tests. The generator streams a raw trace, retains its full SHA-256, checks
  capture diagnostic/no-authority roles, and replays
  `step_route2_enemy_mode_state` only across adjacent coherent intervals with
  no non-decision boundary, stable stage/epoch/non-suppressed phase, no Bomb,
  unchanged effective focus, and active/held/pending/dispatched focus
  agreement.
- **Observed physical result:** complete original-game Stage-5 trace
  `lunatic_route2_stage5_unattended_20260730_041408` contains 11,708 adjacent
  coherent candidate pairs. Of those, 9,839 are eligible and all 9,839 match
  the native `+3/+5/+8` tuple; 1,869 are excluded with explicit reasons.
  Eligible manager deltas cover 1 through 10.
- **Observed clock counterexample:** twenty intervals contain an intervening
  non-decision record and remain outside the recurrence claim. Three
  auto-confirm gaps have manager deltas `1800`, `1800`, and `1801`, while the
  transition counter advances only `79`, `328`, and `553`. The audit retains
  them as CE-0120 clock-boundary evidence rather than treating manager delta
  as a universal physical update count.
- **Evidence:** compact report
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_041408.enemy_mode_recurrence_report.json`
  is deterministically reproducible with SHA-256
  `da13e633dd11b65e837062b9286b736ee5dce618f57ba9bb1930d8c8c0315a66`.
  Its ignored 482,944,752-byte source JSONL hashes to
  `773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`.
- **Validation:** six focused report tests, deterministic full-source
  regeneration, Ruff, format, `py_compile`, and `git diff --check` pass.
  Complete discovery passes 1,181 tests in 14.647 seconds on Linux and 30.388
  seconds through the exact Windows UNC loader; Windows retains three
  existing skips.
- **Authority:** this is physical post-hoc recurrence evidence inside a strict
  ordinary-update boundary. It is not an exact future hazard schedule,
  universal-clock proof, action, hard-survival, live-publication, or NMNB
  authority. Continue with an immutable body/flag/geometry producer and
  body-set differential before any survival integration.

## CE-0193 rejects atomic complete-mask pickup

- **Retained implementation:** added
  `scripts/analysis/th08_enemy_mode_bodyset_report.py` and seven focused
  tests. The source-hashed streaming audit preserves the injective 36-token
  no-Bomb action identity, recorded ordered dispatch, coherent pipeline
  roots, exact native mode tuple, and endpoint contact/damage body sets.
  Exact hidden roots are tested as members of the next observed estimator
  belief; singleton branch state is not incorrectly equated with a
  multi-value observed support. The earlier
  `scripts/analysis/pipeline_pickup_audit.py` now also counts ordered partial
  pickups and adds an explicit live-promotion blocker; its focused regression
  proves the atomic continuity audit can no longer pass this signature
  silently.
- **Observed physical counterexample:** at retained trace lines `297 -> 298`,
  controller frames `386 -> 389`, and captured manager frames `384 -> 387`,
  issue transaction `0x65 -> 0x61 -> 0x41` releases Focus then Down. Native
  next `input_current`/active mask is intermediate `0x61`; held/pending final
  target is `0x41` with remaining support `[1]`. Player mode counter advances
  `378 -> 381`, and all four declared atomic delay branches match the
  mode/body endpoint, but none contains successor active mask `0x61`.
- **Observed scope:** among 11,708 adjacent coherent pairs, 7,162 meet the
  strict action-conditioned endpoint contract. Of these, 3,835 have a
  compatible causal branch and 3,327 fail the diagnostic manager-frame
  decomposition. The 381 intervals that directly observe a recorded
  ordered-transition intermediate mask are CE-0193. The remaining 2,946
  failures are retained only as unknown-direction manager/player/issue phase
  evidence: 85 mode/body, 2,858 pipeline, and 3 joint-branch mismatches.
- **Observed breadth:** the 381 direct witnesses span 141 distinct complete
  transaction/observed-mask signatures. Native observation occurs after edge
  1/2/3/4 in `307/62/10/2` cases. Focus path counts include 48 cases before a
  pending acquire, 25 after acquire but before later edges, and 75 after
  release but before later edges; every witness retains Shot. This is a
  recurring transaction-timing mechanism, not one anomalous mask.
- **Evidence:** deterministic compact report
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_041408.enemy_mode_bodyset_report.json`
  is 178,661 bytes and hashes to
  `db411650de15a050c5ffb815f0f075c538b4ec16cbc01f2fa0f47e403f678464`.
  Its ignored 482,944,752-byte raw source hashes to
  `773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`.
  The report intentionally exits nonzero/fails its strict model gate.
- **Correction boundary:** model an issue as an ordered key-transition
  transaction that native input can sample between edges; carry final
  desired mask, remaining transition suffix, older pending state, and active
  native mask on a revalidated player/input phase clock. Preserve
  complete-mask no-write and non-clairvoyant observation merging. Exact
  future body/flag/geometry production now follows this actuator correction,
  not precedes it.
- **Validation:** seven focused body-set-report tests and five focused
  pipeline-pickup-audit tests pass. Ruff, format, `py_compile`,
  `git diff --check`, and byte-identical full-source regeneration pass.
  Complete discovery passes 1,189 tests in 14.485 seconds on Linux and
  30.602 seconds through the exact Windows UNC loader; Windows retains three
  existing skips.
- **Authority:** this reuses the original-game whole-stage trace and launches
  no game or THPRAC. It adds physical post-hoc falsification, not action,
  hard-survival, live-publication, unfocused-combat, or NMNB authority. Do
  not repeat the unchanged Stage-5 workload.

## CE-0193 ordered transaction offline correction

- **Independent scalar state:** added
  `scripts/touhou_control/ordered_input_transaction_oracle.py`. One exact
  state retains active native mask, held final desired mask, remaining
  ordered single-key-edge endpoints, and a positive final-completion
  publication deadline. Real writes append releases-in-bit-order then
  presses-in-bit-order after an older suffix. Complete-mask no-write samples
  no delay and preserves the exact suffix/deadline.
- **Causal uncertainty:** before the deadline nature may stutter or consume
  any monotone non-final prefix; at the deadline final held input is forced.
  One selected final mask is applied uniformly to every hidden state, and
  queue/deadline states merge only under the same active/held observation.
  This is a conservative scalar publication abstraction, not a measured
  Win32 scheduler.
- **CE-0193 regression:** settled `0x65`, selected `0x41`, and completion
  delay two create queue `(0x61, 0x41)`. The first publication permits old
  `0x65` or observed intermediate `0x61` but forbids final; the second forces
  `0x41`.
- **TH08 composition:** `scripts/th08_enemy_mode.py` retains its atomic APIs
  as explicitly restricted baselines and adds an ordered SEM-MODE decision
  primitive. At its declared post-priority-17 root, priority 9 consumes the
  current mask, player `+3/+5/+8` advances, priority 11 projects contact and
  damage body sets, and priority 17 publishes the next hidden prefix.
- **IDA revalidation and database changes:** priority-9 player movement reads
  `g_input_current` at `0x0044AEE8`; priority-17 later copies raw input into
  current at `0x00452347`. Material comments at both addresses record the
  callback order and CE-0193 consequence. The latter also records the
  retained physical `0x65 -> 0x61 -> 0x41` witness. No instruction bytes or
  types changed.
- **Validation:** nine game-neutral and four TH08 ordered regressions pass,
  including release/press order, deadline, no-write, overwrite, observation
  merging, Bomb/unsupported masks, intermediate Focus mode divergence, and
  missing intermediate action identity. The prior fifteen SEM-MODE tests
  remain green. Ruff, format, `py_compile`, and `git diff --check` pass.
  Complete discovery passes 1,202 tests in 14.611 seconds on Linux and 30.759
  seconds through the exact Windows UNC loader; Windows retains three
  existing skips.
- **Authority:** offline/conservative only. Asynchronous capture and issue
  have not been mapped to the post-priority-17 root; physical completion
  delay, optimized/native parity, immutable future body/flag/geometry,
  movement/collision composition, and a whole-stage physical falsifier remain
  open. Live planner, actuator, cadence, damage objective, strategy, and
  gameplay are unchanged.

## CE-0193 retained publication-phase audit separates estimator and native deadline

Added `scripts/analysis/th08_ordered_input_phase_report.py` and five focused
tests. The report validates every decision dispatch in the complete retained
Stage-5 source, recomputes deterministic release/press paths, rejects Bomb or
schema/order tampering, source-hashes the raw trace, and censors replacement,
explicit discontinuity, and manager gaps beyond 64 rather than inventing a
publication latency.

Observed physical post-hoc results:

- 11,879 decision rows contain 6,423 real writes and 5,456 no-writes;
- 3,823 writes contain more than one key edge;
- 677 later captures observe an intermediate mask from the latest
  transaction;
- 2,760 separately read `previous -> current` pairs match a latest
  transaction edge, have `raw == current`, and retain the same current in the
  later coherent mode capture;
- canonical CE-0193 lines `297 -> 298` retain issue frame 386 / captured
  manager frame 387 and `0x65 -> 0x61` at edge 1/2 toward final `0x41`; and
- 5,357 final masks are first observed before replacement, 1,034
  transactions are replacement-censored, 29 manager-gap-censored, and three
  explicit-discontinuity-censored.

The report deliberately records zero atomic native edge witnesses.
`observe_state` reads raw/current/previous separately, the controller capture
is asynchronous, and the source has no priority-17 callback serial.
`AdaptiveControlDelay` uses enemy-manager-frame
snapshot-to-observed-final-input age; `ActionIssueAlignment` reads its issue
frame before dispatch. Therefore existing `control_delay_candidates` and
pending remaining support cannot instantiate the ordered oracle's post-issue
publication deadline. The 120 final masks first observed outside their issued
support are capture observations, not measured publication overruns.

The compact report is
`artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_041408.ordered_input_phase_report.json`
with SHA-256
`c8f1722587c4514c6ab538f66e73a0b13566b7aac96474805e12bcef16e0152a`.
Its 482,944,752-byte raw source hashes to
`773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`.
Five focused report tests plus the 9 ordered-oracle, 4 ordered SEM-MODE, 6
delay, and 9 epoch focused tests pass. Ruff, format, `py_compile`, and
`git diff --check` pass. Complete discovery passes 1,207 tests in 14.280
seconds on Linux and 30.116 seconds through the exact Windows UNC loader;
Windows retains three existing skips.

The next bounded diagnostic is now explicit: a default-off trace-only native
ring must record monotone priority-17 serial, raw, old current, new current,
and contemporaneous manager frame. Controller issues must retain pre/post
dispatch serial bounds and ordered transaction identity. Overflow/read
failure marks an interval unknown and must not fail-close or stop a stage.
No live authority or physical rerun was added.

## SEM-MODE-C priority-17 callback probe preflight

- **Corrected native semantics:** connected IDA revalidation of
  `replay_record_input_frame` (`0x00452310..0x00452483`) found two possible
  `g_input_current` stores, not one. The active path saves
  `previous <- current` once at `0x00452339`, writes the first raw sample to
  current at `0x00452347`, and the full recording path samples raw again at
  `0x004523B8` and overwrites current at `0x004523C7`. All five exits converge
  at `0x00452480`; callback-exit current is the state eligible for the next
  priority-9 player update.
- **IDA database changes:** material comments at `0x00452339`,
  `0x00452347`, `0x004523B8`, `0x004523C7`, and `0x00452480` record the
  one-save/two-store semantics, common epilogue, shipped bytes
  `8B E5 5D C3`, and the requirement to sample callback exit. No instruction
  bytes, names, or types changed.
- **Shipped-image proof:** the clean executable
  SHA-256 remains
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`;
  its function and epilogue/padding bytes match the connected IDB.
- **Trace-only hook:** added
  `scripts/th08_runtime/priority17_publication_probe.py`. The explicit
  default-off diagnostic detours the common epilogue through five
  unreferenced `CC` padding bytes into a 256-event remote ring. It records
  serial, manager frame, engine flags, raw/current/previous, and the callback
  recording counter, commits the event before the header serial, and then
  executes the shipped epilogue.
- **Patch safety:** activation is padding-first/detour-last. Installation and
  cleanup suspend and snapshot all target threads. Cleanup restores/frees
  only when no EIP lies in the padding trampoline or remote stub. An
  unverified partial activation must abort before gameplay; an unverified
  cleanup requires target termination after the trial. Ordinary installation
  unavailability, bounded-ring overflow, unstable reads, and read errors
  grant no evidence for the interval and do not stop gameplay.
- **Issue boundary:** `commit_physical_issue` samples the publication serial
  immediately before and after a real complete-mask dispatch. It does not
  sample no-write and catches sampler errors without suppressing physical
  dispatch. The serial bracket is observation only and is not
  `control_delay_candidates`.
- **Retention/reporting:** added
  `scripts/analysis/th08_priority17_publication_report.py`. It source-hashes
  the trace, validates serial continuity and ordered paths, retains native
  callback-exit intermediate witnesses, counts same-manager-frame serial
  edges, excludes the after-key-release final drain from unsupported
  last-transaction claims, and rejects Bomb, cleanup error, overflow, or
  unknown brackets from a complete evidence gate. Original-game practice and
  full-route supervisors retain the report automatically when the flag is
  enabled.
- **Scope:** no game was launched. The code has preflight authority only and
  changes no live action, planner, delay support, survival policy, or NMNB
  status. The next physical gate is one complete original-game Lunatic
  Stage-5 run with `--trace-priority17-publications`, hard no-Bomb,
  `stop-after-hits=0`, and ordinary whole-stage completion.
- **Validation:** 86 focused probe/report/issue/CLI/hotkey/supervisor tests
  pass. Ruff, format, `py_compile`, and `git diff --check` pass. Complete
  discovery passes 1,226 tests in 14.104 seconds on Linux and 30.564 seconds
  through the exact Windows UNC loader; Windows retains the three existing
  skips. One preceding Windows rerun transiently missed the unrelated
  auxiliary-ECL timing gate. An immediate isolated repeat passed at
  p95/p99/max `0.311/0.318/0.320 ms` against `0.5/1.0/3.0 ms`, and the
  subsequent complete suite passed; no semantic failure or code rollback is
  inferred from the one timing sample.

## CE-0194 priority-17 physical preflight stopped before decisions

- **Attempt:** original-game run
  `lunatic_route2_stage5_unattended_20260730_082428` verified target SHA-256,
  no-life-decrement, Lunatic, Sakuya/Remilia, and Stage 5. It installed the
  common-epilogue hook and established baseline serial 0.
- **Failure:** at gameplay frame 1 the existing root-only time-scale authority
  check emitted `time_scale_authority_unknown` and terminated before any
  decision. Supervisor cleanup killed the exact target; there were zero hits,
  no Bomb, and no leftover process. This is preflight evidence, not a survival
  sample.
- **Trace-loss witness:** the controller read callback serial 1 before the
  later scale exit and advanced its local cursor without emitting a decision
  batch. Final drain then began at serial 1 and retained no event. This
  falsifies committing the probe cursor before its trace row is durable.
- **Correction:** explicit diagnostic constant-root continuation is now
  scoped to either the enemy-mode observer or priority-17 observer. It remains
  unknown-direction/no-authority and conflicts with exact Final-B scale
  authority. Probe reads move after all pre-issue early exits; the serial
  cursor advances only after the containing decision row is flushed, so final
  drain can recover an uncommitted interval.
- **Next physical command:** repeat the complete Stage-5 unit with
  `--trace-priority17-publications` and
  `--diagnostic-continue-root-only-scale`. Preserve hard no-Bomb,
  `stop-after-hits=0`, stage-level scope, and normal completion.
- **Correction validation:** focused probe/report/issue/CLI/hotkey/supervisor
  tests pass. Ruff and `git diff --check` pass. Complete discovery passes
  1,226 tests in 14.851 seconds on Linux and 30.813 seconds through the
  Windows UNC loader, with three existing Windows skips.

## Priority-17 Stage-5 physical publication gate and CE-0195/0196

Corrected original-game run
`lunatic_route2_stage5_unattended_20260730_083416` completed all of Lunatic
Stage 5: frames `1..43376`, 12,109 decisions, `route_complete`, hard no-Bomb,
no-save, and exact process/input cleanup. It took 11 hit edges. The canonical
fresh nonspell contact is frame 4,323, nine frames after robust-action
exhaustion; CE-0196 retains it separately from ten coupled post-respawn
contacts. The diagnostic constant-root scale proxy and callback hook have
unknown-direction effects, so no aggregate survival comparison is promoted.

The supervisor's first report attempt failed with
`publication serial moved backward: 21 -> 1`. Streaming inspection proved the
physical serial sequence monotone. CE-0195 identifies the analyzer bug:
interval lookup applied a forward-only validator to older events outside the
requested transaction. The corrected selector filters by unsigned forward
membership. It also withholds first-publication timing when any earlier
serial in the prefix is missing. Five focused report regressions pass.

The corrected deterministic report retains 30,904 native callback exits,
6,565 real writes, and 5,544 no-writes. All 6,565 pre/post-dispatch intervals
are complete; 1,223 writes contain callback exit during dispatch, and 478
events directly observe a non-final ordered mask. Consecutive retained events
include 122 serial advances with no `enemy_manager_frame` advance. This is
observed physical closure that callback publication may occur inside ordered
dispatch and that manager frame is not the universal publication clock.

Fully retained prefixes first observe the final held mask in callback steps
`{1:4893, 2:1648, 3:17, 4:2, 5:1}`. One complete one-step interval is
replaced without publishing its transient target; this remains overwrite
censoring rather than an unconditional deadline. Four long gaps drop
`46/296/520/852` events, so the global report integrity remains false and
negative claims across them remain unknown.

The retained compact report hashes to
`395ce5384e14e815afe6a5ce1977b165a94344906f3eb483a56c2484056be9b8`.
Its ignored 501,784,095-byte source hashes to
`319b22f94dfdb2ce5322a0779839f94e6d03b6866c2d985e75e7c323473cae2f`.
The physical observer closes evidence collection, not the live adapter. The
next recurrence must branch callbacks during issue, preserve explicit gaps
and replacement censoring, and avoid forcing every superseded transient
target to become visible before independent optimized parity and future
geometry integration.

The five focused report tests and complete discovery pass 1,227 tests in
14.858 seconds on Linux and 30.896 seconds through the Windows UNC loader;
Windows retains the three existing skips. A fresh in-memory regeneration from
the 501.8 MB raw source is structurally identical to the retained report.

## SEM-MODE-C asynchronous ordered-input scalar/native checkpoint

- **Checkpoint:** `229ab86` (`Implement asynchronous ordered-input parity`).
- **Formal correction:** added
  `notes/ASYNC_ORDERED_INPUT_PUBLICATION_CONTRACT_20260730.md`. One exact
  actuator state is `(active, held, remaining ordered suffix, latest
  completion deadline)`. A real write appends release/press edges after any
  older suffix. Nature branches callback count and monotone suffix cuts
  during dispatch. Each callback's world step consumes the prior active mask
  before priority 17 publishes the next mask. No-write preserves the exact
  state and samples no dispatch phase.
- **Independent scalar/TH08 composition:** the game-neutral scalar recurrence
  retains consumed and published histories separately. The TH08 layer
  composes dispatch callbacks with priority-9 player mode, priority-11
  contact/damage body gates, total cadence, and observation-compatible
  successor merging. Missing complete-mask action identities, Bomb, invalid
  supports, and an empty issue/cadence intersection fail closed offline.
- **Independent native implementation:** added the C++ exact-state enumerator
  `touhou_async_ordered_input_issue_v1`, Python marshalling, and build/ABI
  coverage. It is implemented independently of the scalar oracle. The
  checked production ABI grows from 46 to 47 symbols. Native queue/support/
  callback/branch caps return invalid or unresolved; exhaustion is never
  interpreted as losing.
- **Exact differential:** 115 exact physical/adversarial state/action cases
  and all 2,405 scalar/native branches match on Linux and Windows. The corpus
  covers Focus, Shot, reversals, multi-release/press, stutter, edge skips,
  final-in-dispatch, pending overwrite, no-write, CE-0193, and the Stage-5
  superseded `0x04` transient target. Both physical witnesses retain full
  branch histories.
- **Artifacts:** Linux report
  `artifacts/runtime_reports/async_ordered_input_native_differential_linux_20260730.json`
  hashes to
  `f9fbe98bac5fe9481e8baf4bc09c51b328b12fb744d724132dd0d7adb7e3e4b0`;
  Windows report
  `artifacts/runtime_reports/async_ordered_input_native_differential_windows_20260730.json`
  hashes to
  `91df49e10f919e4c1681cb617563c311c55902b0500ee2ba5b379a23b391885a`.
  Removing platform/native-binary identity yields identical content.
- **Validation before full checkpoint:** scalar 17/17, TH08 composition 9/9,
  native differential 5/5, report 2/2, and native ABI 6/6 focused tests pass.
  Both compilers pass `-Wall -Wextra -Wpedantic -Werror`; Linux sanitizer
  execution passes 5/5. Complete Linux/Windows discovery passes
  1,247/1,247 in 14.990/30.874 seconds; Windows retains the three existing
  skips.
- **Authority:** observed physical occurrence and finite implementation
  parity only. The callback/deadline supports, dispatch/cadence joint
  automaton, immutable future body/flag/geometry producer, movement/collision
  composition, publication deadline, and whole-stage falsifier remain open.
  No live action, hard survival, damage/unfocused combat, or NMNB authority
  changes. No game was launched and no unchanged Stage-5 repeat is
  authorized.

## Immutable future body/flag/geometry offline schedule checkpoint

- **Producer audit:** no complete predictive producer exists. Live future
  coverage remains `UNKNOWN` from root+1. Current body memory, G5 evidence,
  and decision-bracketed Stage-5 traces do not exhaust unseen births, future
  non-mode flags, despawns/transforms, per-update geometry, or joint
  scheduler/cadence support.
- **Finite representation:** added
  `notes/IMMUTABLE_FUTURE_BODY_FLAG_GEOMETRY_SCHEDULE_CONTRACT_20260730.md`
  and `scripts/th08_future_body_schedule.py`. One digest covers root update,
  clock/source identity, all equal-horizon hidden branches, body identities,
  mode-independent flags, and binary32 geometry.
- **Causality correction:** each asynchronous successor automatically carries
  the exact projected body flags and geometry visible at the next decision.
  Auxiliary observation code cannot merge different observed geometry.
  Hidden schedule histories may merge only after their complete body and
  actuator/mode observations converge.
- **Differential:** the deterministic report
  `artifacts/runtime_reports/th08_future_body_schedule_differential_20260730.json`
  hashes to
  `66f0105028451f84b01cdef7c7fe198d9a50d6114ca72df693aa0242da062ee8`.
  Linux and Windows generate identical bytes. Three cases cover eight exact
  branches and 27 per-update comparisons with zero mismatch. The CE-0176
  semantic fixture opens all 16 final contact/damage gates; its geometry is
  synthetic.
- **Focused validation:** ten schedule/report tests pass on Linux and
  Windows. Complete Linux/Windows discovery passes 1,257/1,257 in
  14.286/31.013 seconds, with the three existing Windows skips. Ruff and
  `git diff --check` pass.
- **Authority/next gate:** offline supplied-schedule representation,
  immutable version, and body-set parity only. No live action, hard survival,
  damage, physical, or NMNB authority changes. Build a predictive producer
  for the omitted event classes before viable-state/safe-action-mask
  integration or another stage-level physical run.
