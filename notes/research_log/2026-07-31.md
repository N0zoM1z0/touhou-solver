# TH08 Research Log — 2026-07-31

## WS-H Focus/Shot emission foundation closes the first combat-state omission

- The user reprioritized high-ROI, stage-neutral route/combat/resource work
  ahead of the remaining individual H1 hostile-bullet producer.
- **Observed native semantics:** `player_emit_shot_level` at `0x00450F60`
  selects primary/secondary SHT from Focus-logic byte `+0x03`, advances normal
  levels while truncated Power reaches each threshold, scans 128 player-shot
  slots in native record order, and evaluates no callback when the pool is
  full. `player_update_shot_cadence` at `0x00451500` emits only on an integer
  timer change. Timer current is base `+0x08`; change compares base `+0x00`.
- **Observed callback semantics:** shipped callback-0 table index 7 reaches
  `0x004501B0`. Its due path consumes one `rng_next_signed_unit`, hence two
  gameplay-RNG u16 calls, stores a narrow random angle around `-pi/2`, and
  replaces polar velocity. The function is now named
  `player_shot_emit_due_random_spread` in IDA with a revalidated comment.
- **Observed shipped data:** all normal primary `ply02a.sht` records use
  callback 0. Normal secondary `ply02as.sht` player-center records use
  callback 0 and option records use callback 7. At Power 128 the normal
  selector reaches level 5.
- `scripts/th08_player_shot_model.py` now executes normal level selection,
  native pool/record order, callbacks 0/7, exact callback RNG counts, and the
  stored binary32 random angle. Unsupported callbacks fail closed.
  Native-bit polar/trigonometric geometry remains unknown-direction.
- A dedicated non-live capture in `scripts/th08_player_shot_runtime.py`
  retains the shot timer `(previous, fraction_bits, current)` and all 128 slot
  state words in two reads. Rolling native snapshot schema v4 records this at
  the root and every headless/natural endpoint, includes it in same-action
  determinism, and does not add the 148-KiB read to the live sensing hot path.
  No new native/game trial was run.
- **Inferred root-2129 narrowing:** the retained v3 root has active input
  `0x05`, Focus logic 1, Power 128, and four u16 calls before the seven
  already attributed hostile births. Eight cadence classes of the focused
  level-5 SHT can consume exactly four calls when enough slots are free.
  Player shots are therefore a well-ordered compatible explanation of the
  old pair-0 prefix, but not a unique causal proof because v3 omitted shot
  timer and pool state. Hostile birth 1220 still lacks a producer.
- Deterministic audit
  `artifacts/runtime_reports/th08_route2_focus_shot_emission_root2129_20260731.json`
  has SHA-256
  `f78e820fe7aeabd12d5c6b4a2fd901462a54ada26758f3ba11fae615318738e8`.
  Regeneration is byte-identical. It grants no live Focus/Shot ranking or
  physical predictive authority.
- Focused model/capture/report tests, Ruff, and diff checks pass. Complete
  Linux discovery passes 1,380 tests in 13.531 seconds. Exact Windows UNC
  discovery passes 1,380 tests in 27.294 seconds with the three existing
  skips.
- `COMBAT-FAST-01` remains at semantic-foundation status. Its next useful gate
  is a v4 identical-root ordinary-enemy corpus comparing focused, unfocused,
  and causal refocus schedules inside the unchanged survival-feasible set.

## WS-H kill-before-saturation audit rejects the old observer as kill evidence

- **Observed native boundary:** player-shot damage is resolved and subtracted
  at `enemy+0x2DFC`, then ordinary defeat mode 0 clears active flag bit 0 at
  `0x0042D899` in the same `enemy_manager_update`. The revalidated boundary is
  now recorded at that IDA address. A post-update active-only observer cannot
  retain the ordinary kill edge.
- A new deterministic one-pass audit preserves observation-bounded presence
  episodes, start/end uncertainty windows, epoch censoring, HP/damage flags,
  and the complete digest of all episodes. It never promotes slot continuity
  to native generation or disappearance to an end reason.
- **Observed retained trace result:** 11,735 observations and 133,070 active
  rows produce 1,823 presence episodes. Of 1,789 ended episodes, every last
  active row has positive HP and defeat mode 0. Seventeen are
  damage-adjacent disappearance candidates; zero are verified kills.
- Compact report
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260728_224116.presence_episode_audit.json`
  has SHA-256
  `d483268f3e69443a5d36ace92b71e8da165f87b59e45d761bcdb99fcb2865162`;
  regeneration is byte-identical. The immutable 483-MiB input remains
  SHA-256
  `569a8767e25806661acf9b66365cdf227189d53bb525a906d913cd08fe61d8bb`.
- `COMBAT-KILL-01` therefore remains open. The next valid gate needs a
  same-update end event captured before slot reuse and joined to damage
  source, native clear path, ECL state, drops, RNG, and later hostile births.
  No unchanged physical run was requested or performed; work proceeds to
  another high-ROI WS-H task.
- Focused combat-progress/audit tests and Ruff pass. Complete Linux discovery
  passes 1,382 tests in 13.002 seconds. Exact Windows UNC discovery passes
  1,382 tests in 27.356 seconds with the three existing skips.
- CE-0216 retains the observer failure. The counterexample index's prior
  count was stale at 45 despite CE-0170 through CE-0215 being contiguous; it
  is reconciled to the actual 47 entries through CE-0216.

## WS-H natural Power-0 audit retains route variance without causal promotion

- **Observed native correction:** revalidated `item_manager_update` at
  `0x00440500` reads `g_player_route_id` at `0x0164D0B1` for its route/team
  1/6 auto-homing exception. The inherited solver input
  `stage_load_index` was incorrect. The item model/config now carries
  `route_id`, the IDA instruction has a revalidated comment, and a
  distinguishing test covers route IDs 1, 6, and 2. Route-2 behavior remains
  on the ordinary branch.
- The deterministic `POWER-ROUTE-01` audit binds two accepted natural
  Route-2/Lunatic Game Start sessions to their complete raw SHA-256 values and
  includes only Stage-1/epoch-0 decisions strictly before the first
  `hit_started` row. Both start at Power 0, lives 2, Bombs 3, remain hard
  no-Bomb, and use the passive item policy with item objectives disabled.
- **Observed retained result:** run `002115` has 789 clean decisions through
  frame 2,018 before first hit 2,022 and gains Power `0 -> 5`; run `222529`
  has 712 clean decisions through frame 1,690 before first hit 1,692 and
  remains at Power 0. At or before common clean horizon 1,692 the observed
  Power values are 3 and 0.
- The five `+1` intervals each contain one visible disappeared small-Power
  candidate, but decision gaps omit the same-update pickup edge, so
  item-specific source authority remains false. Neither prefix reaches the
  first normal-shot threshold 8.
- The roots differ and execute no item-seeking counterfactual. The report
  therefore grants route-faithful observation authority only: no causal
  collection policy, threshold shot effect, later combat benefit, survival
  benefit, or action authority.
- Compact report
  `artifacts/runtime_reports/lunatic_route2_power0_clean_prefix_audit_20260731.json`
  has SHA-256
  `837244e9ca86bd70271cc4fa311bbeb67313e4d6c5281285de2872df5e791a6f`;
  regeneration is byte-identical.
- The next causal gate requires a natural first-hit-bounded same root,
  survival-feasible action branches, a same-update item pickup/end event,
  carried world state through a Power threshold, and a later
  shot/damage/kill/survival join. No new physical trial was requested or run.
- Focused tests and Ruff pass. Complete Linux discovery passes 1,385 tests in
  13.048 seconds. Exact Windows UNC discovery passes 1,385 tests in 27.239
  seconds with the three existing skips.
- CE-0217 retains the inherited field-identity error. The counterexample
  range now contains 48 entries through CE-0217.

## CONTENT-01 pins shipped mandatory-stage bytes and ECL structure

- Clean pinned `thpatch/thtk` source at
  `892114a0fcaa0bbdaaecf3cb4ad56f758683fb40` was built out of tree. The
  shipped archive is 46,838,025-byte `th08.dat`, SHA-256
  `9d7edf43b8ddd347cbb641836f6b5050745dd936f688daebbf9382ca557043bb`,
  paired with the accepted executable SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
- **Observed archive differential:** pinned `thdat` and the independent
  native-derived `PbgzArchive` agree in order on name, declared size, and
  stored size for all 317 entries.
- **Observed content differential:** all 188 scoped resources—24 ECL, 18
  STD, 113 ANM, and 33 message files—decode byte-identically through
  `thdat` and the independent repository PBGZ plus `edz?` path. All 42
  pre-existing tracked wrapped and 42 decoded artifacts also match.
- **Observed ECL differential:** pinned `thecl` and the repository parser
  agree on subroutine count, timeline count, and complete opcode sequence for
  all 24 ECL files, totaling 38,696 records including negative-time timeline
  terminals.
- Stage 3, Stage 4A, Stage 5, and Final B now have explicit filename-scoped
  content-set digests. This closes only `CONTENT-01` byte identity; runtime
  resource selection, event reachability, opcode semantics, update order, and
  action authority remain none.
- **Observed external limitation:** release-12 `thstd -d8` succeeds on
  `stage3.std` and `stage3_s.std`, receives `SIGSEGV` on the Stage-4A pair,
  and `SIGABRT` on the Stage-5/Final-B pairs. Because all eight STD payloads
  already passed independent extraction/decoding, this does not reopen
  content identity or block `CONTENT-02`.
- Retained manifest
  `artifacts/runtime_reports/th08_immutable_content_manifest_20260731.json`
  has SHA-256
  `3a52b6f485ada63c833f77ae8cd1653f469ae4fadeef3c38336a737c7e753ae1`;
  regeneration is byte-identical.
- Focused manifest-helper tests and Ruff pass. Complete Linux discovery
  passes 1,390 tests in 12.760 seconds. Exact Windows UNC discovery passes
  1,390 tests in 26.724 seconds with the three existing skips. No game or
  controller process was run.
- CE-0218 retains the external STD parser failure. The current counterexample
  range contains 49 entries through CE-0218.

## CONTENT-02 retains a mandatory-stage static event-atlas foundation

- A deterministic Route-2/Lunatic analyzer now validates every input against
  the exact `CONTENT-01` manifest before classifying mandatory-stage ECL
  events and running conservative route/difficulty-aware flow analysis.
- **Observed pinned-content result:** Stage 3, Stage 4A, Stage 5, and Final B
  contribute 714, 775, 696, and 700 event occurrences respectively, for
  2,885 total. The four CFGs retain 63, 59, 87, and 85 reachable subroutines
  and report no unresolved dynamic subroutine edge.
- The atlas retains exact symbolic identities, overlapping event classes,
  eligibility, conservative CFG status, confidence, arguments, callback
  references, and per-file hashes. Spell-practice ECL files are hash-bound
  references only and carry no natural-route authority.
- **Observed static unknowns:** all 19 unknown eligible occurrences are
  timeline opcode `0x06`: 4/4/2/9 across Stage 3/4A/5/Final B. Shared unknown
  opcode `0x09` does not occur in these route timelines.
- Retained workloads physically reach all four stages, but no event-specific
  runtime image/program-counter join exists. The unknown list is therefore
  prioritized only by workload-level reach and static count. Final B is the
  first capture target; this is not a survival-risk ranking.
- Retained report
  `artifacts/runtime_reports/th08_mandatory_event_atlas_20260731.json` has
  SHA-256
  `6b2580ebffa7658ede7ca756fb3718f969b2c365fa43f32c24e83b2733f16139`;
  regeneration is byte-identical.
- Focused tests and Ruff pass. Complete Linux discovery passes 1,393 tests
  in 12.715 seconds. Exact Windows UNC discovery passes the same 1,393 tests
  in 27.3 seconds with the three existing skips.
- `CONTENT-02` remains open: static reachability is not native execution,
  opcode-side-effect, future-event, or action authority. No physical trial
  was run. Capture debt does not block other high-ROI strategy work.

## WS-H corrects forced-HP-zero and end-reason semantics

- **Observed native correction:** inherited
  `enemy_manager_clear_active_with_score_items` at `0x0042EFB0` does not
  clear active enemies. It sets eligible active non-boss HP to zero, handles
  score items, unlinks the parent relation, and starts a configured end
  subroutine. The actual active-bit clear is deferred to manager defeat
  processing.
- The four revalidated callers distinguish opcode `0x5F`, spell finish,
  boss-defeat cleanup, and message/dialogue start. None is a player kill.
  Opcode `0x5F`, the ECL catalog, and `CONTENT-02` now use
  `forced_enemy_hp_zero`; the atlas was regenerated byte-identically with the
  corrected SHA-256 above.
- **Observed lifecycle boundary:** both first-inactive 480-slot allocators
  and their immediate `-1` retirements are now recorded. Later exact
  retirement sources are main-VM `-1`, the offscreen-cull control edge, and
  defeat-mode-0 active clear.
- `scripts/th08_enemy_end_semantics.py` lowers only those ordered active-clear
  edges into the generation ledger. It verifies player-shot kill only when
  exact pre-damage HP, resolved damage, arithmetic post-HP, no preceding
  forced zero, mode 0, and the active clear all agree. Endpoint disappearance
  or frame damage alone remains insufficient.
- IDA functions at `0x0042EFB0`, `0x0042B2F0`, and `0x0042A680` were renamed
  to their revalidated zero-HP, parent-unlink, and inherited-VM allocator
  roles; eight material instruction/callsite comments were updated.
- Focused tests and Ruff pass. Complete Linux discovery passes 1,400 tests
  in 12.956 seconds. Exact Windows UNC discovery passes the same 1,400 tests
  in 27.9 seconds with the three existing skips.
- CE-0219 retains the inherited semantic error. No game or controller was
  run. Ordered native event-ring capture remains the next gate; the old
  active-only observer must not be repeated.

## WS-H implements the bounded ordinary-enemy lifecycle event ring

- **Observed native boundary:** IDA revalidation and exact byte reads pin two
  post-template allocation sites, five active-bit-clear retirement sites, and
  the separate forced-HP-zero write. The forced-zero event records the caller
  return address and accepts only spell finish, opcode `0x5F`, boss cleanup,
  or message start.
- `scripts/th08_runtime/enemy_lifecycle_probe.py` implements a default-off
  256-entry, 40-byte/event native ring. Each x86 stub preserves flags and
  registers, invalidates an overwritten slot, records pre-state, replays the
  exact shipped bytes, records post-state, and publishes slot/header serials
  in order.
- The decoder validates ordinary-pool pointer/stride, event kind, forced-zero
  caller, slot serial, and double-read header stability. Overflow and trace
  truncation have exact dropped counts; an unstable overwrite becomes
  `race_unknown`, never partial evidence.
- Multi-site install verifies every shipped span before activation. Synthetic
  tests retain reverse rollback after a mid-activation failure, cleanup
  quiescence across all patch/stub ranges, reverse exact restoration, and
  unsafe no-free behavior after a failed restore.
- Eight IDA comments now record the exact allocation/retirement/forced-zero
  trace boundaries. The database still describes shipped semantics; no hook
  was installed.
- Focused discovery and Ruff pass. Complete Linux discovery passes 1,414
  tests in 13.197 seconds. Complete Windows UNC discovery passes 1,414 tests
  in 28.768 seconds with the three existing skips.
- No game, controller, supervisor, or native replay was run. The ring has
  implementation/synthetic-test authority only; runtime generation, kill,
  emission-prevention, strategy, and action authority remain none.

## WS-H rejects coarse Focus-width ranking and completes the native HP gate

- Revalidated the complete player-shot path from `enemy_manager_update`
  (`0x0042C660`) through `player_compute_damage_to_enemy` (`0x00451670`) to
  the flags-bit-`0x08` HP subtraction at `0x0042D275`.
- Added `scripts/th08_enemy_damage_model.py`, separating manager-update,
  damage-block, shot-collision, and HP-subtraction gates with explicit blocked
  reasons. Corrected boss progress to include the HP flag, player transition,
  Bomb, spell-active, and active-spell-owner state.
- Extended player-shot semantics for state/type eligibility, type-4/5 mode
  suppression, post-overlap hit callbacks, unsupported update callbacks,
  piercing types, and the type-3 velocity exception.
- Added a deterministic Route-2 atlas over all six normal Power partitions,
  the native 0..19 firing-timer cycle, and target rises 64/128/192. Focused
  callback-7/option geometry is explicitly an optimistic continuous outer
  envelope; fixed callback-0 geometry is a scalar projection without a
  native-bit trigonometric differential.
- The atlas falsifies the general “unfocused is wider” heuristic: focused
  outer support is wider in 14/18 rows and unfocused support in 4/18.
  Empty-pool nominal base damage is also non-monotone: unfocused is higher
  only at Power 80..127.
- Retained
  `artifacts/runtime_reports/th08_route2_normal_shot_coverage_atlas_20260731.json`,
  SHA-256
  `2d62726829fd18a3f0df94f1f37862cdbd5a4e02eb7d3834269548bea97f73f4`.
- Added seven durable IDA comments at `0x0042C936`, `0x0042C95D`,
  `0x0042CF47`, `0x0042D08B`, `0x0042D275`, `0x004516A9`, and `0x004518DC`.
- Focused Ruff and 28 targeted tests pass. Complete Linux discovery passes
  1,430 tests in 13.398 seconds. Complete Windows UNC discovery passes 1,430
  tests in 28.178 seconds with the three existing skips.
- No live Focus ranking, targeting, damage objective, strategy promotion,
  physical predictive authority, TH08 process, or gameplay trial was added.

## WS-H corrects the item recurrence and completes the static Power ledger

- **Observed native correction:** small/large Power additions that reach or
  cross 128 immediately write canonical Power 128. The old deterministic test
  expected Power 124 plus a large item to remain 132.
- CE-0220 also corrects two pool-order omissions: allocation uses a persistent
  cyclic 2,096-slot cursor, and update/pickup follows active linked-list
  allocation order rather than numeric slot order.
- Full-Power conversion at `0x00441450` changes other active type-0/2 items to
  overflow type 8 and conditionally writes `(vx, vy)=(0,-0.5)` before their
  later active-list turns.
- `ItemPoolState` now retains allocation cursor and active order. Deterministic
  regressions cover a `(slot 10 large, slot 2 small)` ordering adversary,
  non-lowest free allocation, out-of-range cursor/RNG preservation, capped
  Power, and before/after-conversion motion.
- Added a 387-row exhaustive static ledger over Power 0..128 and requested
  small, large, or Full Power pickups. Each row retains effective type, capped
  result, crossed thresholds, conversion, and both Focus-conditioned SHT
  capability deltas.
- Retained
  `artifacts/runtime_reports/th08_route2_power_capability_ledger_20260731.json`,
  SHA-256
  `06a12eb6f2de97823286613322f6da9fbeac7abec490099ee3298bc6acf1dcf9`.
- Added six durable IDA comments at `0x00406FB0`, `0x004400BD`,
  `0x00440413`, `0x00440D5F`, `0x004411D8`, and `0x00441496`.
- Focused Ruff and 25 targeted tests pass. Complete Linux discovery passes
  1,437 tests in 13.586 seconds. Complete Windows UNC discovery passes 1,437
  tests in 28.463 seconds with the three existing skips.
- No same-update physical pickup, safe collection preference, later combat
  benefit, live action authority, TH08 process, or gameplay trial was added.

## WS-H corrects Boss HP/timeout transition ordering

- Revalidated that `enemy_manager_update` starts at `0x0042C660` and runs the
  short-circuit HP/timeout transition loop at `0x0042CE31` before player-shot
  HP subtraction at `0x0042D349`.
- Health thresholds use strict signed `current_hp < threshold`, scan slots
  `0..3`, restore current/phase-start HP to the selected threshold, clear that
  slot, and disable timeout. The loop can consume another already-crossed
  slot.
- Timeout uses the integer timer only, loses to any health transition, and
  restores the greatest strictly positive retained threshold before resetting
  the timer.
- CE-0221 records the old decoder failure: post-damage HP 490 with retained
  thresholds `(500, 100, -1, -1)` was mislabeled as a phase ending at 100.
  Native still considers 500 the current boundary and transitions on the next
  eligible enemy update.
- `th08_boss_phase.py` now retains every active threshold, accepts signed
  post-damage HP, projects the exact bounded transition prefix, and exposes
  pending completion separately from a confirmed phase change.
- The game-neutral tracker attributes health versus timeout only across a
  stable bracket with matching gameplay epoch, stage, and Boss pointer.
  Missing/unbracketed exits remain unknown. Sensing adds pending/cause, health
  progress, and time remaining without changing live ranking or action
  authority.
- Five IDA comments retain the order at `0x0042CE31`, `0x0042B50B`,
  `0x0042B99C`, `0x0042BA15`, and `0x0042D349`.
- Detailed boundary:
  `notes/research/stage5_combat/BOSS_PHASE_TRANSITION_MODEL_CONTRACT_20260731.md`.
- Seven focused Boss tests, four generic progress tests, the sensing-trace
  regression, and Ruff pass. Complete discovery passes 1,444 tests in 13.482
  seconds on Linux and 28.529 seconds through the Windows UNC loader, with
  three existing skips. No game, controller, replay, or physical trial was
  run.

## WS-H builds a route-wide source/emission candidate atlas

- Revalidated the ordinary-source lifetime boundary: active bit clear skips
  later source updates at `0x0042C88D`; main ECL and staged-periodic emission
  run before player-shot HP subtraction; and mode-0 retirement does not
  cancel already-live bullets or lasers.
- Built
  `scripts/analysis/th08_source_emission_program_atlas.py` over pinned
  Route-2/Lunatic Final-B ECL for Stages 1/2/3/4A/5/Final B.
- Same-source closure includes only direct calls, installed interrupt
  subroutines, and same-enemy auxiliary VMs. Child spawns, cross-enemy calls,
  and phase exits remain separate ownership boundaries.
- The six stages contain 991 eligible timeline spawns and 70 unique root
  programs. Twenty-two programs are conservatively Boss-possible. The 39
  ordinary-compatible emitter candidates correspond to 909 spawns, 65 direct
  emission sites, 88 child-emitter sites, and 39 periodic-control sites.
- Retained
  `artifacts/runtime_reports/th08_source_emission_program_atlas_20260731.json`,
  SHA-256
  `6ae9494a40ff5a08143564c653b3c2007e1125063a16b1108854db84f74531b5`;
  internal pre-digest
  `33ef575bc8bafec1b557d6341357199bd227762d742856f257f0c20a25db4b48`.
  A second generation is byte-identical.
- This is a symbolic causal-root selection index, not execution evidence.
  Local VM timer guards are not kill deadlines, same-update births cannot be
  undone by later damage, children persist after creation, and existing
  projectiles persist after ordinary source retirement.
- Four IDA comments retain the boundary at `0x0042C88D`, `0x0042C9A0`,
  `0x00423159`, and `0x0042D899`.
- Detailed boundary:
  `notes/research/stage5_combat/ROUTE2_SOURCE_EMISSION_PROGRAM_ATLAS_CONTRACT_20260731.md`.
- Three focused tests and Ruff pass. Complete discovery passes 1,447 tests in
  15.217 seconds on Linux and 29.408 seconds through the Windows UNC loader,
  with the three existing skips. No game, controller, replay, or physical
  trial was run.

## WS-H wires the exact lifecycle ring into route diagnostics

- Added explicit `--trace-enemy-lifecycle-events` forwarding through the live
  CLI, hotkey contract, stage-practice supervisor, and full-route supervisor.
  The option remains default-off and trace-only.
- Installation occurs only after shipped-target identity verification and
  before the controller configuration is retained. Route/difficulty/stage,
  raw-input, and foreground checks precede the flushed baseline.
- Each changed-manager-frame decision reads one bounded batch before issue,
  embeds it in the flushed decision record, and advances the consumed serial
  only after that flush. After key release, one final batch is retained before
  reverse-order detour cleanup.
- Priority-17 and lifecycle runtime probes are rejected in the same trial.
  An unsafe activation or cleanup state releases keys and terminates only the
  exact verified TH08 image instead of leaving an instrumented game available
  for further play.
- Unavailable, overflow, race-unknown, and read-error states remain explicit.
  Nothing in the ring enters sensing, viability, ranking, or issue authority.
- Updated
  `notes/research/stage5_combat/ORDINARY_ENEMY_LIFECYCLE_EVENT_RING_CONTRACT_20260731.md`.
- Complete discovery passes 1,454 tests in 13.774 seconds on Linux and 29.844
  seconds through the Windows UNC loader, with the three existing skips.
  Ruff and diff checks pass. No game, runtime installation, replay, or
  physical trial was run.

## WS-H lowers only complete lifecycle prefixes into generations

- Added
  `scripts/analysis/th08_enemy_lifecycle_trace_audit.py` with report schema
  `th08-enemy-lifecycle-trace-audit-v1`.
- A valid baseline starts one uint32 serial chain. Exact/no-event batches must
  preserve serial distance, event count, consecutive event serials, zero
  drops, and exact pointer/slot identity.
- Nonadvancing `read_error` and `race_unknown` batches can be recovered by a
  later exact read from the unchanged serial. Overflow, malformed advancement,
  or invalid event identity cuts the authoritative prefix immediately.
- Each allocation starts one observed per-slot generation. Baseline-active
  slots remain explicit partial starts. Forced zero stays inside a lifetime;
  retirements reuse the existing fail-closed scripted/offscreen/forced-zero/
  lethal-damage classifier.
- `--require-complete` returns status 2 unless an installed probe has a
  complete post-baseline stream and final post-key-release batch.
- Detailed boundary:
  `notes/research/stage5_combat/ROUTE2_ENEMY_LIFECYCLE_TRACE_LOWERING_CONTRACT_20260731.md`.
- Seven focused adversarial tests and Ruff pass. Complete discovery passes
  1,461 tests in 13.569 seconds on Linux and 28.447 seconds through the
  Windows UNC loader, with the three existing skips.
- No game, runtime ring batch, replay, controller, or physical trial was run.
  Generation/source/emission/strategy/action authority remains unpromoted.

## WS-H joins the native Boss recurrence to all six route ECL files

- Added
  `scripts/analysis/th08_route2_boss_phase_configuration_atlas.py` and retained
  `artifacts/runtime_reports/th08_route2_boss_phase_configuration_atlas_20260731.json`,
  SHA-256
  `95e872a31085b64e9660d9e488ffba16983a3cdb3d26e76a051a174e983bc837`.
  Its internal pre-digest is
  `6854c77f54d67869bcd74bd794f4eb86d9baf25c7303e1a1bb389e4c3dbf1ae7`;
  a second generation is byte-identical.
- The pinned Route-2/Lunatic Final-B corpus contains 189 eligible
  `0x83..0x86` sites, 180 CFG-reachable sites, 103 literal transition edges,
  88 phase subroutines, and 22 Boss roots. All phase-control operands are
  literal; Stage 2 retains three and Stage 3 six eligible but unreachable
  sites.
- Four subroutines have partially shared HP/timeout literal successor sets:
  Stage 2 sub 29, Stage 3 subs 35 and 38, and Stage 5 sub 56. This is exact
  multi-boundary content, not runtime phase sequence or eventual route
  divergence.
- Fresh connected-IDB revalidation corrected an omitted gate. Opcode `0x85`
  always writes its HP threshold and opcode `0x86` always writes timeout and
  resets the timer, but each writes its successor only when engine bit
  `0x4000` is clear or mode bits `7..8` are zero. The suppressed branch
  retains the previous successor. Opcode `0x84` timer semantics were promoted
  from inferred to observed.
- CE-0222 retains the `engine_flags=0x4080` falsifier. The TH08 Boss adapter
  now has an executable successor-write predicate, the opcode catalog and
  generated catalog reports state the gate, and the atlas labels every
  literal transition edge with the required mode branch.
- IDA comments were added at `0x0041C941`, `0x0041CB26`, `0x0041CB70`, and
  `0x0041CCFC`.
- Detailed boundary:
  `notes/research/stage5_combat/ROUTE2_BOSS_PHASE_CONFIGURATION_ATLAS_CONTRACT_20260731.md`.
- Twenty-seven focused tests and Ruff pass. Complete discovery passes 1,470
  tests in 14.450 seconds on Linux and 30.425 seconds through the Windows UNC
  loader, with the three existing skips.
- No TH08 process, ECL runtime probe, replay, controller, or physical trial
  was run. Runtime instruction execution, successor registry, phase sequence,
  damage benefit, strategy, and action authority remain unpromoted.

## WS-H captures the complete Boss successor registry

- CE-0223 rejects the old phase identity. Two stable observations can have
  the same Boss pointer, HP, timer, thresholds, and timeout while retaining
  different successor registers, so they are not control-equivalent at the
  next native phase boundary.
- Revalidated `enemy_apply_health_phase_transition`: the selected health
  successor is read from `enemy+0x3368[slot]` at `0x0042B540` before its ECL
  subroutine starts. The timeout helper reads its successor from
  `enemy+0x337C` at `0x0042BA53`.
- The stable Boss capture now extends the existing manager-frame bracket
  through `+0x337C`, retaining all four health successors and the timeout
  successor. The five registers enter phase identity.
- Every bounded health/timeout transition step now reports the exact captured
  successor it selects. The projection does not execute ECL or model the
  timeout helper's later successor-register rewrite, so successor side
  effects and complete post-transition state remain unknown-direction
  omissions outside hard safety authority.
- A deterministic regression changes only health successor slot 0 from 51 to
  99 and requires a different phase key. Decode, serialization, health,
  timeout, and multi-threshold tests retain the exact selected targets.
- Nine focused Boss tests and Ruff pass. Complete Linux discovery passes
  1,471 tests in 14.568 seconds. Complete Windows UNC discovery passes 1,471
  tests in 30.340 seconds with the three existing skips.
- No TH08 process, runtime probe, replay, controller, or physical trial was
  run. This checkpoint grants shipped-instruction, stable-capture-schema, and
  deterministic-prefix authority only; runtime phase sequence, damage
  benefit, strategy, and action authority remain unpromoted.

## WS-H joins native defeat drops to route-wide Power opportunities

- Fresh shipped-instruction/dataflow revalidation corrects the item/drop
  authority boundary. `enemy_manager_initialize_template_and_pool` zeroes the
  inherited template, so ordinary allocations begin with primary item type 0
  (small Power) and zero extra counts. `enemy_spawn_configured_defeat_items`
  consumes primary, extra Power, then extra point requests for modes 0..2;
  mode 3 bypasses the helper.
- ECL `0x8D/0x8E/0x8F/0x90/0xA8` are promoted from partial/inferred labels to
  exact observed semantics. `0x90` writes point count at `+0x3308` before
  Power count at `+0x330C`; `0x8E` below full Power requests one large then
  remaining small Power items, while configured defeat Power counts request
  small Power. All randomized requests consume independent native x/y RNG in
  a `+/-64` square.
- The executable recurrence retains modes, native request/RNG order, the
  uint16 every-third global primary schedule, full-Power conversion,
  primary-only Bomb-related homing, count clearing, and mode-3 bypass. Nine
  deterministic tests distinguish requests from successful item-pool
  allocation.
- The pinned six-stage Route-2 atlas retains 991 timeline spawns, 218 program
  roots, 151 allocation-origin programs, and 60 route-reachable literal item
  sites: `0x8D` 3, `0x8E` 8, `0x90` 41, and `0xA8` 8. There is no reachable
  `0x8F`; 56 ordinary-compatible allocation programs retain the default
  small-Power candidate. Every reachable site maps to a program root and all
  CFGs have zero unresolved dynamic subroutine edges.
- Retained report:
  `artifacts/runtime_reports/th08_route2_item_drop_opportunity_atlas_20260731.json`,
  SHA-256
  `0692985c579a3040def4e635115a3399c0e4323a338d5f03427c4930821dc0b0`.
  Exact regeneration is test-enforced.
- IDA renames `0x00429E00` to
  `enemy_manager_initialize_template_and_pool` and `0x0042BEA0` to
  `enemy_spawn_configured_defeat_items`. Eight comments retain template,
  opcode, request-order, schedule, clearing, and no-Bomb motion semantics.
- Complete Linux discovery passes 1,484 tests in 14.807 seconds. Complete
  Windows UNC discovery passes 1,484 tests in 30.415 seconds with the three
  existing skips.
- No TH08 process, runtime probe, replay, controller, or physical trial was
  run. This checkpoint grants native request-recurrence and shipped-content
  static-opportunity authority only; runtime execution/end reason, item
  allocation/pickup, safe collection, later combat benefit, strategy, and
  action authority remain unpromoted.

## WS-H intersects combat and Power candidates across the whole route

- A new fail-closed board joins the accepted source/emission and item/drop
  atlases only by immutable ECL SHA-256 plus root subroutine. It verifies
  schema, input hashes, content manifest, route profile, stage order, ECL
  identity, root uniqueness, and complete coverage of all 70 timeline source
  programs.
- All 39 ordinary static emitter candidates also retain the native default
  small-Power request on an eligible HP defeat, covering 909 timeline spawns.
  Sixteen additionally configure positive extra Power and cover 97 spawns.
- Named overlapping mechanism cohorts retain 32 direct-emission programs
  covering 878 spawns, 14 child-emitter programs covering 74, and 13 periodic-
  control programs covering 479. Nine ordinary resource programs covering 60
  spawns remain a no-emitter comparison cohort.
- The board intentionally performs no scalar utility ranking. A local VM
  timer is not a kill deadline, a static item signal is not an allocation or
  pickup, and a static emission site is not a prevented birth. All 148
  item-only child/cross-enemy/end/phase roots remain outside the timeline-root
  join instead of being merged into a physical generation.
- Retained report:
  `artifacts/runtime_reports/th08_route2_combat_resource_candidate_board_20260731.json`,
  SHA-256
  `34e70a50e6c38c8241df0425be83367e6bf9e369106d600956d5a052dfa8cfea`.
  Exact regeneration is test-enforced.
- Three focused tests and Ruff pass. Complete Linux discovery passes 1,487
  tests in 15.671 seconds. Complete Windows UNC discovery passes 1,487 tests
  in 30.206 seconds with the three existing skips.
- No TH08 process, runtime probe, replay, controller, or physical trial was
  run. This checkpoint grants immutable static-cohort selection authority
  only; runtime execution, kill/end reason, prevented birth, item allocation/
  pickup, causal Power/survival benefit, phase-option edges, strategy, and
  action authority remain unpromoted.

## WS-H carries exact ECL program identity into enemy generations

- Fresh connected-IDB instruction/dataflow revalidation confirms that signed
  word `[ebp+8]` at allocation hooks `0x0042A55F` and `0x0042A6FF` is passed
  unchanged to `ecl_start_subroutine`. Native `g_stage_route_index` at
  `0x0164D2CC` selects the loaded normal-stage ECL table. IDA comments at both
  hooks now retain the exact `(stage_route_index, root_subroutine)`
  allocation-identity boundary.
- Lifecycle probe schema `th08-enemy-lifecycle-probe-v2` expands each event
  from 40 to 48 bytes. Allocation events retain the signed-word root;
  non-allocation events require null/-1 root identity; every event retains and
  validates native stage-route index `0..8`. The 256-entry ring still fits its
  fixed `0x4000` remote allocation.
- Trace-lowering schema `th08-enemy-lifecycle-trace-audit-v2` carries exact
  stage/root identity through each observed generation and rejects a lifetime
  that crosses stage identity. Summary counts retain both root and concrete
  stage/root program occurrence.
- The optional `--candidate-board` join verifies the immutable board SHA-256
  `34e70a50e6c38c8241df0425be83367e6bf9e369106d600956d5a052dfa8cfea`
  and its pinned source-atlas identity. It matches only exact
  `(stage_route_index, root_subroutine)` timeline programs; partial starts and
  child/phase/non-timeline roots remain explicit rather than receiving an
  invented candidate label.
- Fifteen focused probe tests and ten focused lowerer tests pass. Complete
  Linux discovery passes 1,491 tests in 14.072 seconds. The first Windows
  discovery failed only the pre-existing auxiliary-ECL timing gate after
  30.903 seconds; its isolated two-test repeat passed in 0.217 seconds, and
  complete Windows UNC discovery then passed 1,491 tests in 31.252 seconds
  with the three existing skips.
- No TH08 process, probe installation, runtime batch, replay, controller, or
  physical trial was run. This grants shipped-dataflow and synthetic
  generation-identity/join authority only. Runtime execution, end reason,
  prevented birth, item allocation/pickup, Power or survival benefit,
  strategy, and action authority remain unpromoted.

## WS-H joins enemy generations to item allocation and pickup transactions

- Fresh connected-IDB instruction/dataflow revalidation identifies four
  exact item lifecycle boundaries: successful allocation at `0x0044044D`,
  non-pickup cull at `0x00440991`, pickup begin before reward dispatch at
  `0x00440A39`, and pickup commit after reward dispatch at `0x00440C1E`.
  Exact overwritten bytes are pinned. IDA comments at all four sites retain
  the observed boundary.
- All 58 shipped direct `item_pool_spawn` caller returns are pinned. Five
  returns inside `enemy_spawn_configured_defeat_items` expose the exact source
  enemy through saved caller EBP and `[caller_ebp-0x14]`; other callers must
  retain no owner. The successful hook is post-RNG and post-effective-type
  conversion, so pre-RNG and original requested type are deliberately not
  reconstructed.
- Probe schema `th08-enemy-item-lifecycle-probe-v3` extends the same
  default-off total-order ring from eight to twelve hooks and from 48- to
  128-byte typed events. It retains effective item state, player/input/
  resource state, allocation cursor/list predecessor, post-allocation RNG,
  exact defeat source, non-pickup cull, and one paired same-update pickup
  pre/post resource/RNG transaction. The 256-entry ring uses at most two
  contiguous reads across wrap.
- Lowering schema `th08-enemy-item-lifecycle-trace-audit-v3` creates exact
  item generations, closes them by cull or pickup before slot reuse, rejects
  stage/type/RNG/transaction disagreement, and retains Power/lives/Bombs
  deltas plus thresholds `8/24/48/80/128`. A defeat allocation joins only to
  an exact enemy generation whose serial interval contains it.
- The SHA-pinned board join now represents
  `candidate root -> enemy generation -> defeat-item generation ->
  pickup/resource delta/Power threshold`. It performs no ranking, does not
  interpret absent events as a failed allocation, and grants no collection
  or later-benefit authority.
- Eighteen focused probe tests, twelve focused lowerer tests, and the existing
  transport suites pass. Complete Linux discovery passes 1,496 tests in
  14.227 seconds. Complete Windows UNC discovery passes 1,496 tests in 30.491
  seconds with the three existing skips.
- No TH08 process, probe installation, runtime batch, replay, controller, or
  physical trial was run. This grants revalidated shipped-boundary and
  synthetic implementation/lowering authority only. Runtime item identity,
  failed allocation attempts, survival-feasible collection, causal Power or
  later combat/survival benefit, strategy, and action authority remain
  unpromoted.

## CE-0224 corrects the v3 lifecycle ring stride encoding

- Review of the next native transaction hook found that the v3 selector used
  `imul ecx, ecx, imm8` with byte `0x80` for its 128-byte event stride.
  IA-32 sign-extends that immediate, so every nonzero slot would have walked
  backward from the ring base and corrupted the remote image.
- The selector now uses `imul ecx, ecx, imm32` with exact positive stride
  128. A deterministic test builds a production stub, decodes the signed
  immediate, exercises the `>= 0x80` boundary, and rejects the old
  `6b c9 80` sequence.
- The omission existed because earlier structural tests checked replay,
  return, fit, activation, and cleanup but did not decode selector arithmetic.
  CE-0224 retains the minimal falsifier and correction.
- Focused probe discovery passes 19 tests. Complete Linux discovery passes
  1,497 tests in 14.888 seconds. Complete Windows UNC discovery passes 1,497
  tests in 30.680 seconds with the three existing skips. Ruff and diff checks
  pass.
- No TH08 process or v3 probe installation occurred before or after the
  correction. Runtime event, strategy, and action authority remain
  unpromoted.

## WS-H joins exact resolved damage into the combat/resource transaction chain

- Fresh connected-IDB instruction/dataflow revalidation identifies the
  minimum paired damage boundary inside `enemy_manager_update`: begin at
  `0x0042D06D` (`8b4dc8 c7815433000000000000`) immediately before
  per-frame damage reset, and commit at `0x0042D343`
  (`2b45e8 8b4dc8 8981fc2d0000`) after native shot/flags/spell/Bomb/Boss
  scaling has produced positive resolved damage at `[ebp-0x18]`.
- Revalidated `player_compute_damage_to_enemy` at `0x00451670` iterates 128
  player-shot slots at player `+0xBE838`, stride `0x484`, with eligibility
  `state != 0 && (state == 1 || type == 3)`. The general shot-emission timer
  is `+0xE2AC4`; the damage-loop timer is the distinct `+0xE2AF4`.
- Pinned `th08-decomp`
  `84738749bdcf6cffabe8d0d76e17f19253a20d50` supplies address/extent
  agreement only: central enemy update remains an empty stub and
  `0x00451670` has only an unnamed mapping. Pinned `thtk/eclmap` have no
  comparable central runtime damage implementation. Shipped instructions
  remain authority. IDA comments at both hook sites retain the exact
  begin/commit meaning.
- Probe schema `th08-enemy-item-damage-lifecycle-probe-v4` adds two exact-byte
  guarded hooks to the existing total-order ring. One damage event retains
  same-pointer/same-frame HP before, resolved damage, HP after, target
  position/hitbox/main-VM PC/timer, Focus/player/Bomb/route/input/Power,
  shot-emission and damage timers, occupied/native-eligible shot counts, and
  gameplay RNG before/after.
- CE-0225 rejects assuming per-site activation quiescence covers the interval
  between paired hooks. Pickup and damage commits now require begin marker,
  subject pointer, and manager frame before selecting a slot. Mismatch takes
  a replay-only unlink/HP-write path; both paths clear the marker, with
  success clearing before publication.
- Lowering schema
  `th08-enemy-item-damage-lifecycle-trace-audit-v4` attaches exact damage
  transactions and totals to the open enemy generation, cross-checks
  same-frame defeat retirement against exact damage, and extends candidate
  join v2 to
  `candidate -> enemy generation -> damage -> defeat item -> pickup/resource`.
  The join reports damage count/total/manager frames without ranking or causal
  benefit claims.
- Twenty-one focused probe tests and thirteen focused lowerer tests pass.
  Ruff and diff checks pass. Complete Linux discovery passes 1,500 tests in
  13.953 seconds. Complete Windows UNC discovery passes 1,500 tests in
  31.615 seconds with the three existing skips.
- No TH08 process, probe installation, runtime batch, replay, controller, or
  physical trial was run. This grants revalidated shipped-boundary plus
  synthetic implementation/lowering authority only. Runtime damage identity,
  target-motion/action causation, prevented emissions, survival-feasible
  collection, later benefit, strategy, and action authority remain
  unpromoted.

## WS-H projects complete native combat roots without ranking them

- Fresh connected-IDB revalidation covers
  `player_shot_initialize` (`0x0044FB70`), `player_update_shots`
  (`0x00451150`), `player_compute_damage_to_enemy` (`0x00451670`), and
  `enemy_manager_update` (`0x0042C660`). The full 128-slot, stride-`0x484`
  shot pool at player `+0xBE838` already lies inside the retained full native
  player component. Its emission timer `+0xE2AC4` and damage-loop timer
  `+0xE2AF4` are distinct.
- The active shot decoder retains position, hitbox, velocity, speed, angle,
  timer, damage/state/type, Focus-at-birth, ANM index, update/hit callbacks,
  SHT record pointer, per-slot digest, and the full pool digest. Inactive
  stale bytes remain in root identity deliberately; this can reject an
  otherwise usable repeat but does not merge unproved initializer/callback
  futures.
- Active enemy targets are decoded from the already-retained pool component:
  position, primary/alternate hitboxes, HP/max HP, flags/flags2, frame damage,
  main-VM identity/timer, and causal-tail digest. Revalidation corrects the
  alternate-pass condition to ordered width greater than positive zero at
  `0x0042D0EE`.
- `th08-native-combat-root-projection-v1` evaluates the existing exact damage
  gate and native primary/alternate AABB passes. It computes a numeric
  subtotal only for supported eligible ordinary shots and exposes type-4/5
  and nonzero-hit-callback overlaps as unresolved. Attack-region and later
  spell/Boss/route/HP scaling remain outside the subtotal.
- Rolling native snapshot schema v5 and causal-search schema v3 carry this
  projection through roots, ticks, promoted subroots, deterministic repeats,
  natural references, and compact portfolios without changing collision
  projection v7 or the live sensing path.
- The strict `th08-native-combat-branch-comparison-v1` lowerer accepts only
  completed deterministic source transactions and rejects every native
  player-phase-2 branch. Published frame damage and supported overlaps are
  retained as observed/proxy state; cross-slot positive-HP change is
  explicitly not generation-safe. No candidate score or live authority is
  produced.
- IDA comments at `0x0042D0EE`, `0x004516CF`, and `0x004511C8` retain the
  revalidated alternate-width, pool-layout, and update-callback boundaries.
  Pinned `th08-decomp`
  `84738749bdcf6cffabe8d0d76e17f19253a20d50` provides address/extent
  agreement only; its relevant implementations are stubs or unnamed.
- Four focused projection tests, four report tests, twelve rolling tests,
  and four causal-search tests pass. Ruff and diff checks pass. Complete
  Linux/Windows discovery passes 1,508 tests in 14.185/30.682 seconds, with
  the three existing Windows skips.
- No TH08, replay, controller, supervisor, native runner, or physical trial
  was launched. This checkpoint grants offline exact-root and synthetic
  supported-overlap authority only. A later explicitly authorized v5/v3
  corpus must compare focused/unfocused/dynamic-refocus schedules inside the
  hard survival set and join exact v4 lifecycle generations/damage before
  claiming kill or prevented emission.

## WS-H closes unreachable normal-shot callback branches

- Revalidated `player_emit_shot_level` (`0x00450F60`) selects primary versus
  secondary SHT from Focus-logic byte `+0x03`, then advances through signed
  Power thresholds. Its distinct Route-2 special override selects secondary
  level 6/7 only for an active odd Bomb callback at Bomb-local timer 60+.
- After a record emits, native code stores its record pointer at slot `+0x480`
  and relocated callback-1/2/3 pointers at `+0x474/+0x478/+0x47C`.
  Callback-1 controls the pre-motion update edge; callback-3 is invoked after
  damage overlap. Callback-0 index 7 remains the already revalidated focused
  random-spread birth callback. IDA comments at `0x00451015` and
  `0x004510EE` retain the selector and callback-field boundaries.
- The SHA-pinned `ply02a.sht` and `ply02as.sht` share normal bounds
  `8/24/48/80/128/999`. Their 26 unfocused plus 27 focused normal records are
  all type 0 with callback-1 and callback-3 zero. Focused normal callback-0 is
  only 0 or 7. The 34 secondary level-6/7 records are type-6 Bomb-only
  content and do not enter the normal selector.
- Deterministic report
  `artifacts/runtime_reports/th08_route2_normal_shot_content_audit_20260731.json`
  has SHA-256
  `4361ec2814a8885dd6c4dd17bd42039f5a9bb38bccbeebcb8c43b6816df6d4e1`.
- Native combat projection summaries now count active slots compatible with
  normal Route-2 damage paths (`type == 0`, zero update/hit pointers). The
  branch lowerer includes root and future ticks; any incompatible active slot
  preserves an explicit non-normal-content status.
- Three content-audit, four projection, and five combat-report focused tests
  pass with Ruff and diff checks. Complete Linux/Windows discovery passes
  1,512 tests in 13.868/31.165 seconds, with the three existing Windows skips.
  No TH08, replay, controller, native runner, or physical trial was launched.
- This closes type-4/5 and nonzero update/hit callback work only for exact
  Route-2 zero-Bomb/no-hit histories whose runtime slots stay compatible. It
  grants no SHT-record provenance by field match alone, generation-safe
  damage, combat benefit, target selection, ranking, physical prediction, or
  live authority.

## WS-H binds active shots to exact loaded SHT records

- Fresh connected-IDB review of `player_sht_load` (`0x0044DD70`) confirms
  in-place relocation: level offsets at header `+0x38` become heap pointers,
  and record callback indices at `+0x28/+0x2C/+0x30/+0x34` become executable
  pointers. `player_emit_shot_level` reads the two bases from player
  `+0xE2A74/+0xE2A78` and stores exact source ownership at slot `+0x480`.
- The only nonzero relocation in both complete pinned Route-2 files is
  secondary callback-0 index 7 to `0x004501B0`. The new decoder validates
  level counts, Power bounds, pointer order/range, record/sentinel topology,
  and every callback before reversing relocation. IDA comments at
  `0x0044DE07` and `0x00450F83` retain the relocation and player base-pointer
  ownership boundaries.
- The normalized 1,584-byte primary and 3,568-byte secondary images must hash
  to their pinned raw SHT identities. The exact loaded map contains 87 source
  pointers: 53 normal and 34 Bomb-only special.
- Combat projection v2 retains both normalized images and per-active-slot
  profile/level/offset/normal ownership. Rolling snapshot v6 and causal
  search v4 carry that identity through deterministic roots and futures.
  The offline branch report checks root plus every tick and explicitly keeps
  special or unknown sources outside normal-content closure.
- Three dedicated relocation/provenance tests cover the exact 87-record pair,
  scalar-byte digest corruption, and a foreign callback pointer. Projection
  and report tests cover exact-normal and unknown source records. Ruff and
  diff checks pass. Complete Linux/Windows discovery passes 1,517 tests in
  14.241/31.181 seconds, with the three existing Windows skips.
- No TH08, replay, controller, native runner, or physical trial was launched.
  This grants offline exact loaded-content/source-ownership implementation
  authority only. A v6/v4 runtime corpus, v4 generation-safe damage, prevented
  births, survival benefit, ranking, and live authority remain absent.

## WS-H corrects ordinary damage versus hit feedback

- Revalidated the exact `player_compute_damage_to_enemy` instruction order at
  `0x004517F6..0x00451CD5` and both ordinary callers in
  `enemy_manager_update`.
- **Observed:** ordinary hits accumulate in `[ebp-0x34]`. The branch at
  `0x0045199F` limits only a temporary increment added to caller-owned enemy
  `+0x2E10`; the uncapped accumulator continues through the damage-region pass
  and is returned to later enemy scaling/HP arithmetic.
- **Corrected:** the scalar player-shot model now returns uncapped damage and
  exposes the capped feedback increment separately. Combat projection v3 and
  supported-pass v2 make the same distinction. CE-0226 retains the minimal
  two-40-damage-shot falsifier: native return 80, feedback increment 50, old
  model return 50.
- IDA comments at `0x0042A1FA` and `0x004519A3` retain the corrected
  accumulator roles. All affected combat, resource, architecture, handoff,
  and strategy authority documents are reconciled.
- No TH08, replay, probe, controller, native runner, or physical trial was
  launched. This grants shipped-instruction/static-dataflow and deterministic
  offline regression authority only. The 192-slot damage-region pass and
  later enemy scaling/HP arithmetic remain the next general WS-H boundary.
- Focused player-shot and combat-projection tests pass. Complete Linux/Windows
  UNC discovery passes 1,518 tests in 15.670/31.282 seconds, with the three
  existing Windows skips.

## WS-H projects the complete player damage-region pool

- Combat projection v4 now reads all 192 player damage-region records at
  player `+0xB8834`, stride `0x40`, and retains both the complete pool digest
  and every active slot's geometry/accounting identity.
- The supported scalar pass follows the shipped active flag, signed
  remainder-zero tick gate, inclusive circle/axis-aligned/rotated rectangle
  overlap, damage accumulation, positive cap clipping, and damage-disable
  order. Primary-pass mutation is carried into the alternate pass; Bomb-region
  overlap is retained as a distinct caller signal.
- Synthetic coverage includes a cap-clipped primary contribution followed by
  a zero-damage but still overlapping alternate check. Existing independent
  scalar tests retain circle boundary, rectangle rotation, due/update order,
  and Route-2 knife profiles.
- Immutable combat identity changes bump rolling snapshots to v7 and causal
  search to v5. No v6/v4 artifact is reinterpreted, and no v7/v5 runtime
  corpus exists.
- Complete Linux/Windows UNC discovery passes 1,519 tests in
  14.216/31.472 seconds, with the three existing Windows skips. No TH08,
  replay, probe, controller, native runner, or physical trial was launched.
  Later character/alternate/spell/Boss/HP-write scaling remains outside the
  numeric projection and is the next general WS-H semantic boundary.
