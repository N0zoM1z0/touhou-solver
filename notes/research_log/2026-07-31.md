# TH08 Research Summary — 2026-07-31

## Combat and resource reconstruction

**Observed:** shipped Route-2 SHT selection, normal-shot records, focused
option emission, player-shot pool identity, supported enemy damage, enemy
lifecycle, defeat cleanup, Boss transitions, item allocation/pickup, and
resource writes were revalidated and implemented in a fail-closed offline
path.

Important corrections:

- Power additions cap immediately at 128.
- Forced HP zero and cleanup are not player-shot kills.
- Boss successor registry is part of phase identity.
- Lifecycle ring stride must use the correct immediate encoding.
- Paired hook commits require matching marker/pointer/manager frame.
- Returned damage and the 50-point hit-feedback clamp are distinct.
- Unconsumed timeline engine events invalidate integrated simulation.
- Effective item type 7 performs one native cursor probe.

Static content/coverage atlases and a route-wide candidate board were retained
under `artifacts/runtime_reports/`. They identify roots and opportunities;
they do not prove execution, kills, pickups, Power benefit, or survival.

The action hypotheses are:

- kill ordinary enemies before pattern saturation, but only inside the
  survival-feasible action set;
- use Focus dynamically rather than permanently holding Shift;
- collect early Power only through a safe path with a causal later benefit.

Static SHT coverage rejects a simple “unfocused is wider” policy: focused
outer coverage is wider in 14/18 audited rows. These hypotheses therefore need
same-root native branching.

Current authority and next experiment are consolidated in
`../CURRENT_COMBAT_RESOURCE_MODEL.md`.

## Physical Stage 3/4A/5 ring

Three original-game Lunatic Sakuya/Remilia Route-2 practice trials ran at
checkpoint `d2b810d` with all WS-H ranking disabled:

| Stage | Run | Hits | First hit | Bombs | Replay |
| --- | --- | ---: | ---: | ---: | --- |
| 3 | `20260731_091104` | 5 | 2150 | 0 | accepted |
| 4A | `20260731_091925` | 13 | 2555 | 0 | accepted |
| 5 | `20260731_093027` | 12 | 2124 | 0 | accepted |

Different-root comparisons were 15→5, 10→13, and 19→12. **Observed only:**
the mixed result proves neither improvement nor regression. CE-0229 retains
that boundary. Raw replay-capable traces remain local/ignored; compact
reports, dossiers, and content-addressed no-Bomb replays are tracked.

## Workspace consolidation

An annotated safety tag `pre-workspace-prune-20260731` and three local
compressed recovery bundles were created before cleanup. Historical docs,
raw artifacts, retired G5/priority17/Final-B/supplemental lanes, dormant live
flags, and redundant tests were removed from the active tree.

The promoted baseline/pre-loss live path, native snapshot wind tunnel, exact
pipeline workspace, Final-B scale authority, current physical ring, and
combat/resource reconstruction remain. The cleanup changes no intended live
action semantics and therefore requires no physical trial. Linux discovery
passed 1,136 tests in 9.611 seconds; complete Windows UNC discovery over the
same test set exited zero. Both native builds pass the 41-symbol ABI gate.
