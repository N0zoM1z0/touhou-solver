interface:
  display_name: "TH08 Native-to-Solver Audit"
  short_description: "Audit IDA semantics against Python and native solver code"
  default_prompt: "Use $th08-audit-native-solver-semantics to audit TH08 IDA-to-solver semantics; keep investigation-only work read-only and apply evidence-backed corrections when the request authorizes fixes."
