# TH08 Research Log — 2026-07-29

## Pre-commit review closed an auxiliary-event version-boundary gap

- **Observed:** the runtime identity service already accepted only route 2,
  but the downstream auxiliary-event service independently compared static
  bytes, difficulty, stage, and gameplay epoch without comparing the retained
  route ID. A forged accepted-version value from another route could therefore
  cross that module boundary in isolation.
- Added an immutable `expected_route_id` to the event-program configuration,
  reject negative configuration, and require exact route equality before
  binding or lowering. The controller fixes it to route 2.
- Added a regression case showing route 1 fails closed as
  `runtime_identity_mismatch`. This correction does not grant physical or
  action authority; the focused spell-107 delivery trial remains pending.
- Focused delivery/replay tests and Ruff pass. Complete Linux discovery passes
  1,011 tests in 11.843 seconds; complete Windows UNC discovery passes 1,011
  tests in 19.660 seconds with three existing platform skips.

## Schema-v4 auxiliary-event physical delivery gate failed

- **Observed:** focused Lunatic Stage-5 run
  `lunatic_route2_stage5_unattended_20260729_014125` completed frames
  `2..41804`, 11,805 decisions, 11 hits, hard no-Bomb, accepted route
  completion, no-save selection, and exact cleanup.
- All 142 transactions were coherent and immutable-version matched. The
  independent raw-byte oracle accepted 3,830/3,830 requests over targets
  69/72/73 with zero unknown; total transaction and cadence gates passed.
- The fixed delivery gate still failed. Six initial spell-edge transactions
  had no native context and retained `no_usable_contexts`. Event derivation
  p95/p99/max was `0.879/1.058/4.871 ms`; replay compact maximum was
  `11.335 ms`; and prior synchronous emit p95/p99 was
  `2.871/3.495 ms`. The limits remain unchanged.
- The failed report regenerates byte-identically at SHA-256
  `780b8f62...b3e`; raw trace SHA-256 is `6d779046...c872`. CE-0168 records
  the falsified live-delivery assumptions.
- The next step is a separately contracted exact-version bounded cache,
  visible pre-bind, hash-addressed compact replay bundle, and explicit
  independently verified empty-prefix status. Schema v4 remains failed and
  has no delivery/action authority.

## Schema-v5 corrected delivery passes isolated preflight

- **Observed:** five replays of all 142 retained schema-v4 physical
  transactions pass the fixed corrected-delivery gates on both Linux and
  Windows. The first repeat has exactly 46 misses, 1,797 persistent hits,
  1,987 request-local hits, and zero eviction.
- Schema v5 uses one visible exact-version preparation, a bounded 512-entry
  exact LRU, an independently verified initial `empty_complete` prefix, and a
  hash-addressed zlib-level-1 replay bundle. The independent auditor decodes
  and hashes every raw blob, replays every request, and separately simulates
  the LRU transitions.
- Linux event-derive/replay-compact/no-write-JSON p95 is
  `0.371/0.329/0.585 ms`; Windows is `0.408/0.433/0.626 ms`. Both reports
  pass every fixed isolated gate. Report SHA-256 values are
  `bb9fb6e3...e4cb` and `805ac6df...fea9`.
- Ruff and all focused tests pass. Complete Linux discovery passes 1,020
  tests in 11.561 seconds; Windows UNC discovery passes 1,020 in 20.672
  seconds with three existing skips. The failed V1 report remains
  byte-identical.
- This is isolated replay, not physical contention or action authority. The
  unchanged focused Lunatic Stage-5 spell-107 schema-v5 gate remains next.

## Schema-v5 physical run exposed epoch/version conflation and 20 hits

- **Observed:** run `20260729_095849` from checkpoint `5ad2bb1` completed
  hard no-Bomb Stage 5 with 20 hits: 11 nonspell and 2/2/3/2 on spells
  103/107/111/115. This is physically unacceptable for handoff.
- The exact runtime image and V2 program were prepared at controller epoch 0,
  but all 142 selected batches occurred at epoch 3. Every event derivation
  failed closed as `runtime_identity_mismatch`; no request reached lowering,
  cache, or independent replay. CE-0169 records the model-version failure.
- The earlier accepted runtime-image contract defines program equivalence
  without controller gameplay epoch. Epoch resets invalidate observation and
  control continuity; they do not assert an ECL instruction-image write.
- The 20 hits cannot be attributed to cache contention: there was no
  lowering, the first ten hits preceded the batch window, cadence p50/p95
  remained `2/4`, prior complete Stage-5 runs span 8–23 hits, and the
  schema-v3 run `20260728_193820` also had 20. However, the frame-2 cold bind
  consumed `5.317 ms` total and cannot be declared physically side-effect
  free from one p95 statistic.
- Schema v5/event v2 is rejected. A new pre-implementation V3 contract
  separates program identity from observation epoch, moves exact target
  validation/materialization before gameplay, tightens online preparation to
  `1.000 ms`, and requires every run to be retained. Before handoff, two
  consecutive corrected Stage-5 runs must each have at most ten hits and
  other Lunatic stages must show no obvious regression.
- **Hypothesized combat follow-up:** inside an unchanged viable set and away
  from precision micro-dodging, release Shift to test whether Sakuya's wider
  unfocused coverage kills distributed nonspell enemies earlier. Native
  team/shot mode, damage, kill time, exposure, and survival margin must be
  observed before any live rule; reduced margin returns to focused control.

## Schema-v6 epoch-safe implementation passes isolated cross-epoch replay

- **Observed:** V3 byte-validates all 1,664 Stage-5 instructions before
  gameplay, proves the nine retained target instructions have no executable
  same-difficulty successor outside their owner closure, and binds only those
  nine descriptors after runtime-base acceptance.
- Five service instances per platform replay 142 retained physical
  transactions across accepted epoch 0 and observation epochs 1/2/3. Linux
  and Windows each pass all 710 samples with the exact first-repeat cache
  totals: 46 misses, 1,797 persistent hits, 1,987 request-local hits, and no
  eviction.
- Online preparation maximum falls to `0.044 ms` on Linux and `0.073 ms` on
  Windows. Event-derive p95 is `0.366/0.436 ms`; all fixed isolated timing
  gates pass. Static validation is pre-game and separately recorded.
- Ruff passes; complete Linux/Windows discovery passes 1,027 tests, with
  three existing Windows skips. The immutable failed V1 report regenerates
  byte-identically.
- This is isolated trace-only evidence. The next physical Stage-5 run is
  retained regardless of outcome, and two consecutive results at no more
  than ten hits remain required before handoff.

## Schema-v6 physical semantics pass but delivery and survival fail

- **Observed:** run `20260729_104222` completed hard no-Bomb Stage 5 with
  exact cleanup and 11 hits. Phase hits nonspell/103/107/111/115 were
  `4/0/1/4/2`; all followed global viability-kernel exhaustion.
- The epoch correction worked physically: all 127 selected batches crossed
  accepted program epoch 0 to observation epoch 2. All 3,384 requests
  independently replay complete with zero unknown; online preparation is
  `0.121 ms`.
- Physical delivery still fails event-derive, replay-compact, and previous
  emit p95 at `0.620/0.564/2.862 ms`. Batch JSON line p50/p95/max is
  `66080/92142/97166` bytes, and previous emit correlates `0.785` with the
  preceding line size.
- CE-0170 retains both the delivery and survival rejection. A V4 contract is
  fixed before implementation: preserve usable raw states, source indices,
  status proof, and exact independent replay while replacing proved-null
  record dictionaries and repeated derived JSON with narrow projections and
  canonical commitments. No timing or survival threshold is loosened.

## Schema-v7 compact delivery passes isolated Linux/Windows preflight

- **Corrected before implementation:** the independent byte oracle cannot
  rebuild every descriptive field of `result.record()`. V4 therefore commits
  exactly the oracle-compatible recurrence core, not the complete production
  dictionary. Full VM/ECL bytes remain retained; excluded descriptive
  arguments gain no authority.
- **Observed:** five repeats of all 142 retained physical transactions pass on
  Linux and Windows across accepted epoch 0 and observation epochs 1/2/3.
  Each platform classifies all 710 samples, with the unchanged first-repeat
  cache totals 46 misses, 1,797 persistent hits, 1,987 request-local hits,
  and zero eviction.
- The selected observation keeps its native full-count/status proof and raw
  replay bundle but projects only usable rows with exact source indices.
  Event output keeps narrow request rows and canonical result commitments.
- Linux/Windows maximum line size is `23495/23494` bytes. Event derive,
  replay compact, and no-write JSON p95 is `0.251/0.290/0.185 ms` on Linux
  and `0.277/0.387/0.191 ms` on Windows; every fixed isolated gate passes.
- Ruff passes. Complete Linux/Windows discovery passes 1,032 tests, with
  three Windows skips. The failed V1 report remains byte-identical at
  `780b8f62...b3e`.
- This is isolated trace-only evidence. A fresh retained Stage-5 physical gate
  is next; two consecutive corrected `<=10`-hit results and later cross-stage
  checks remain required before handoff.

## First schema-v7 physical attempt was foreground-contaminated

- **Observed:** attempt `20260729_112743` from checkpoint `5fae77a` armed with
  exact executable/patch/menu identity, then lost TH08 foreground at summary
  frame 2,736. The controller failed closed, released keys, and the supervisor
  completed exact process cleanup.
- The trace has 1,169 decisions and one early nonspell hit at frame 2,726,
  after global-kernel exhaustion. It ended before spell 107 and emitted zero
  auxiliary batches, so it supplies no V4 delivery evidence and is excluded
  from the consecutive survival count.
- CE-0171 retains the contamination and the invalid assumption that an armed
  unattended launch necessarily retains foreground. The unchanged checkpoint
  requires a fresh retained rerun.

## Schema-v7 physical semantics pass but compact delivery and survival fail

- **Observed:** uncontaminated Stage-5 run `20260729_113124` completed frames
  `1..42928`, 11,797 decisions, hard no-Bomb, accepted artifacts, route
  completion, and exact cleanup. It took 14 hits:
  nonspell/103/107/111/115 = `8/2/3/1/0`.
- All 184 batches and 5,124 requests independently replay exactly with zero
  unknown. Preparation, cache, event derive, replay compact, transaction
  total, cadence, no-Bomb, completion, and cleanup gates pass.
- Physical context cardinality exceeded the isolated sample. Batch-line
  p95/max was `24142/25296` bytes, failing the fixed 24,576-byte maximum.
  Previous synchronous emit p95 was `1.366 ms`, failing the fixed 1.000-ms
  limit. The strict failed report is byte-stable at
  `2d5ebcf8...d29`.
- CE-0172 rejects V4 delivery and survival authority while preserving its
  physical semantic evidence. The next fixed transport-only V5 contract uses
  versioned columnar usable-record and request rows to remove repeated JSON
  keys while retaining exact indices, status proof, hashes, raw replay bytes,
  epochs, program identity, and recurrence commitments. No threshold is
  loosened and no action changes in this correction.
- Eight hits were in nonspell and every hit followed global-kernel
  exhaustion. The separate survival line will test earlier nonspell
  kill/despawn and, only when survival margin permits, Sakuya unfocused shot
  coverage. This remains a hypothesis and is not part of V5 delivery.

## Schema-v8 columnar delivery passes isolated Linux/Windows preflight

- **Observed:** the fixed V5 transport replaces repeated usable-record and
  request keys with exact fourteen-/three-column schemas. Raw replay bytes,
  status proof, source order, hashes, epochs, program identity, cache records,
  and recurrence commitments remain unchanged.
- Linux and Windows each pass five repeats over 142 retained transactions:
  19,150/19,150 requests independently replay complete with zero unknown,
  target counts remain `950/965/1915` per first repeat, and independent cache
  parity retains 46 misses, 1,797 persistent hits, 1,987 request-local hits,
  and zero eviction.
- Maximum line size falls to `13743/13741` bytes. Derive/compact/JSON p95 is
  `0.190/0.236/0.100 ms` on Linux and `0.280/0.390/0.149 ms` on Windows.
  Every fixed isolated gate passes without relaxing a threshold.
- Pure column projection of the actual 34-record failed V4 physical trace
  yields a 14,038-byte maximum. This supports size margin but is not physical
  V5 timing.
- Ruff and complete 1,038-test Linux/Windows suites pass. V1 and physical V4
  failed reports retain exact SHA-256. This checkpoint remains trace-only;
  one fresh V5 Stage-5 delivery run is next, separate from combat changes.

## Schema-v8 physical semantics pass but separate publication and survival fail

- **Observed:** run `20260729_120859` from checkpoint `35d3163` completed
  hard no-Bomb Stage 5 with accepted artifacts, route completion, exact
  cleanup, and 14 hits. Phase hits nonspell/103/107/111/115 were
  `5/1/5/2/1`.
- All 154 columnar batches and 4,238 requests independently replay complete
  with zero unknown. Preparation, program identity, schema, cache, oracle,
  line size, event derive, transaction total, cadence, no-Bomb, completion,
  and cleanup pass.
- Physical line p95/max falls to `14899/15453` bytes, so V5 fixes V4's size
  failure. The still-separate synchronous emit p95 is `1.412 ms`, and replay
  compact p95/max is `0.507/8.452 ms`; those fixed gates fail.
- **Inferred:** rows with at least 16 records pay an approximately
  one-millisecond fixed write cost even after columnarization. The next
  correction must change publication composition, not keep removing proof
  fields: a separately contracted envelope should restore exact V5 bytes
  inside the same-iteration decision publication and eliminate the second
  OS write.
- CE-0173 retains the failure. No threshold is loosened. Survival remains
  separate: 13/14 hits follow global-kernel exhaustion, and spells 107/111
  contribute seven hits, so nonspell targeting alone is insufficient.
- The survival-filtered unfocused-Sakuya hypothesis remains proposed only:
  release Shift for wider coverage when native shot/damage evidence and hard
  margin allow, then return to focus before dense geometry. It was not
  exercised in this run.

## V6 coalesced-publication contract fixed before implementation

- The immutable inner evidence is one complete schema-v8 V5 object encoded as
  canonical sorted compact JSON with `previous_emit = null`. The outer
  envelope uses zlib level 6, strict base64, exact compressed/uncompressed
  lengths and SHA-256, and sequence/frame/epoch/snapshot/stage bindings.
- One envelope attaches only to the decision produced later in the same
  controller iteration. V6 forbids a standalone auxiliary batch row and
  reuses that decision's one existing synchronous flush. The following
  decision's existing `previous_trace` is the causal publication measurement;
  a missing measurement fails closed.
- Independent analysis must reconstruct canonical bytes, validate every
  binding/order field, and reuse the unchanged V5 byte/cache oracle.
  Corruption, truncation, zlib trailing data, noncanonical JSON,
  stale/cross-epoch attachment, duplicate/gapped sequence, and a standalone
  row are explicit falsifiers.
- Ten Linux diagnostic passes over all 154 physical V5 rows yield inner
  p95/max `13934/14448` bytes, base64 p95/max `7024/7196` bytes, and pack
  p95/p99/max `0.365/0.416/0.825 ms`. Fixed V6 bounds are wider and still
  falsifiable; this diagnostic is not Windows or physical contention
  evidence.
- V6 remains post-issue trace-only and action-neutral. Its first physical run
  must not include Focus/unfocused, targeting, Power, planner, or other
  survival changes.

## V6 coalesced delivery passes isolated Linux/Windows preflight

- The controller no longer performs a standalone auxiliary batch emit. It
  packs one exact canonical V5 object and attaches the envelope to the
  same-iteration decision before that decision's existing synchronous flush.
- Independent modules validate bounded base64/zlib, both hashes and lengths,
  canonical JSON, sequence/frame/epoch/snapshot/stage bindings, standalone
  absence, and next-decision causal publication timing before reusing the V5
  byte/cache oracle.
- Five repeats per platform independently replay 21,190 requests with zero
  unknown. Linux pack p95/p99/max is `0.468/0.557/0.803 ms`; Windows is
  `0.667/0.833/1.580 ms`. Both pass the fixed
  `0.750/1.250/3.000 ms` gate.
- Canonical/compressed/base64 maxima are `14448/5397/7196` bytes on both
  platforms. Exact first-repeat cache totals remain 46 misses, 1,970
  persistent hits, 2,222 request-local hits, and zero eviction.
- Complete Linux/Windows suites pass 1,052 tests, with three existing Windows
  skips. Failed V1, physical V4, and physical V5 reports regenerate
  byte-identically.
- This remains isolated trace-only evidence. A fresh action-neutral Stage-5
  run must measure actual coalesced decision publication and survival before
  any delivery claim.

## V6 physical semantics and one-write composition pass, but full gate fails

- **Observed:** run `20260729_125453` from physical checkpoint `3f02ff1`
  completed hard no-Bomb Stage 5 with accepted artifacts, route completion,
  exact cleanup, and 18 hits. Phase hits nonspell/103/107/111/115 were
  `6/2/4/2/4`.
- All 186 ordered envelopes independently decode, and 5,250/5,250 requests
  replay complete with zero unknown. There are zero standalone auxiliary
  writes. Pack p95/p99/max is `0.642/0.773/1.203 ms`; exact cache and all
  semantic, size, combined-transaction, all-decision emit, cadence, no-Bomb,
  completion, and cleanup gates pass.
- Replay compact p95/max remains above its fixed limits at
  `0.535/8.388 ms`.
- CE-0174 corrects another invalid assumption: the V6 report compares
  spell-107-only envelope-bearing decisions with an all-stage baseline.
  That population is not control-equivalent. The historical V6 report stays
  failed; a new contract/report must use a phase- and workload-matched
  comparator rather than relabeling the old gate.
- The canonical frame-731 nonspell hit precedes every auxiliary envelope.
  V6 cannot cause that first failure or all early nonspell failures, although
  spell-107 contention remains possible. Later hits are coupled through
  death, respawn, Power, position, and route timing.
- Comparing eight-hit checkpoints `faed791` and `3adad09` to current code
  finds no planner, native-pipeline, recurrence, ranking, or issued-mask
  algorithm change after `3adad09`; `9e3bce9` is an issue-stage extraction,
  while later relevant work is default-off/selected trace and structure.
  The 18-hit sample therefore does not identify one code change to revert.
  The next causal gate is a current-checkpoint Stage-5 run with every optional
  observer off, followed by a second consecutive `<=10` result before any
  promotion.

## One-shot launch and UNC handoff hardened

- Attempt `20260729_125411` failed before controller ready because shell
  escaping changed the immutable ECL path to
  `artifactsdecodedecldata5.ecl`. No accepted gameplay evidence resulted and
  cleanup completed.
- Checkpoint `d85cca1` moves immutable ECL path/read/file/SHA validation
  before any game/process side effect. Both repo BATs now resolve their
  supervisor from `%~dp0`, and two direct Windows tools now prepend their
  parent `scripts/` directory.
- Actual practice/full-route BAT `--help`, WindowsApps and IDA-Python safe
  imports, the real ECL UNC path/hash, Linux focused tests, and Windows UNC
  focused tests pass.
- Complete Linux discovery passes 1,055 tests in 14.423 seconds. An initial
  simultaneous Linux/Windows run caused the existing Windows auxiliary-event
  benchmark timing gate to fail; its isolated focused rerun passed. This
  rejects parallel platform performance gates. The final isolated Windows
  discovery passes 1,055 tests in 29.477 seconds with three existing skips.
- Canonical commands and remaining machine-specific external-BAT limitations
  are retained in
  `notes/review/LAUNCH_AND_UNC_WORKFLOW_AUDIT_20260729.md`.

## Repo-scoped reusable skills installed for handoff

- The first skill is a low-freedom physical
  run/preflight/monitor/cleanup/retention workflow.
- Separate skills cover isolated dual-platform gates, evidence checkpoint
  retention, physical regression audits, and IDA/runtime semantic
  revalidation.
- Official Codex guidance places repo/team skills in `.agents/skills`.
  Five handoff workflows were created there with `SKILL.md` and
  `agents/openai.yaml`; a concurrently present comprehensive read-only
  native-to-solver audit was preserved without modification. All six pass
  `quick_validate.py`.
- A fresh Codex `0.144.6` app-server
  `skills/list(cwds=[repo], forceReload=true)` reports all six as enabled
  repo-scope skills with parsed interface metadata. The physical skill was
  not forward-tested because this handoff stopped new physical experiments.
- Volatile strategy and planner promotion stay in repository authority
  documents rather than being copied into skills.

## Runtime-report emission and retention audit

- `artifacts/runtime_reports/` occupies 27 GiB. Ignored raw JSONL accounts
  for 26.126 GiB; tracked/staged compact history accounts for only
  81.137 MiB, and launch logs for 0.024 MiB.
- Raw decision geometry is required for replay and first-hit attribution.
  The V6 trace has 473.978 MiB of decision rows, including 236.239 MiB of
  nearby-bullet geometry; it is retention-bounded rather than safe to omit.
- Practice/full-route materializers previously wrote artifact dossier
  Markdown and copied it to `notes/runs/`. Checkpoint `e4e266f` writes the
  durable run note directly and adds focused tests proving no duplicate
  runtime Markdown. Isolated Linux/Windows complete discovery passes
  1,057 tests in 13.844/29.961 seconds, with three existing Windows skips.
- A schema/config/observer-compatible audit finds eight non-raw-path-
  referenced cleanup candidates totaling 2.711 GiB. See
  `notes/review/RUNTIME_REPORT_RETENTION_AND_EMISSION_AUDIT_20260729.md`.
- **Observed:** after explicit user approval, the eight candidates were
  permanently deleted at `2026-07-29T14:27:15+08:00`. Pre-deletion SHA-256,
  byte sizes, compatible successor paths, and compact-evidence checks are
  retained in
  `notes/review/RAW_RUNTIME_REMOVAL_MANIFEST_20260729.json`.
  Exactly 2,910,858,360 bytes (`2.710948102 GiB`) were reclaimed; the runtime
  report directory decreased from 28,138,096,705 to 25,227,238,345 bytes.
  All eight compact evidence sets, all compatible successor raw bundles, and
  the active Stage-5 `120859`/`125453` raw bundles remain present. The
  remaining raw families were not touched.

## Native-to-solver audit reconciliation and Lunatic NMNB roadmap

- The user-supplied `/tmp/ths_analysis.md` was moved into the review
  responsibility directory as
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`.
  Its pre-move SHA-256 is
  `6e5a81f41ef4866c294ef0ab5db937efe614de2b8a3179a559fc878b4a65cb21`.
- The audit baseline was `d85cca1`; diff review through pre-documentation
  HEAD `f28e13c` finds no implicated solver-semantic source change. A bounded
  current-source and shipped-IDB recheck confirms the material findings.
- **Observed IDA identity:** connected `th08.exe`, base `0x00400000`, size
  840704, metadata MD5 `454c96e08fe3c14df7064d104c26accf`, metadata
  SHA-256
  `ec101fcff80b77e717d43b54e326375487af19661bb7c8d11a19ee5e0fbf928b`.
  The analysis image has the known no-life-decrement byte `0x00` at
  `0x0044D0FA`; the clean WSL executable has `0xFF` at raw `0x4CEFA`,
  size 840704, and SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
- **Observed native/source mismatches:** ECL elapsed/fraction timer semantics;
  unscaled player and laser projection; focus-conditioned enemy
  contact/damage eligibility; player/laser geometry; bullet lifecycle and
  collision suppression; offline clamp bounds; callback/action-dependent RNG
  completeness; bounded ECL replacement/call behavior; and native cursor/
  quantization arithmetic boundaries. CE-0175–CE-0181 retain these failures.
- The derived-observer `INT_MAX` reproduction exits with child
  `SIGSEGV`/139. Extreme finite local-beam quantization retains only one of
  two Python-distinct states; the surviving index is deliberately not
  asserted because the native conversion is undefined.
- The Phase-B1 ECL contract now preserves Phase-A capture authority and the
  108 immutable zero-fraction fixtures while withdrawing general native
  timer exactness. Product and scalar code were structurally separate but
  not independent with respect to timer representation. This changes no live
  action authority.
- `notes/review/LUNATIC_NMNB_PROGRAM_REVIEW_AND_ROADMAP_20260729.md` records
  the current live boundary, finding-by-finding disposition, certificate
  invalidation rules, semantic/viability/G5/performance/resource dependency
  order, stop rules, and physical NMNB acceptance ladder.
- Immediate physical priority is unchanged: observer-off Stage-5 controls,
  two consecutive `<=10`-hit passes, Stage-3 and other-stage regression
  checks, then a complete Lunatic Route-2 hard no-Bomb run. No physical trial
  was authorized or launched during this documentation checkpoint.
- No solver source, executable, retained raw capture/report, live strategy
  status, or IDA annotation was changed.
- Complete Linux discovery passes 1,057 tests in 13.557 seconds;
  `git diff --check`, Markdown structural parsing, explicit new-path checks,
  temp-source removal, and no-gameplay-process checks pass. Windows tests were
  not rerun because the checkpoint is documentation-only.

## Physical workload, combat/resource, and IDB-authority refinement

- The Lunatic NMNB roadmap was refined against the retained Stage-3, Stage-4A,
  Stage-5, and Final-B physical dossiers plus proposed strategy S18. No new
  gameplay sample was taken.
- **Observed:** the ten retained complete Stage-5 practices all place the
  canonical first hit in nonspell at practice Power 128. Nonspell accounts
  for 60/128 hits over 82,588/125,088 decisions, which establishes a large
  absolute burden but not greater intrinsic danger per decision.
- **Hypothesized:** some ordinary-enemy segments have a
  kill-before-saturation deadline; verified earlier removal may prevent later
  tracking/homing emission. Spell behavior remains survival-first, with
  damage allowed only as a survival-equivalent phase-compression tie-break.
  Releasing Focus for wider Sakuya coverage remains disabled until delayed
  mode/contact/damage and action-dependent RNG semantics are established.
- Practice and route evidence now have explicit separate authority.
  Mechanics-focused practice may use configured/max Power. Resource authority
  starts Stage 1 at Power 0 and carries earned items, Power, position, RNG,
  damage, and transition history into Stage 3, Stage 4A, Stage 5, and Final B.
  Stage-3 entry Power is accumulated route state, not artificially zero.
- The four stages above are the mandatory integrated physical promotion ring.
  A narrow change first takes the smallest causal physical falsifier; an
  integrated model version must obtain fresh compatible evidence in all four
  scenes before full-route promotion. Stage 1 and Stage 6B remain supporting
  workloads.
- Dynamic debugging is explicitly selective: IDA/source, deterministic
  oracle/replay, and default-off bounded tracing precede an isolated debugger
  session. Breakpoint, stepping, focus, or cadence contamination makes a run
  diagnostic-only and cannot satisfy survival or timing.
- The user clarified that the previous audit's “no IDA database change”
  statement was checkpoint-local. Fix/implementation/accepted-roadmap work is
  authorized and expected to apply evidence-backed IDB renames, types, and
  comments after native revalidation, with material changes recorded here.
  Investigation-only requests remain read-only. The audit and revalidation
  repo skills and their UI metadata were corrected to preserve this
  distinction.
- A bounded GitHub search found and cloned two external hypothesis sources
  outside this repository:
  `thpatch/thtk@892114a0fcaa0bbdaaecf3cb4ad56f758683fb40` and
  `Priw8/eclmap@f146162e330c27d1b0a8880c3a41884615147a11`.
  thtk supports TH08 archive/ECL/MSG/STD disassembly and contains TH08 opcode
  parameter formats; eclmap supplies community TH08 mnemonic candidates.
  Neither is executable-model authority. Use them during
  `SEM-SOURCE`/`COMBAT-GEN` to generate IDA/probe questions. The eclmap
  snapshot exposes no license file, so its content must not be copied into
  this repository.
- The immediate order is now explicit:
  `PHYS-BASE-RING -> SEM-TIMER/SCALE/MODE/GEOM/SOURCE/ROBUST ->
  MODEL-VNEXT -> PHYS-MODEL-RING -> COMBAT-GEN/SHADOW -> POWER-ROUTE ->
  G5/viability/performance`, with causal stop rules at every physical failure.
  No solver source, live strategy authority, executable, retained run, or IDB
  annotation changed in this planning refinement.
- Validation: `git diff --check` passes; the modified audit and revalidation
  skills both pass `quick_validate.py`; LeanToken reconciles the changed
  Markdown as parse-complete and structurally complete; the two external
  clone commits match the recorded pins; and no TH08 game, live agent, or
  physical supervisor process is running. The solver test suites were not
  rerun because this checkpoint changes documentation and skill instructions
  only.

## Observer-off Lunatic Stage-5 baseline pass 1

- **Observed contract:** from clean repository checkpoint `b34f905`
  (executable checkpoint `e4e266f`, unchanged physical code `3f02ff1`), the
  canonical non-TTY Stage-5 command ran with every optional observer,
  auxiliary trace, pipeline shadow, and viability audit disabled. The clean
  executable SHA-256 was
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`;
  the supervisor applied only the authorized no-life-decrement byte at
  runtime.
- **Observed preflight:** no game/controller/supervisor was present; Linux
  and Windows discovery of `test_th08_practice_supervisor.py` each passed
  28 tests; Windows imports reported NumPy 1.26.4; disk headroom was 837 GiB;
  native menu validation selected Lunatic Route 2 Stage 5 and
  Sakuya/Remilia.
- **Observed result:** run
  `lunatic_route2_stage5_unattended_20260729_154229` completed frames
  `2..42335`, 11,710 decisions, route completion, accepted artifacts, exact
  no-save exit, process cleanup, and hard no-Bomb with zero mask, flag, or
  action violations. It took ten hits at
  `[10740,13761,20210,23467,24881,27715,28610,31348,39129,39929]`;
  nonspell/103/107/111/115 split is `5/2/1/1/1`. This is observer-off
  baseline pass 1 of 2, not NMNB success.
- **Observed canonical failure:** frame 10,740 at player
  `(349.070,383.773)`, active `up_fast`, Power 128, 883 bullets, and zero
  lasers is an exact same-epoch enemy-body overlap. Body `0x005A7170` was
  present in causal/action snapshots with AABB clearance `-12.270`; global
  viability exhausted at frame 10,731 and robust/pipeline warnings led by two
  frames. All ten contacts followed global viability exhaustion. CE-0182
  retains the boundary; observer causality and CE-0176/SEM-MODE causality
  remain unproved.
- **Observed health:** cadence median/p95 was `2/4` frames; local-plan
  median/p95 was `11.430/23.615 ms`; 11,531 robust queries contained 7,990
  empty action sets. Issue-time sensing saw 2,382 geometry changes and
  recertified all 2,382, with 41 action overrides and zero silent
  outside-global selections. Three selected cached globally certified
  actions contradicted the fresh local-prefix check and 34 selected actions
  were outside the reported global winning set after exclusions; these are
  retained forecast/version diagnostics, not yet attributed to the canonical
  hit.
- **Observed retention:** the ignored 477,619,552-byte raw JSONL remains
  local with SHA-256
  `fef21b64bfa6c906cb3dcd73b5c08f5ea1fdd00f6ea8e67d3a8466519c533e72`.
  The compact comparison, deaths, dossier, regressions, session, summary, and
  durable run note are retained; the launch log remains ignored. No older raw
  bundle was removed.
- **Observed canonical tracked LF hashes:** comparison
  `8f1824ea710c8f7e16759e13f078388ed4cb2877a35ad0c78dcfdc8b98e26928`;
  deaths CSV
  `56c8f1ebe5b858471737c487cfe606b82e0f607922f7821bb5fb9667ae27ccd5`;
  dossier
  `d688f2dd9bd72d16b7ac7c8211f097d9e55fdd6c9d626c6122981f6182525d0a`;
  regressions
  `98d438f2db341fa7a3c6f360f4faecc16b4d27e419a49d8800ea1bb23f5b8ea2`;
  session
  `99f48e6c98e1195bf3f14414d68611fbba6c83cd6eab2cc959035efa87eadd11`;
  summary
  `91985415017fc836b5ffa552bd0cdcbabdc41f37d364fec5b923f8d45f1cd5d8`;
  run note
  `e848936dca5e2a962f0189cb179c7daddc12f1cbd433c95a803cc73153991299`.
- **Inference boundary:** returning from 18 to ten hits shows current code can
  still realize the historical band without rollback. Different physical
  histories/RNG prevent a causal observer A/B claim, and one pass cannot
  satisfy a two-consecutive-run gate.
- **Next gate:** repeat the exact observer-off Stage-5 command without source,
  strategy, Focus, combat, Power, worker, or planner changes. A result above
  ten resets the sequence and stops expansion for canonical first-hit
  analysis; a second accepted `<=10` result advances to the Stage-3
  mechanics control.
- **Checkpoint validation:** focused supervisor discovery passed 28/28 on
  Linux and 28/28 on Windows before launch; post-run Linux repeat passed
  28/28. All five retained JSON reports parse, the deaths CSV has ten
  consistent 26-column rows, every changed Markdown file is structurally
  complete, `git diff --check` passes, raw/log ignore rules match, recorded
  hashes recheck, and no game/controller/supervisor remains.

## Unchanged observer-off Stage-5 pass 2 rejects consecutive recovery

- **Observed contract:** repository checkpoint `39366f2` changed no
  executable path after `e4e266f`; physical code remained `3f02ff1`.
  Target SHA-256, runtime patch, controller config, Lunatic Route-2 Stage-5
  selection, native backends, hard no-Bomb policy, and every optional
  observer/shadow flag exactly match pass 1. Both controls started at Power
  128, lives 8, and Bomb stock 3.
- **Observed preflight:** the repository was clean; game size/hash and clean
  raw byte `0xFF` at `0x4CEFA` matched; the attach patcher SHA-256 remained
  `1ca9608159dee934a64778391051936a79da28ccebf56d02010949771b810dbd`;
  Windows imported NumPy 1.26.4; Linux and Windows focused supervisor
  discovery each passed 28/28; 836 GiB was free; and no stale gameplay
  process existed.
- **Observed result:** run
  `lunatic_route2_stage5_unattended_20260729_161313` completed frames
  `2..45288`, 12,639 decisions, route completion, accepted artifacts, exact
  no-save exit, process cleanup, and hard no-Bomb with zero mask, flag, or
  action violations. It took 18 hits at
  `[2524,4207,7437,10989,11297,12280,12949,13623,23994,25063,30911,31335,32174,32597,33211,37793,38688,41047]`;
  nonspell/103/107/111/115 split is `10/2/5/1/0`. The consecutive sequence is
  rejected and reset.
- **Observed canonical failure:** frame 2,524 at player
  `(371.121,82.873)`, active `down_left`, issued `down_left_fast`, Power 128,
  267 bullets, and zero lasers is a modeled committed-prefix collision.
  Global viability exhausted at frame 2,284, the robust action set at 2,516,
  and usable pipeline warning arrived only at contact. The final alive
  decision still had current pipeline clearance `+0.539`, while the robust
  prefix minimum was `-4.027` over delay support `2..6`; contact clearance
  was `-1.808`.
- **Observed health:** every one of 18 contacts followed global viability
  exhaustion. Causes were 11 committed-prefix, five observed-bullet, one
  observed-enemy-body, and one sensor/unmodeled. Cadence median/p95 remained
  `2/4` frames; local-plan median/p95 was `11.798/24.375 ms`; 12,475 robust
  queries contained 8,840 empty action sets. Issue-time sensing recertified
  all 2,587 changed geometries, overrode 67 actions, and recorded zero silent
  outside-global selections.
- **Observed regression comparison:** controller config and entry resources
  are exact equal, but the trials are not same-seed/history paired. Pass 1
  first hit at frame 10,740 was a lower-playfield enemy-body overlap with
  delay support `2..4`; pass 2 first hit at frame 2,524 was an upper-right
  committed-prefix collision with support `2..6`. Optional observers are
  ruled out as a necessary cause of an 18-hit realization, but a smaller
  paired effect remains unresolved. No changed action-authority path or
  rollback target exists. CE-0183 retains the causal boundary.
- **Observed retention:** the ignored 550,969,744-byte raw JSONL remains local
  with SHA-256
  `50e8924ba23da9e703b887d8dfd320a025f044754625b66b2939c858ba357ba5`.
  The compact comparison, deaths, dossier, regressions, session, summary, and
  durable run note are retained; the launch log remains ignored. No older raw
  bundle was removed.
- **Observed canonical tracked LF hashes:** comparison
  `4467325b7c0129273f95feffa574cb674c46245cb2f6fa76d90d2592358089ec`;
  deaths CSV
  `0fee4467c0d31792c8e0737312dc87485b3d7c8ec50626fa3bed6c14c00dbb4f`;
  dossier
  `687429f8d8de62ba9985b7d96f851a3b1cb5c35ad88128757d6fc9e6a8ac784a`;
  regressions
  `1bdb1e7bc92de649e4227ea4610d013bf284009f80611f08f0d0ddb4e08f9777`;
  session
  `bf347217bef45c51e15b8c6e8d253adf0ffe8f78f07a934d7fd80b4536b5444c`;
  summary
  `6501803f5091cd115b33bdf9a7644a415fe728293987aff55548d901161a7220`;
  run note
  `ef697eab48b2716749ce49ca75258d96530d29bf6a1943f0baf7d4efe65caede`.
- **Next gate:** `PHYS-BASE-RING` stops before Stage 3. Do not launch a third
  unchanged Stage-5 trial. Begin the roadmap at `SEM-TIMER`, revalidate and
  correct the native elapsed/fraction transition with an independent oracle
  and immutable model version, then take the smallest physical falsifier
  required by that corrected primitive.
- **Checkpoint validation:** focused supervisor discovery passed 28/28 on
  Linux and Windows before launch and 28/28 on Linux after completion. All
  five compact JSON reports parse, the deaths CSV has 18 consistent
  26-column rows, controller-config comparison is exact, every changed
  Markdown file is structurally complete, `git diff --check` passes,
  raw/log ignore rules match, and no game/controller/supervisor remains.

## SEM-TIMER native component semantics pass the offline gate

- **Observed shipped identity:** the connected IDA database analyzes
  `th08.exe` at image base `0x00400000`, input size `0xCD400`, MD5
  `454c96e08fe3c14df7064d104c26accf`, and SHA-256
  `ec101fcff80b77e717d43b54e326375487af19661bb7c8d11a19ee5e0fbf928b`.
  This is the patched analysis input; the clean launch executable identity
  remains separately enforced by `START_HERE.md`.
- **Observed native recurrence:** `0x00447421` reads float32 scale at
  timing-state `+0x188`. Bits `0x3F7D70A4` are the fast-path threshold.
  Above it, elapsed increments and fraction is preserved. Otherwise x87 adds
  scale, stores/rounds to dword before testing carry, then performs at most
  one elapsed increment and dword-rounded subtraction by one. The only direct
  helper caller at `0x00406660` copies current elapsed to previous before the
  call.
- **Observed ECL semantics:** `0x004185AF` gates on exact signed integer
  elapsed equality and ignores fraction. The shared opcode-`0x04` and taken
  opcode-`0x05` path at `0x004186F1` replaces only elapsed, preserves
  fraction, applies relative PC, and continues the same update. The setter at
  `0x00406610` writes elapsed, zeros fraction, and sets previous to `-999`.
- **Material IDA database changes:** declared partial
  `Th08GameTimingState` through `gameplay_time_scale` at `+0x188`; renamed
  `sub_447421` to `advance_scaled_timer_components`; assigned prototype
  `void __thiscall advance_scaled_timer_components(Th08GameTimingState *this, int *elapsed, float *fraction);`;
  and added evidence comments at `0x00447421`, `0x0044743C`, `0x0044744F`,
  `0x00447461`, `0x00406660`, and `0x004186F1`. Each annotation was re-read
  after mutation. No speculative bulk struct or unrelated IDB change was
  made.
- **Implemented correction:** product version
  `th08-native-timer-components-v1-00447421` keeps signed elapsed and raw
  float32 fraction bits, one-frame transition, exact-equality bounded
  scheduling, fraction-preserving branch, and reset components. A raw-bit
  Python oracle shares neither product representation nor helper and uses
  exact integer significand/exponent alignment with explicit
  round-to-nearest-even. A small native probe independently uses x87
  `fadd`/`fsub` and `fstp dword`.
- **Propagation:** the offline VM-local shadow is V2; the existing live
  velocity lookahead and trace-only birth lookahead also replace scalar
  `elapsed+fraction`/`ceil` scheduling with component state. Live traces now
  retain timer fraction bits, scale bits, component identity, and lookahead
  version. No opcode/source coverage, planner action, Bomb, or offline-shadow
  authority was added.
- **Observed differential gate:** Linux and Windows each pass 17/17
  deterministic/adversarial product-oracle-native cases. Report SHA-256
  values are
  `cd9504623ec7ca13649d11525899b1578d6590b7d3658572ccb2fae023881162`
  and
  `15ab29a52a1dc5c149d7599bcae0d75a2b8929c07ae1996786d050c6722adff4`.
  A seeded 4,096-case finite raw-bit product/oracle sweep also passes,
  including matched fail-closed overflow.
  Native probe binaries are ignored; their measured hashes are
  `823c5bf1c6e554b5edb90454c745eea41d154ad4ee8119496f1bd7cb53d25d75`
  on Linux and
  `be4699f03ee5695060f870b65ea9616292a975ea80041cc953f1c9e69895ef11`
  on Windows.
- **Observed replay scope:** immutable historical V1 fixture SHA-256 remains
  `6c34d09752abb7805c84e537b8df52ad24a1aea90614c8b5a2687d730d73ab3c`.
  V2 fixture/replay hashes are
  `112e58b4866faed7a1bed76b91e489aea9e627a8216b7c1ce0a2f17921788e6a`
  and
  `b52eeb18cbe1d53e7ba773f3d5c0dfea8e73ce5478a6382c359d934cce9466ce`.
  All 3,117 retained Stage-4A in-scope rows decode, including 1,730
  opcode-`0x05` roots and 108 unique cases; zero fail and zero newly complete.
  Those physical rows still have fraction zero and unit scale.
- **Validation:** focused timer/live-lookahead/birth/shadow/replay/audit tests
  pass. Focused Ruff passes. Final complete Linux discovery passes 1,075
  tests in 12.845 seconds. The first complete Windows discovery had one
  CE-0166-compatible auxiliary-event timing-tail failure; the isolated
  two-test repeat and subsequent complete discoveries passed, with the final
  1,075-test suite passing in 28.568 seconds with three existing platform
  skips. The fixed timing threshold was not weakened and the first failure is
  not relabeled as a semantic pass.
- **Authority and next gate:** Phase-1A's offline semantics gate passes.
  Executable/physical code is fixed at checkpoint `e309c81`. CE-0175 remains
  open for the observer-off Stage-5 physical rebaseline and the distinct
  player/laser `SEM-SCALE` omission. Launch exactly one Stage-5 falsifier. A
  result above ten hits stops the sequence for first-hit audit; an accepted
  result starts, but does not by itself complete, the corrected two-run gate.

## SEM-TIMER Stage-5 physical falsifier fails the survival threshold without timer-causal attribution

- **Observed preflight and immutable scope:** repository checkpoint `55ec4ee`
  fixes physical code at `e309c81`. The clean executable remained 840,704
  bytes at SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`,
  with clean byte `0xFF` at raw offset `0x4CEFA`; attach-time no-life-decrement
  instrumentation remained the only runtime patch. Focused practice-
  supervisor discovery passed 28/28 on Linux and 28/28 on Windows before
  launch. The exact Stage-5 observer-off command kept every optional
  observer/shadow false and hard no-Bomb.
- **Observed physical result:** run
  `lunatic_route2_stage5_unattended_20260729_173957` completed frames
  `1..42021`, 11,597 decisions, accepted route completion, exact no-save
  transition, key release, process cleanup, and zero Bomb action/flag/mask
  violations. It took 12 hits at
  `[2069,4339,10942,12268,12898,13997,22831,23379,28817,30275,35316,39806]`;
  nonspell/103/107/111/115 split is `7/2/2/0/1`. Because `12 > 10`, the fixed
  physical threshold fails and same-version repetition plus Stage-3/4A/
  Final-B expansion stops.
- **Observed canonical failure:** frame 2,069 at player `(376,424)`, active
  `up`, Power 128, 663 bullets, and zero lasers is a modeled committed-prefix
  collision with pipeline clearance `-2.222`. Global viability exhausted at
  frame 1,829, robust actions at 2,063, and usable pipeline warning led by
  two frames. Every one of 12 contacts follows global viability exhaustion.
- **Observed timer-consumer scope:** the corrected velocity lookahead is
  absent on 7,657 decisions and present on 3,940, first at frame 21,751 and
  last at 42,018. The canonical and first six hits precede the consumer.
  Every row carries
  `th08-ecl-velocity-lookahead-v2-native-timer-components` and
  `th08-native-timer-components-v1-00447421`, with exact elapsed/fraction/
  scale identity agreement and no reported error. There are 2,794 complete
  schedules, 1,146 explicit unsupported-control-flow prefixes, and 9,413
  retained prefix events on 2,699 rows.
- **Observed physical slice:** every fraction bit pattern is `0x00000000`
  and every scale bit pattern is `0x3F800000`. This exercises live delivery
  and the historical unit-scale/zero-fraction slice, not nonzero fraction,
  carry, or nonunit scale. Those semantics retain offline authority only.
- **Revalidated IDA/native equality:** shipped helper `0x0040D3F0` loads
  timer elapsed at `this+0x08`, subtracts the argument, and forms Boolean
  equality through `neg/sbb/inc`. At `0x004185AF`,
  `enemy_ecl_vm_step` passes current instruction dword `+0x00` and exits the
  update when the predicate is false. This revalidates, rather than changes,
  the existing equality annotation; no new IDB mutation was needed.
- **Corrected address analysis:** an initial manual decode incorrectly
  applied Stage-4A runtime base `0x0B1D1430` and falsely suggested past-time
  roots. Fresh `infer_runtime_base` analysis over every current live root
  uniquely selects Stage-5 base `0x0B1D0048`: 33/33 unique PCs map, versus
  runner-up 19. Against decoded `ecldata5.ecl` SHA-256
  `3148f45faf78bd8211a956edcdc353be73d2781995d3dadd36bdca8132f8fe19`,
  relations are 3,835 future, 105 equal, zero past, and zero unmapped. No
  source, IDB, or authority conclusion retains the rejected manual decode.
- **Regression conclusion:** the aggregate result is six hits below unmatched
  `161313` but is not a same-seed/history causal improvement. The canonical
  and first six failures cannot be caused by a consumer that is absent.
  Physical-regression audit therefore does not justify timer rollback.
  CE-0184 retains the failed survival threshold and the address-identity trap;
  the ordered roadmap continues at `SEM-SCALE` without SEM-TIMER survival
  promotion.
- **Retained provenance:** ignored raw JSONL is 477,436,227 bytes and 11,691
  lines at SHA-256
  `e0b32ee0e042056079d7159f1055aa2ad289447d03efc078d2c2d73f98585367`.
  The two newest compatible replay-capable raw bundles remain this run and
  `161313`; no older bundle was removed. Compact comparison, deaths, dossier,
  regressions, session, summary, and the durable run note are retained; the
  launch log remains ignored.
- **Canonical tracked LF hashes:** comparison
  `1c0b71e12d40df0e455e734eaad6deec329ddf0c3540d5b983bdc63b2f3a4eac`;
  deaths
  `ddbc54cef09b72bc3b6af84fa97d1e9cb99753fe6740b0011bf059cbf904d5a6`;
  dossier
  `72329ac180f1babe0c447a800d51edcc8fdbf0b00ddb6a7d8f47bfc039f15504`;
  regressions
  `bec32cca8bd897b35ede1f1bfc075cbbd7b36025b747e88a0108d26fc196d533`;
  session
  `b440a1a31a9b77a8ac5a7def090017927ed43468bbcbe1ba5d23f2fcad98b02a`;
  summary
  `5d88c19983ac9a20c01195b9a574aff8717132e949ceb3ef697ca85f960733b7`;
  run note
  `e4f5cc78b01898955d0018607181e0a3c3417b843dc63e6c44928a8cb0d65b4e`.
- **Retention validation:** post-run focused Linux supervisor discovery passes
  28/28. All six compact hashes and all five JSON parses recheck; the deaths
  CSV has 12 rows of 26 columns. Raw size/line/hash and ignore rules agree,
  the launch log remains ignored, and no Windows game or Linux
  controller/supervisor process remains.

## SEM-SCALE-A exact player/laser primitives pass the offline semantic gate

- **Observed static order and transitions:** connected shipped-game IDA
  revalidation confirms player priority 9, enemy/ECL priority 11, and
  bullet/laser priority 14. Player deltas are stored float32 after SHT speed
  times axis scale and again after global scale at
  `0x0044BA67/0x0044BA79`; position additions store at
  `0x0044BAA8/0x0044BAD8`. Laser head uses one x87
  `speed * global_scale + head` chain with its field store at
  `0x00431BC9..0x00431BE1`; phase/collision fallthrough precedes the scaled
  timer call at `0x004320E2..0x004320ED`. An enemy callback can therefore
  change the same update's laser scale after the player has already moved.
- **Observed writers:** callback 18 at `0x00424F90` writes float32
  `1.0 / int32(arg1)`. Callback 28 at `0x004251B0` writes that reciprocal and
  float32-scales all active bullet velocity vectors. Callback 29 at
  `0x00425290` restores active bullet velocity using the reciprocal current
  scale and finally writes `1.0`. Initialization/reset writers do not prove a
  constant future horizon.
- **IDA database changes:** `0x004251B0` is renamed
  `ecl_cb_apply_global_slowdown_and_scale_bullets`; `0x00425290` is renamed
  `ecl_cb_restore_bullets_and_time_scale`; `0x00423D70` is renamed
  `vec3_scale_inplace`. Evidence comments are persisted at those three
  addresses plus `0x00431BC9` and `0x0044BA67`. Callback 18's inherited name
  was revalidated and retained. No unproved function prototype was promoted.
- **Corrected state contract:** semantics version
  `th08-player-laser-phase-scale-v1-0044ba67-00431bc9` carries raw root bits,
  separate exact player/laser future prefixes, coverage, provenance, and
  source frame. Root-only live evidence proves one next-player scale and no
  next-laser scale. Missing frames are rejected rather than filled by root
  repetition.
- **Implementation:** Route-2 movement now accepts exact scale bits and
  preserves every declared float32 store. Laser head/tail/width/lifecycle
  stepping accepts exact scale bits and delegates elapsed/fraction carry to
  `th08-native-timer-components-v1-00447421`. Cached/object/packed laser
  projections require explicit schedule coverage. A structurally independent
  rational raw-bit oracle shares no product movement/laser/store helper, and
  a small C++ probe executes explicit x87 stores on Linux and Windows.
- **Observed differential gate:** all 17 deterministic cases pass on both
  platforms: 36 player steps and 44 laser steps across unit, `1/2`, `1/3`,
  `1/4`, `1/12`, zero stop/resume, direction/focus changes, clamp, timer
  carry, cull, and warmup/active/fade fallthrough. The independent seeded
  product/oracle sweep passes 4,096 player plus 2,048 laser cases with zero
  mismatch and digest
  `9cd09a2ed69aba52f4f2c98dee3a7f22d124bfa585975483a87fa92e662a593d`.
- **Retained evidence:** Linux/Windows reports are
  `artifacts/benchmarks/th08_player_laser_scale_differential_linux_20260729.json`
  and
  `artifacts/benchmarks/th08_player_laser_scale_differential_windows_20260729.json`
  at SHA-256
  `15f75b79dc3732014dae0e8a1f34801f4a31541f4e30026322453523c2387637`
  and
  `e28c9d50d78f66595cbebf215571fd598e77a094f00840602d10c836bca07197`.
  Ignored native binaries are
  `94573324286fcf95994a6dd22c3ff971a5aa7bbf5110fbef5a0b2b0639e163c9`
  on Linux and
  `929aeb2b0af8fb0c62ecf0199f95ba522ac4ea7b33dc09fb4b1bc57e7e5c5121`
  on Windows.
- **Validation:** focused Ruff passes. Nine SEM-SCALE and 17 laser tests
  pass. Complete Linux discovery passes 1,084 tests in 13.076 seconds;
  complete Windows UNC discovery passes 1,084 in 28.687 seconds with the
  three existing platform skips.
- **Authority and stop:** SEM-SCALE-A's exact primitive gate passes.
  Executable/offline checkpoint is `225ccc8`.
  SEM-SCALE-B live observation/identity/consumer propagation and
  SEM-SCALE-C Final-B/Extra capsules plus physical falsifiers remain open.
  This checkpoint does not authorize a scale-sensitive hard certificate,
  does not close CE-0175, and does not launch or expand any physical scene.

## SEM-SCALE-B propagates identity and fails closed without a complete schedule

- **Implementation checkpoint:** `6a71ac1`
  (`Complete SEM-SCALE-B authority propagation`) from clean handoff
  `c19ffb0`; primitive baseline remains `225ccc8`. The shipped executable is
  unchanged at SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
- **Observed live boundary:** the runtime now reads
  `0x017CE8E0` between two `enemy_manager_frame` reads. An unstable bracket is
  discarded. A stable post-update root is retained as exactly one future
  player scale and zero future laser scales; it is not repeated.
  `CapturedIteration` records source/root bits, coverage, provenance, source
  frame, and player-projection authority.
- **Observed consumer propagation:** immutable phase schedules now cross
  `PhysicalHazardSnapshot`, local and fresh-issue requests, committed-prefix
  projection, Python beam stepping, vectorized robust certificates,
  supplemental exact versions, laser timelines/lowerers, corridor
  artifact/pipeline identities, sensing traces, and corridor audit capsules.
  Player stepping repeats the native-order float32 primitive per frame.
  Native local-beam reduction ranks positions produced by those exact steps.
- **Observed authority rejection:** local validation requires complete
  player/laser coverage through the maximum read-delay, planning, threat, and
  certificate horizon. Root-only/short input raises
  `IncompleteTimeScaleScheduleError`. The current coarse corridor accepts
  only a complete unit schedule; complete non-unit or varying schedules raise
  `UnsupportedTimeScaleScheduleError` and remain `UNKNOWN` until scaled
  corridor action semantics exist. No rejected solve publishes a policy
  identity.
- **Historical compatibility:** flat planner/issue wrappers and the retained
  pre-scale audit explicitly construct only the finite unit prefix needed by
  that call under `historical_*_unit_assumption`. They do not claim future
  completeness or upgrade old artifacts.
- **Independent/parity evidence:** the scalar local-pipeline oracle consumes
  a generic per-step movement schedule. Complete constant non-unit
  Python/native local-beam decisions match; optimized/scalar certificates
  match safe-action masks and CVaR on the retained authority probe. Laser
  lowering respects a zero-scale stop. Root-only local/corridor and complete
  non-unit/varying corridor probes all take explicit `UNKNOWN`.
- **Retained reports:** Linux
  `artifacts/benchmarks/th08_scale_schedule_authority_linux_20260729.json`
  at SHA-256
  `bc2ff09955e12bd9ffc629b20dfd93dc7ecbc46f35a4d29407859c042e5ea966`;
  Windows
  `artifacts/benchmarks/th08_scale_schedule_authority_windows_20260729.json`
  at SHA-256
  `8a202bdd7c51f05bc7d59368c054333d19f2944ae2c09fda8d14a972d472f891`.
  Both pass all eight declared checks and report native local-beam
  availability.
- **Validation:** `git diff --check` passes. Complete Linux discovery passes
  1,087 tests in 11.752 seconds. The verified Windows UNC loader passes 1,087
  tests in 25.138 seconds with the three existing platform skips.
- **Authority and stop:** this is an observed offline authority-boundary
  result, not live schedule completeness or physical evidence. With only a
  root observation, the daemon now emits `time_scale_authority_unknown`,
  terminates before corridor/local issue, and uses its existing finalizer to
  release keys. No game was launched, no IDA database change was needed for
  B, no strategy was promoted, and CE-0175/CE-0184 remain open.
  Continue at SEM-SCALE-C: retain the compact Final-B `0.25` root capsule,
  implement and independently validate a causal full-horizon phase-schedule
  producer, then take trace-only and preregistered focused physical gates in
  that order.

## SEM-SCALE-C1 retains the physical Final-B quarter-scale root capsule

- **Checkpoint and scope:** repository checkpoint `1f639ef`
  (`Retain Final-B quarter-scale root capsule`) adds a streaming extractor,
  focused tests, and compact retained report. It consumes three existing
  shipped-game sessions only; no game was launched, no live solver path or
  strategy changed, and no IDA database mutation was made for C1.
- **Observed physical roots:** spell 190 contributes 68 rows at frames
  `74791..75025`, 78 at `74726..74962`, and 84 at `73345..73585`, for 230
  selected binary32-`0.25` roots. Their gameplay epochs are 5/3/3. Every
  selected row has zero active lasers, false Bomb emission, and constant
  per-session Power 14/5/6.
- **Raw provenance:** the local ignored traces are respectively
  323,061,590/384,027,287/459,012,741 bytes at SHA-256
  `2ba795ee89354fb2f02935a7de81f079f24845275b27e09379c593808171cdf0`,
  `d9075e4e2946d74cc3337d6e2a99a8680ee85c730f7f850b6a03e06c87a1524a`,
  and
  `b7384a0c7708f401c24abd80fe5e15f4370914bf10bf4e9e06967591adc355a2`.
  No raw bundle was removed.
- **Inferred historical replay:** each retained projected-player and
  control-origin coordinate is reproduced exactly by the pre-scale unit
  helper. The movement action is reconstructed from those outputs; stationary
  focus ambiguity is retained only where both choices are movement
  equivalent. This is not evidence that held desired input was native active
  input.
- **Corrected conditional result:** all read-lag horizons are zero or one, so
  the observed root covers the scale used in the input-conditional replay.
  The corrected primitive changes 113 legacy read-lag positions;
  mean/p95/maximum delta is
  `1.157934983/3.000009642/3.000020432` pixels. Corrected native
  unit-versus-quarter one-player-phase comparison changes 180 rows, with
  maximum `3.000024815` pixels.
- **Explicit non-authority:** mechanically repeating the root over the old
  multi-frame control-delay horizon reaches `12.000081728` pixels of
  difference, but is labeled noncausal after the first phase. The capsule
  proves no future laser scale, active/pending input history, action
  certificate, clean survival, or NMNB claim.
- **Retained report:**
  `artifacts/benchmarks/th08_finalb_scale_root_capsule_20260729.json` has
  SHA-256
  `d0b48a68c883a00f6c0b73636461ebac09985bf8a935677f799c8671131a82e4`
  and canonical payload digest
  `e841ea9ecc3f2a03abe786305474bf407421e7348c6c1608d40a9c4f987656ab`.
- **Validation:** focused C1 discovery passes 2/2 on Linux and Windows.
  Windows independently parses all three raw sessions to identical source
  hashes and aggregate metrics. Complete Linux discovery passes 1,089 tests
  in 13.735 seconds; complete Windows UNC discovery passes 1,089 in 28.586
  seconds with the three existing skips.
- **Next gate:** implement and independently validate the causal player/laser
  phase-schedule producer without extending root coverage, then retain the
  separate static Extra workload and take the trace-only gate. Focused
  Final-B physical execution remains unauthorized until those pass.

## SEM-SCALE-C2/C3 adds the causal isolated-source schedule and static Extra

- **Implementation checkpoint:** `555bbf8`
  (`Add causal ECL scale schedule producer`) from retained C1 handoff
  `b67af09`. The shipped executable remains SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
- **Observed ECL integer semantics:** connected IDA revalidation confirms that
  ECL variable 10099 reads `g_spell_card_state` bit 2 while active bit 0 is
  set and bit 9 otherwise. Variable 10100 calls `timer_current` on the
  embedded timer at spell-state `+0x108`, so its integer current is at
  `+0x110`. The runtime spell sensor now reads through `+0x113` in the same
  process-memory call and exposes `timer_elapsed`; the legacy 68-byte decoder
  remains accepted with `None` for that field.
- **IDA database changes:** `0x004178A0` was renamed
  `spell_state_active_flag`; `0x0041FD90`,
  `spell_state_active_and_flag_bit2`; `0x00405260`,
  `spell_state_flag_bit9`; and `0x0041FDD0`,
  `spell_state_timer_elapsed`. Comments at `0x0041FB4D`, `0x0041FB7C`, and
  `0x0041FDD0` record the exact 10099/10100 branch and `+0x110` timer-current
  dataflow.
- **Causal phase recurrence:** for each future physical update the producer
  first records the scale used by player priority 9, executes the supported
  ready ECL prefix at enemy priority 11, records the scale used by laser
  priority 14, and then advances the VM timer with that post-write scale.
  This makes a same-frame callback-18 write visible to laser but not player.
- **Authority boundary:** a complete schedule requires the exact singleton
  scale-writer source set, complete writer inventory and scheduler ordering,
  absence of installed scale callbacks and unmodeled phase transitions,
  coherent post-update VM/root/external state, and supported control flow.
  Missing evidence yields root-only or partial coverage. A future
  spell-finish branch also requires the declared no-hit/no-Bomb continuation;
  losing histories do not receive the favorable capture branch.
- **Supported subset:** literal jumps, the projected VM-local decrement loop,
  literal integer assignments, supported integer conditionals including
  10099/10100, callback 18/28 reciprocals, callback 29 restore, and the
  control-relevant `spell_card_finish` flag writes are modeled. Callback
  install, unsupported control, missing external schedules, and invalid
  divisors stop before laser authority. Callback 28/29 writes record whether
  active bullet velocity is also rescaled; live hazard authority remains
  blocked until that side effect is consumed.
- **Independent oracle:** the raw oracle decodes instruction bytes locally,
  carries timer/scale/locals as plain integers, uses the separate exact raw
  timer recurrence, and rounds positive int32 reciprocals from exact rational
  arithmetic. Product/oracle agreement also holds for 100,012 deterministic
  and seeded reciprocal divisors.
- **Static Final-B and Extra:** the isolated Final-B sub44 workload at
  `ecldata7.ecl:0x5c58` and Extra sub86 workload at
  `ecldata8.ecl:0x87e8` both have complete 300-frame product/oracle parity.
  Both execute callback 18 restore on future frame 241. Player has 241
  quarter-scale phases while laser has 240, because laser observes the
  same-frame restore. Final-B consumes external variable 10099 and restores
  at `0x6018`; Extra consumes no external variable and restores at `0x8b58`.
- **Historical physical transition:** session `004142` supplies stable ECL
  frame 74790, runtime base `0x0B1D0048`, PC offset `0x5c90`, elapsed 1,
  zero fraction, flags `0x825`, and quarter scale. The literal set at
  `0x5c6c` establishes inferred loop counter 6. Product and oracle predict
  restore 237 updates later at ECL frame 75027, exactly the first observed
  unit-scale row. Sessions `011639` and `163501` stop ECL scale capture at
  spell-finish/scene-freeze immediately around their predicted restore and
  are retained as right censored.
- **Retained report:**
  `artifacts/benchmarks/th08_ecl_scale_schedule_capsule_20260729.json` has
  SHA-256
  `2c6cedcc5b30b4e9f805ff19cc7cbcd465f6123df2ccfda63e2fac21a8777d27`
  and canonical payload digest
  `77ff84d3d8fa8ae9dd5c076edd8881a0a6101457ec8603ba103ea4f55e2ba380`.
  Linux and Windows render byte-identical retained output.
- **Validation:** focused Ruff and schedule/runtime tests pass. Complete Linux
  discovery passes 1,096 tests in 14.353 seconds. The verified Windows UNC
  loader passes 1,096 tests in 29.889 seconds with the three existing skips.
- **Authority and stop:** C2/C3 pass the declared isolated-source mechanics
  and static Extra gate plus one historical physical transition. They do not
  prove complete live main/auxiliary source inventory, installed-callback
  state, scheduler/phase coherence, callback-28 hazard parity, clean survival,
  or NMNB. No game was launched and no strategy was promoted. Next implement
  a default-off trace-only complete-source join; only its accepted result may
  authorize the preregistered focused Final-B transition falsifier.

## SEM-SCALE-C4 implements the read-only complete-source observer

- **Implementation checkpoint:** `ead1f21`
  (`Add Final-B complete scale-source observer`) leaves the shipped
  executable unchanged at SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`.
- **Corrected native layout:** connected IDA revalidation at
  `0x0041E852` shows that the installed per-update callback and retained ECL
  record are current ECL VM `+0x10/+0x14`, not enemy base
  `+0x10/+0x14`. Main selection is enemy `+0x7F8`, so the fields are enemy
  `+0x808/+0x80C`; auxiliary selection uses context active VM `+0x08`.
  The main VM completes before auxiliary contexts 0..3, and enemy motion at
  `0x0041ECA7` follows them. IDA comments at `0x0041E852` and
  `0x0041EBBC` retain the corrected fields/order. CE-0185 preserves the
  inherited-label failure.
- **Complete capture transaction:** the standalone Windows observer waits for
  Lunatic Final-B spell 190 without sending input or changing foreground. It
  establishes exact runtime/static `ecldata7.ecl` identity, then brackets one
  480-slot ordinary-pool read plus any out-of-pool spell owner with manager
  frame, route/difficulty/stage, full spell state, ECL context, root scale,
  Bomb-active, and predeath identity.
- **Fail-closed source join:** every active source retains main PC,
  component timer, locals, selected-VM callback/function-record, and four
  auxiliary pointers. C4 accepts only a singleton valid spell-owner main VM,
  no auxiliary context, no installed scale callback, exact binary32 quarter
  scale, no current Bomb, stable retained predeath state, sub44 ownership,
  complete 300-frame causal synthesis, at least one supported scale write,
  and no callback-28/29 active-bullet velocity rescale. All other cases are
  `UNKNOWN`; nonzero predeath narrows clean-player/survival authority.
- **Independent physical report boundary:** the report tool rechecks
  executable/runtime-ECL/scope/configuration identity, full-pool source
  completeness, callback/auxiliary absence, clean quarter root, complete
  schedule, supported callback-18 restore, and empty internal error/reason
  state. It explicitly states that the no-hit/no-Bomb future is a target
  continuation, not an observation.
- **Validation:** 11 focused source/report tests and Ruff pass. Complete Linux
  discovery passes 1,107 tests in 13.728 seconds. The verified Windows UNC
  loader passes 1,107 tests in 29.427 seconds with the three existing skips.
- **Authority and stop:** no game was launched, no physical C4 row was
  captured, no input path or live schedule consumer changed, and no strategy
  was promoted. The live daemon remains root-only fail-closed. Next run only
  the read-only observer against an already driven Final-B spell-190 scene or
  replay; an accepted strict report is required before live delivery and the
  focused Final-B transition falsifier.

## Native replay physically accepts the SEM-SCALE-C4 source gate

- **Replay selection:** 15 native TH08 replay files were parsed before
  launch. Slot 13 was selected because `th8_13.rpy` is Route 2/Lunatic,
  contains Final B only, has 51,711 input records, and has zero Bomb presses.
  Its file SHA-256 is
  `1026289ffec9f3dd1858378e81bbbbb84f568f041047a401dd86f74211c4a7f2`;
  the decoded input digest is
  `90c75156cf36a1c1576f082b2fe2b435cec8395af09014a1ad6c10c02e7a060e`.
  This binds a repeatable shipped-runtime scene without granting solver
  action authority.
- **Native replay-menu revalidation:** `0x0046E136` is renamed
  `title_replay_menu_update`. The path at `0x0046E29C` compacts successful
  fixed/user replay decodes, so selecting on-disk slot N requires binding all
  preceding fixed entries and verifying the compact native entry. Comment
  `0x0046ED45` records the final launch state: replay flag bit `0x08`,
  selected path, route, difficulty, and stage. The physical supervisor checks
  that state before accepting gameplay.
- **First falsifier:** attempt `20260729_211715` triggers on the first
  spell-190 row while scale is still `1.0`. The full-pool capture takes
  36.834 ms and crosses the manager-frame bracket, so the strict report
  correctly fails. CE-0186 retains the false spell-only trigger and
  allocation assumptions. Capture/report SHA-256 values are
  `4686ee59f655345d9bf22c6b247418dc980e0e2298b0a843835e09488238c36c`
  and
  `a12635d359af79067c470787624a0b92badadf653c85d63fe09c537545502128`.
- **Capture correction:** checkpoint `88b8c14` arms only at spell 190 plus
  binary32-quarter scale and Bomb zero, reuses a preallocated reader buffer,
  avoids copying the ordinary pool, and treats the stable bracket frame as
  the source root. Complete Linux discovery passes 1,114 tests in 14.015
  seconds; the verified Windows suite exits zero.
- **Authority falsifier:** attempt `20260729_213600` reaches spell 190 at
  manager frame 74794 but never triggers because predeath is 7. Historical
  physical decision frame 74791 in session `004142` has the same residue,
  `player.phase=3`, no hit edge, no Bomb, and a last recorded hit thousands
  of frames earlier. The no-life-decrement patch preserves such native
  residue. CE-0187 rejects conflating a zero-predeath clean-player gate with
  the source-semantic gate. Capture/report SHA-256 values are
  `53b7f0845e96c717ed289a7863cb8cb796443e2c195abe09ee1873e12677965c`
  and
  `6ada457ffae19a3c16aaabee5b48d57192d3f06ea55300ab6288def9482b0b25`.
- **Authority correction:** checkpoint `0c33645` requires stable
  quarter-scale and no-Bomb source state while retaining the exact predeath
  contamination. The continuation is explicitly
  `no_new_hit_no_bomb_scale_target_not_future_observation`. Complete Linux
  discovery passes 1,114 tests in 12.873 seconds; the verified Windows suite
  exits zero.
- **Accepted physical gate:** attempt `20260729_215613` binds executable
  SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`,
  the verified no-life patch, and exact runtime/static `ecldata7.ecl`
  SHA-256
  `20b35dca3820438f0b90ae44e3362a7af27d2fc1ac7ae5888c477dc1c89a3734`.
  One transaction captures manager frame 74787 in 7.734 ms using 22 reads and
  10,312,710 bytes. All 480 ordinary slots are inactive; the out-of-pool
  spell owner is the one valid main-VM source. Auxiliary pointers and
  installed callback are zero. Root scale bits are `0x3E800000`, Bomb is
  zero, and predeath residue is 7.
- **Accepted schedule:** the physical main VM yields all 300 requested frames
  and 199 instructions. The only scale write is callback 18 at relative frame
  240, from quarter to unit scale. No callback-28/29 active-bullet velocity
  side effect occurs. The independent strict report passes every check.
  Capture/report SHA-256 values are
  `22e3d69d249b512f6c73e3816e3206373c89776b53f183fc0ebf7e1d71a2e48d`
  and
  `d4aa9b9c065bde6de3ba899ff95e4ff3eea10f166714e561c00743100d4e138a`.
- **Authority:** observed exact replay/executable/ECL identity and one
  coherent complete-source physical transaction; inferred only the declared
  no-new-hit/no-Bomb target schedule. This does not prove a clean
  zero-predeath root, live action authority, callback-28 hazard consumption,
  survival, or NMNB. No live controller input path changed.
- **Reusable method:** the procedure is retained in
  `notes/operations/NATIVE_REPLAY_PHYSICAL_FALSIFICATION.md`. Future agents
  should use exact replay preflight, native menu/state verification,
  menu-only input, read-only gameplay observation, strict independent
  reporting, explicit replay/resource contamination, and exact cleanup when
  a shipped replay can reproduce a native hypothesis more faithfully than a
  synthetic fixture.

## SEM-SCALE-C5 connects exact source schedules to the live local path

- **Default-off authority:** live control gains explicit
  `--enable-finalb-scale-source-authority`. Validation requires Lunatic,
  stage route index 7, hard no-Bomb, and a paired immutable static ECL path
  and SHA-256. Existing invocations remain unchanged and root-only
  fail-closed.
- **Typed source handoff:** the C4 observer retains its accepted typed
  `Th08TimeScaleSchedule` but still declares no action authority. A separate
  live authority binds it to gameplay epoch, route, difficulty, stage, spell,
  and original source frame.
- **Causal rebase:** for current manager-frame offset `d`, the observed
  post-update root must equal the original root at `d=0` or the original
  laser-phase value at `d-1`. The future player and laser tuples are sliced
  exactly from `d`, assigned the current source frame, and never extended
  after exhaustion.
- **Exact physical continuation:** exact live use binds the predeath value
  stable inside the complete-source transaction. Fresh hit, Bomb-active
  state, baseline change, immutable-context mismatch, future/past source
  frame, observed-root mismatch, or insufficient horizon returns root-only;
  the controller then emits `time_scale_authority_unknown`, terminates, and
  releases keys. A nonzero baseline remains contamination, not a clean root.
- **Stage-launched transport:** THPRAC is unavailable, so the existing
  original-game supervisor selects all of Stage 6B. Before the exact source,
  unit roots receive an explicitly unknown-direction 256-frame transport
  schedule that may act but is serialized with `hard_authority=false`.
  Repeated phase tuples are omitted with `phase_schedule_omitted=true`, and
  the strict report streams rather than retaining all pre-target decisions.
  Non-unit unknown rows wait without input. The exact C5 acceptance scope
  begins only at offset zero.
- **Consumer boundary:** varying/non-unit coverage remains rejected by the
  corridor precheck. The exact schedule reaches the synchronous local
  planner, certificate, issue, and sensing records. No stale corridor policy
  can be created from this unsupported schedule.
- **Retained physical regression:** the accepted C4 artifact drives a
  deterministic test. At offset 239, the next player scale is quarter while
  the next laser scale is unit; at offset 240 the observed root and both
  remaining schedules are unit.
- **Physical infrastructure:** the unattended original-game entry point is
  `run_th08_finalb_scale_live_trial.bat`. The fixed stage launch, cleanup,
  first-hit/Bomb stop conditions, and exact-scope acceptance boundary are in
  `notes/operations/FINALB_SEM_SCALE_LIVE_DELIVERY_GATE.md`. The independent
  streaming report is
  `scripts/analysis/th08_finalb_scale_live_delivery_report.py`.
- **Report fail-closed review:** the strict report independently checks
  Route 2/Lunatic/Final B/spell 190 in both the trace envelope and captured
  native phase, exact schedule/source identity, one live origin, each
  decision-to-authority identity, stable predeath baseline, experimental
  transport labeling, runtime errors, and automatic exact-restore
  termination. Wrong scope, a hidden second origin, baseline drift, or a
  foreground/runtime error has a retained rejecting test.
- **Validation:** focused authority, C4 observer, supervisor/CLI, and strict
  report tests plus Ruff pass. Complete Linux discovery passes 1,132 tests in
  13.596 seconds. The first Windows complete run exposed an implicit GBK
  decode of the retained UTF-8 physical artifact; explicit UTF-8 fixes the
  fixture boundary. The final complete rerun passes 1,132 tests in 29.764
  seconds with three existing skips.
- **Authority and stop:** no game was launched and no live physical action,
  hit-free transition, survival, or strategy promotion is claimed. Only the
  preregistered focused spell-190 transition is authorized next.
