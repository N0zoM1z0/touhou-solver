# TH08 Counterexamples CE-0170–CE-0219

## CE-0170: Exact cross-epoch event replay still missed physical delivery and survival gates

Date: 2026-07-29

Observed symptom:

- Schema-v6 Stage-5 run `20260729_104222` passed exact program identity across
  accepted epoch 0 and observation epoch 2.
- All 127 batches and 3,384 requests independently replayed complete with
  zero unknown; online preparation was `0.121 ms`.
- Event derive, replay compact, and previous synchronous emit p95 were
  `0.620/0.564/2.862 ms`, failing fixed `0.500/0.500/1.000 ms` limits.
- The run also took 11 hits: nonspell/103/107/111/115 =
  `4/0/1/4/2`, one above the user-set maximum.

Invalid assumption:

- Removing full-program bind cost and fixing epoch identity is sufficient to
  make the existing verbose evidence representation physically deliverable.
- Repeating null-record dictionaries and full derivation JSON is necessary
  for independent replay.

Evidence or trace:

- code checkpoint `fcc37ec`;
- raw trace SHA-256
  `365aaee1c9a328b45b538510536bb5736a0ed66f699058e50b93a6a80882eee4`;
- strict failed report SHA-256
  `3738d329d1921932b0688223f044d4666f29825e72023b9ee43cb03ee76cb24b`;
- result note
  `notes/research/g5/G5_AUXILIARY_LITERAL_FIRE_CYCLE_RUNTIME_DELIVERY_V3_STAGE5_FAILURE_20260729.md`.

Root-cause class: control/runtime evidence materialization and delivery;
survival remains a separate planner/workload failure.

Correction:

- retain V3 as the first observed cross-epoch semantic delivery but reject
  physical delivery authority;
- project only usable records with exact source indices while proving omitted
  records null through the native status histogram;
- retain every raw replay byte and replace repeated derived results with
  independently reconstructed canonical commitments;
- decode only the timer state consumed by the literal proxy; and
- keep every timing, cadence, hard no-Bomb, cleanup, and survival threshold
  unchanged under a separately fixed V4 contract.

Regression test:

- projection rejects omitted non-null status, duplicate/out-of-range source
  indices, raw/hash/bundle mismatch, and result-commitment tampering;
- independent raw-byte replay and LRU simulation remain exact;
- retained replay crosses at least three observation epochs and meets the new
  isolated line-size/serialization gates;
- V1/V2/V3 reports remain immutable.

Live verification: V3 failed as observed. V4 requires a fresh focused
physical run and two consecutive Stage-5 runs at no more than ten hits before
handoff.

Status: observed physical counterexample; V3 semantic epoch correction
accepted as evidence, delivery/survival rejected, V4 fixed before
implementation.

## CE-0171: Foreground loss contaminated the first schema-v7 attempt

Date: 2026-07-29

Observed symptom:

- The first checkpoint-`5fae77a` Stage-5 attempt armed correctly and reached
  frame 2,736, then the foreground guard observed a non-TH08 foreground PID.
- The controller immediately raised
  `TH08 lost foreground; refusing to send or retain keys`.
- The attempt ended before spell 107 with one nonspell hit and zero auxiliary
  batches. The supervisor retained a failed session and completed exact
  process/input cleanup.

Invalid assumption:

- A successfully focused and armed unattended launch will retain foreground
  for the complete physical route.
- A partial low-hit attempt can be counted as a V4 delivery or survival pass.

Evidence or trace:

- run note
  `notes/runs/lunatic_route2_stage5_unattended_20260729_112743.md`;
- raw trace SHA-256
  `94f3a68306f230a96341311f474c8d4f1e9f577aaa2e26653cfe32cd79c47114`;
- normalized session SHA-256
  `9ea76db56ccf43fcc55c544a095b0c4fd449652b7075f8e82f7c06fd52662a5f`.

Root-cause class: control/runtime external foreground contamination.

Correction:

- keep the hard foreground guard, fail-closed key release, exact cleanup, and
  failed-run retention;
- exclude the attempt from delivery and consecutive survival counts; and
- retry the unchanged checkpoint only after verifying no residual process
  and valid executable identity.

Regression test:

- existing controller/supervisor tests require foreground before injection
  and verify release/cleanup on error;
- a completed physical run must retain foreground until route completion.

Live verification: pending an uncontaminated rerun.

Status: observed external contamination; controller fail-closed behavior
accepted, run excluded from V4 and survival acceptance.

## CE-0172: Compact dictionary projection exceeded physical line, emit, and survival gates

Date: 2026-07-29

Observed symptom:

- Completed schema-v7 Stage-5 run `20260729_113124` independently replayed
  all 5,124 requests with zero unknown and passed semantics, cache,
  preparation, component-compute, cadence, hard no-Bomb, completion, and
  cleanup gates.
- Physical batch-line p95/max was `24142/25296` bytes; the maximum exceeded
  the fixed 24,576-byte limit.
- Previous synchronous emit p95 was `1.366 ms`, above the fixed 1.000-ms
  limit.
- The run took 14 hits: nonspell/103/107/111/115 = `8/2/3/1/0`.

Invalid assumption:

- The isolated retained-trace maximum of 23,495 bytes upper-bounded fresh
  physical usable-record cardinality and JSON size.
- Removing null rows and repeated result dictionaries was sufficient while
  retaining repeated keys in each usable-record and request dictionary.
- One prior 11-hit run implied that the unchanged Stage-5 controller was
  already inside the user-set `<=10` survival boundary.

Evidence or trace:

- code checkpoint `a31f74d`;
- raw trace SHA-256
  `9385f9dcc9859e44fc9256df3bac811aec1973d38e86e63605f951290bb4fe8c`;
- strict failed report SHA-256
  `2d5ebcf862cb5ce9fc3af1b3c858ae95c7444727deaa959d188f94ed7d33ed29`;
- result note
  `notes/research/g5/G5_AUXILIARY_LITERAL_FIRE_CYCLE_RUNTIME_DELIVERY_V4_STAGE5_FAILURE_20260729.md`.

Root-cause class: control/runtime evidence representation and delivery;
survival remains a separate planner/workload failure.

Correction:

- retain V4 as exact physical semantic evidence but reject delivery and
  survival authority;
- fix a V5 columnar record/request transport contract before implementation,
  retaining every raw byte and proof field while removing repeated JSON keys;
- keep all physical size/timing thresholds unchanged; and
- address nonspell survival only through a separately versioned,
  survival-filtered combat intervention after delivery is corrected.

Regression test:

- strict V5 decoding rejects column, order, arity, source-index, status,
  raw-bundle, commitment, epoch, and program-identity tampering;
- Linux/Windows retained replay must fit the fixed line and serialization
  limits;
- V1 through V4 retained reports remain immutable; and
- fresh physical Stage-5 delivery plus two consecutive `<=10`-hit results
  remain required.

Live verification: V4 failed as observed. V5 and the independent survival
correction are pending.

Status: observed physical counterexample; semantic replay accepted,
delivery/survival rejected.

## CE-0173: Columnar rows remained a separate synchronous publication

Date: 2026-07-29

Observed symptom:

- Completed schema-v8 Stage-5 run `20260729_120859` independently replayed
  all 4,238 requests with zero unknown and passed schema, program identity,
  preparation, cache, oracle, line-size, event-derive, total, cadence,
  no-Bomb, completion, and cleanup gates.
- Physical line p95/max was only `14899/15453` bytes, but previous separate
  synchronous emit p95 was `1.412 ms`, above the fixed 1.000-ms limit.
  Replay-compact p95/max was `0.507/8.452 ms`, just/strongly above its fixed
  limits.
- The run took 14 hits: nonspell/103/107/111/115 = `5/1/5/2/1`.

Invalid assumption:

- Reducing one separately emitted UNC trace row below 16 KiB would remove its
  fixed write cost.
- Continued field-level JSON compaction was necessarily the correct next
  delivery optimization.
- One earlier ten-hit observation established survival stability for an
  action-neutral observer.

Evidence or trace:

- code checkpoint `35d3163`;
- raw trace SHA-256
  `4f2505ff84753e07fa5bf827dc71f770c8190e4aafc01e536819d87b23d50088`;
- strict failed report SHA-256
  `1c3018c4831063bd645594856c7e5dc3384e4fd50a54f35b06f5da764d922d6e`;
- result note
  `notes/research/g5/G5_AUXILIARY_LITERAL_FIRE_CYCLE_RUNTIME_DELIVERY_V5_STAGE5_FAILURE_20260729.md`.

Root-cause class: control/runtime evidence publication; survival remains a
separate planner/workload failure.

Correction:

- retain schema-v8 as exact physical trace-only semantic evidence but reject
  its separate synchronous delivery and survival authority;
- retain the implemented V6 versioned coalesced envelope that independently
  restores the exact canonical V5 row inside the already-required
  same-iteration decision publication, with
  hash/size/frame/epoch/snapshot/stage/order validation, and require its
  fresh physical combined-publication gate;
- measure pack time plus combined publication and cadence rather than
  weakening or relabeling V5's failed emit gate; and
- address survival through a separately versioned intervention after
  delivery is side-effect free.

Regression test:

- exact inner V5 replay must survive pack/unpack on Linux and Windows;
- corruption, truncation, substitution, wrong frame/epoch/version/order, and
  stale/deferred attachment must fail closed;
- combined decision publication and cadence must fit fixed new limits without
  a second auxiliary write; and
- fresh physical Stage-5 delivery plus two consecutive `<=10`-hit results
  remain required.

Live verification: V5 failed as observed. V6 isolated Linux/Windows preflight
passes, and physical run `20260729_125453` proves exact coalesced semantics
and zero standalone writes but fails compact tails, survival, and the
non-control-equivalent comparator retained as CE-0174.

Status: observed physical counterexample; V5 semantics/line size accepted,
separate publication/survival rejected.

## CE-0174: Coalesced delivery used a non-control-equivalent emit baseline

Date: 2026-07-29

Observed symptom:

- Completed V6 Stage-5 run `20260729_125453` independently decoded 186
  ordered envelopes and replayed 5,250/5,250 requests with zero unknown.
  There were zero standalone auxiliary-batch writes, and pack, combined
  transaction, all-decision emit, cadence, no-Bomb, completion, and cleanup
  gates passed.
- Replay compact p95/max was `0.535/8.388 ms`, above the unchanged
  `0.500/3.000 ms` limits.
- The strict report also marked envelope-bearing decision emit as regressed,
  but compared spell-107-only bearing rows to all decisions from the retained
  baseline. Those populations are not phase- or workload-equivalent.
- The run took 18 hits: nonspell/103/107/111/115 = `6/2/4/2/4`. Its canonical
  first hit was frame 731 nonspell, before any selected auxiliary envelope.

Invalid assumption:

- An all-stage decision-emit distribution was a valid counterfactual for
  dense spell-107 envelope-bearing decisions.
- Passing isolated compaction bounded live scheduler/allocation tails.
- One aggregate hit count from an uncontrolled game history could identify a
  trace-only code regression.

Evidence or trace:

- physical code checkpoint `3f02ff1`;
- raw trace SHA-256
  `bee834257bc577299d1ca684383bcc8c0591e3fcc02a8b0e43bf39932fc2a2ca`;
- strict failed report SHA-256
  `bd64f9d8d88af6d907d316c0c46b743667cbc5e2c1fc6c480e281144c23a25cd`;
- result note
  `notes/research/g5/G5_AUXILIARY_LITERAL_FIRE_CYCLE_COALESCED_DELIVERY_V6_STAGE5_FAILURE_20260729.md`;
- regression audit
  `notes/research/g5/STAGE5_EIGHT_HIT_CHECKPOINT_REGRESSION_AUDIT_20260729.md`.

Root-cause class: control/runtime evidence comparison and compact-tail
attribution; survival remains a separate sensing/planner/workload failure.

Correction:

- preserve the V6 report as failed and preserve its exact semantic and
  no-second-write sub-evidence;
- fix a new report/contract using a phase- and workload-matched publication
  comparator before making a delivery-regression claim;
- attribute or reduce compact tails without weakening V1–V6 historical
  limits; and
- establish survival first with current code and all optional observers off,
  then add one independently contracted observer at a time.

Regression test:

- report construction must reject an all-stage comparator for a
  phase-filtered bearing population;
- matched V5 batch-parent and V6 bearing populations must retain explicit
  workload identity;
- old V6 report regeneration remains byte-identical; and
- two consecutive fresh Stage-5 controls at `<=10` hits remain required
  before Stage 3, other-stage, and full-Lunatic acceptance.

Live verification: V6 failed as observed. Its exact coalesced semantics and
one-write composition are retained, but full delivery and survival authority
are rejected.

Status: observed physical counterexample; comparator correction, compact-tail
attribution, and survival recovery pending.

## CE-0175: Solver timer and motion transitions omit native scaled time

Date: 2026-07-29

Observed symptom:

- The shipped ECL timer keeps an elapsed integer and a fractional float32
  component. Its scaled path adds `g_gameplay_time_scale` to the fraction and
  carries into elapsed; opcode `0x05` changes elapsed while preserving the
  fraction.
- The current VM-local product and its alleged independent scalar oracle both
  store one scalar timer and reset it to the branch target. All 108 retained
  opcode-`0x05` fixtures have zero fraction, so their parity does not exercise
  the native distinction.
- Shipped player and laser displacement multiply speed by
  `g_gameplay_time_scale`. Current live player/read-lag/local-beam and laser
  projection advance by unscaled speed.

Invalid assumption:

- One scalar timer is control-equivalent to the shipped elapsed/fraction
  pair.
- Player and laser reachability may be projected with unit time scale across
  every route phase.
- Product/oracle parity is independent physical validation when both share
  the same reduced timer representation.

Evidence or trace:

- connected IDA paths `sub_447421`, `0x004186F1`,
  `0x0044BA6A..0x0044BAE6`, `0x00431BC9`, and `0x004320ED`;
- current source paths and reproductions in
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`,
  findings F-007, F-010, and F-013;
- retained Phase-B1 fixture
  `artifacts/ecl_reports/stage4a_vm_local_op05_cases_20260728_110438.json`.

Root-cause class: world model and transition semantics.

Correction:

- represent native elapsed/fraction time and float32 carry explicitly
  (**implemented and offline-verified 2026-07-29**);
- use a structurally independent transition oracle with nonzero fractions
  (**implemented and matched to Linux/Windows native probes**);
- include versioned time scale in player and laser transitions
  (**one-frame and exact supplied-schedule primitives implemented and
  offline-verified**);
- fail closed or branch conservatively when future scale changes are not
  observed (**phase schedule rejects uncovered horizons; one exact
  spell-190 live schedule is now physically delivered, while stage-wide
  source coverage remains open**).

Regression test:

- bitwise product/oracle/native-probe agreement for nonzero fraction, carry,
  opcode-`0x05` preservation, unit/non-unit scale, stop/resume, and laser
  phase transitions;
- model identity must reject a time-scale or timer-layout mismatch;
- old zero-fraction fixtures remain immutable and explicitly scope-labeled.

Partial correction evidence:

- `scripts/th08_native_timer.py` and the independent raw-bit oracle/native
  x87 probes agree on 17/17 component-transition cases per platform;
- product, VM-local shadow, existing velocity lookahead, and trace-only birth
  lookahead now preserve separate elapsed/fraction identity and opcode
  `0x04`/taken-`0x05` fraction;
- the new versioned Stage-4A replay retains all 3,117 in-scope rows, 108
  unique cases, zero mismatches, and zero new completion; and
- exact recurrence, artifacts, hashes, IDA changes, and limitations are in
  `notes/architecture/TH08_NATIVE_ECL_TIMER_SEMANTICS_20260729.md`;
- phase-specific player/laser scale product, independent raw oracle, and
  Linux/Windows explicit-x87 probes agree on 17/17 deterministic cases and
  6,144 seeded product/oracle cases with zero mismatch; and
- exact scale recurrence, retained report hashes, IDA changes, and live
  limitations are in
  `notes/architecture/TH08_PLAYER_LASER_GLOBAL_TIME_SCALE_SEMANTICS_20260729.md`.

Live verification: the corrected timer consumer completed its fixed
observer-off Stage-5 falsifier at 12 hits, but the first six hits precede that
consumer and every consumer row is zero-fraction/unit-scale. The survival
threshold failed without a timer-causal rollback target. Player/laser scale
now has retained Final/Extra capsules plus one physically passed
source-local spell-190 live delivery interval. Stage-wide future-source
coverage, clean non-unit survival, and general physical authority remain
open.

Status: timer and player/laser primitive sub-counterexamples corrected
offline; timer physical survival gate failed; Final-B exact scale delivery
physically passed in a contaminated 22-hit stage; general physical
verification pending.

## CE-0176: Focus history changes enemy contact and damage eligibility

Date: 2026-07-29

Observed symptom:

- Shipped code writes enemy flag `0x800` from the player's
  secondary-character state and skips the corresponding enemy-manager
  contact/damage block while that flag is set.
- A retained runtime transition changes from unfocused at frame 10065 to 16
  contact-eligible bodies at frame 10075.
- Current `decode_enemy_bodies` filters `flags & 0x830` before future
  action-conditioned projection, even when its compatibility option asks to
  include contact-disabled bodies.

Invalid assumption:

- The current visible enemy-body set is action-independent over the planning
  horizon.
- Focus/unfocus is merely a movement-speed choice.
- Enemy contact and damageability may share one static eligibility label.

Evidence or trace:

- connected IDA `sub_42C420`, enemy-manager flag test, and player focus
  transition at `0x0044B1D9`/`0x0044B42C`;
- retained frame `10065 -> 10075` evidence and current-source trace in
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`,
  findings F-009 and F-011.

Root-cause class: world model and action-conditioned sensing.

Correction:

- retain player focus/secondary-character transition state;
- project contact and damage eligibility per action/history;
- merge only observation-compatible branches;
- keep unfocused combat/Power promotion disabled until the transition and its
  RNG effects are modeled.

Regression test:

- native/source parity for focus-entry/exit delay and adversarial toggles;
- body sets and damage eligibility agree at the retained frame transition;
- every root action is evaluated against its own causal eligibility history.

2026-07-30 partial correction:

- `include_contact_disabled=True` now retains active bit-`0x800` geometry
  instead of permanently dropping it at decode;
- the pure `th08_enemy_mode` projection separates bit-`0x04` contact from
  bit-`0x40` player-shot damage and applies the bit-`0x100 -> 0x800` sync;
- native player `+3/+5/+8` are exposed in diagnostic state/decision traces;
- exhaustive 9-step focus histories from three adversarial initial states
  match an independent scalar recurrence; a CE-0176 fixture opens the
  16-body gate after the seventh steady release update.

Live verification: pending. The new mode fields are not yet captured in one
frame-bracketed player/enemy transaction, the projection is not yet part of
the live hazard recurrence, and no retained hit is attributed to it.

Status: latent-geometry decoder loss corrected offline; causal
action-conditioned recurrence and physical contribution remain open.

## CE-0177: Solver collision geometry and offline clamp do not match TH08

Date: 2026-07-29

Observed symptom:

- Shipped SHT hitbox width is divided by two before storage; the active lethal
  player half-extent is `1.0`, while current live movement uses
  `PLAYER_RADIUS = 2.0`.
- Shipped laser collision transforms the player into laser-local coordinates
  and tests a rotated local AABB; the solver uses a capsule approximation.
- The offline movement model permits `x=0..384`, `y=0..448`; shipped/live
  physical clamp is `x=8..376`, `y=16..432`.

Invalid assumption:

- A conservative physical radius, lattice error, and route margin may be
  collapsed into one undocumented `2.0` radius without changing viable-space
  interpretation.
- Capsule and rotated-rectangle laser collision are exactly equivalent.
- Offline edge positions are attainable physical roots.

Evidence or trace:

- connected IDA SHT load/divide/store and laser collision paths;
- current `scripts/th08_live/movement.py` and movement-model bounds;
- findings F-012, F-014, and F-016 in
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`.

Root-cause class: world-model collision geometry.

Correction:

- represent the exact physical hitbox separately from explicit
  lattice/sampling clearance;
- implement native rotated-local-AABB laser collision behind an independent
  scalar oracle;
- use the physical clamp in every offline/live recurrence.

Regression test:

- viable-state and safe-action-mask parity for boundary, rotation,
  head/tail, nearest-lattice, and retained capsule cases;
- all expected deltas are classified by direction;
- no offline witness starts from an unreachable edge root.

Live verification: pending. The player-radius error is conservative; the
global direction of the laser approximation is not yet certified; the
offline clamp can be optimistic.

Status: observed implementation counterexample; correction pending.

## CE-0178: Bullet lethality and collision suppression are not state-complete

Date: 2026-07-29

Observed symptom:

- The solver treats every nonzero bullet state as active/lethal, but shipped
  fade state 5 is nonlethal.
- Shipped collision checks skip a bullet while its `+0x10B4` suppression flag
  is set. Callback code toggles that flag and changes velocity.
- Capture decodes callback auxiliary state, but current collision projection
  does not carry a per-frame suppression schedule.

Invalid assumption:

- Pool occupancy is equivalent to lethality.
- A current auxiliary-state observation can be ignored after velocity
  lowering without changing collision availability.

Evidence or trace:

- connected IDA collision test at `0x004315A1` and callback paths
  `0x00424ADE`/`0x00424B59`;
- public/current-source reproductions in findings F-015 and F-019 of
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`.

Root-cause class: world-model hazard lifecycle.

Correction:

- model explicit bullet lifecycle/lethal states;
- carry callback-driven collision suppression and velocity changes over the
  same versioned frame schedule;
- keep an unknown callback transition unavailable to hard coverage.

Regression test:

- scalar/Python/native parity over fade, suppression on/off, velocity scale,
  stop/resume, and transition-boundary cases;
- retained safe-action and viable-state diffs must be conservative or fully
  explained.

Live verification: pending. Current omissions are conservative in the
observed cases and can cause false viability exhaustion.

Status: observed implementation counterexample; correction pending.

## CE-0179: Future births alias action-dependent RNG and omitted callbacks

Date: 2026-07-29

Observed symptom:

- Sakuya/Remilia focus/shot behavior reaches an option-shot callback that
  consumes the shared gameplay RNG.
- The current future-hazard model does not carry action-dependent RNG state.
- The callback catalog omits custom/global/lethal effects and cannot prove
  all future event sources complete.

Invalid assumption:

- Future bullet births are independent of focus/shot actions once current
  enemies and VM state agree.
- Uncatalogued callbacks can be ignored because one retained sample emitted
  no supported event.

Evidence or trace:

- shipped player-shot/callback callers and RNG dataflow revalidated in the
  connected IDA database;
- findings F-008, F-017, V-002, and V-003 in
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`;
- existing G5 source-coverage results retain unsupported suffixes as
  `UNKNOWN`.

Root-cause class: world model, action-conditioned uncertainty, and future
source coverage.

Correction:

- capture and version the relevant RNG/callback/source state;
- evolve RNG once per causal action branch with observation-compatible
  merging;
- inventory every callback family and fail closed on unknown effects;
- do not promote unfocused/damage targeting until the source model is
  complete for its declared workload.

Regression test:

- two otherwise equal focus/shot histories that consume different RNG must
  not share one model state;
- independent callback/RNG oracle parity over retained and adversarial
  sequences;
- unknown callback effects cannot become a complete hazard schedule.

Live verification: pending. The native mechanism and model omission are
observed; the size of its survival effect is unknown.

Status: observed semantic counterexample; complete source correction pending.

## CE-0180: ECL auxiliary replacement and bounded-call semantics are reduced

Date: 2026-07-29

Observed symptom:

- Shipped opcode `0x87` replaces an auxiliary VM slot, evaluates its target
  through native semantics, and is not an interrupt. Current analysis/model
  terminology and helper behavior do not preserve all of those distinctions.
- At native call depth 15, TH08 drops the current caller state rather than
  providing an unbounded or conventional overflow stack.
- The current IDA database has no typed ECL/enemy/player structure catalog to
  constrain accidental field or calling-convention assumptions.

Invalid assumption:

- Auxiliary VM replacement is equivalent to interrupt delivery.
- Calls can be modeled as an ordinary unbounded stack.
- Pseudocode names/types supply structure authority.

Evidence or trace:

- shipped opcode and call/return instruction/dataflow;
- findings F-004, F-005, and F-006 in
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`;
- IDA local-type list contains only `_SCOPETABLE_ENTRY`.

Root-cause class: world-model ECL control semantics and reverse-engineering
authority.

Correction:

- model exact replace/slot/single-evaluation semantics;
- model the bounded call stack and fail closed before unsupported overflow;
- introduce reviewed IDA structures/types incrementally with caller/callee
  and runtime validation.

Regression test:

- replacement versus interrupt, slot boundary, target-evaluation side
  effect, depth 14/15/16, and return-state cases;
- unknown or overflow paths cannot publish complete future coverage.

Live verification: pending. Shipped content observed during this audit stays
inside the currently seen auxiliary-slot bound, but the general model remains
incomplete.

Status: observed semantic counterexample; correction pending.

## CE-0181: Native observer and beam arithmetic do not fail closed at extremes

Date: 2026-07-29

Observed symptom:

- The native derived-pattern observer increments an `int32` cursor before
  validating the next value. A deterministic `INT_MAX` reproduction exits
  with child `SIGSEGV`/status 139.
- Native local-beam quantization rounds an arbitrary finite ratio and casts it
  to `int64` without representability validation. Extreme finite inputs
  merge/drop one of two states that remain distinct under Python
  arbitrary-precision keys.

Invalid assumption:

- “Finite” implies safe integer conversion.
- Signed cursor increment can be validated after arithmetic.
- Which state survives undefined behavior is stable enough to use as a
  regression expectation.

Evidence or trace:

- deterministic current-build reproductions and source paths in findings
  F-018 and F-020 of
  `notes/review/TH08_NATIVE_TO_SOLVER_READ_ONLY_AUDIT_20260729.md`.

Root-cause class: control/runtime reliability and native planner arithmetic.

Correction:

- validate cursor bounds before increment and return an explicit fail-closed
  status;
- validate quantized-key representability before conversion;
- define fallback/no-dedup behavior for out-of-domain finite coordinates;
- use owned error propagation across the Python/native boundary.

Regression test:

- `INT_MIN`, `INT_MAX`, next-cursor, non-finite, extreme-finite,
  half-even-boundary, and normal-live-domain cases under sanitizers;
- native and Python key partitions agree whenever the native path returns
  success;
- no test asserts the unstable retained index of an undefined-behavior run.

Live verification: pending. Normal live coordinates are currently clamped,
but the observer crash is independently reachable through its public native
boundary.

Status: observed deterministic implementation counterexample; correction
pending.

## CE-0182: Observer-off Stage-5 control reaches an enemy body after viability exhaustion

Date: 2026-07-29

Observed symptom:

- Accepted observer-off Lunatic Stage-5 control
  `lunatic_route2_stage5_unattended_20260729_154229` completed frames
  `2..42335`, 11,710 decisions, hard no-Bomb, route completion, retained
  artifacts, and cleanup. It took ten native hits, so it is pass 1 of the
  two-consecutive-run baseline gate, not an NMNB success.
- The canonical fresh hit at frame 10,740 occurred in nonspell at player
  `(349.070,383.773)`, active `up_fast`, Power 128, 883 bullets, and zero
  lasers. The exact same-epoch contact candidate is enemy body
  `0x005A7170` at `(342.340,384.138)`, half-extents `(18,18)`, flags
  `0x0100114D`, and signed AABB clearance `-12.270`.
- That body was present in the causal, hit-decision, and action snapshots.
  Global viability was exhausted at frame 10,731; the robust action set was
  empty at frame 10,738; robust and pipeline warnings led contact by two
  frames; pipeline clearance at contact was `-13.264`.
- Every one of the ten contacts followed global viability exhaustion. Causes
  split into four modeled committed-prefix collisions, four observed-bullet
  overlaps, one observed-enemy-body overlap, and one sensor/unmodeled case.

Invalid assumption:

- Returning to the eight-to-ten aggregate band once proves survival recovery
  or closes the two-consecutive-run baseline gate.
- One unmatched observer-off sample proves that optional observer work caused
  the earlier 18-hit sample.
- This body contact proves CE-0176's focus-conditioned body-set omission
  caused the hit. The slot was present in every relevant snapshot and its
  observed flags did not make it contact-disabled.

Evidence or trace:

- compact artifacts and run note for
  `lunatic_route2_stage5_unattended_20260729_154229`;
- ignored 477,619,552-byte raw JSONL SHA-256
  `fef21b64bfa6c906cb3dcd73b5c08f5ea1fdd00f6ea8e67d3a8466519c533e72`.

Root-cause class: physical viability preservation and world-model/planner
boundary.

Correction:

- count this only as observer-off Stage-5 pass 1 of 2 and repeat the identical
  control before any source or strategy change;
- preserve the first-hit body geometry and determine why no causal action
  history retained viability before contact;
- keep CE-0176/`SEM-MODE` correction required on independent native evidence,
  but do not attribute this hit to that omission without a causal replay or
  bounded physical test;
- do not infer rollback or observer causality from aggregate counts across
  different RNG histories.

Regression test:

- an unchanged second observer-off Stage-5 control must also complete at
  `<=10` hits to close the first gate;
- replay the retained first-hit state with exact body geometry, action/delay
  history, and an independent collision oracle;
- future corrected-model trials must preserve a non-empty causal viable set
  before this window or retain the first newly falsifying state.

Live verification: failed as a consecutive recovery gate. The unchanged
follow-up `20260729_161313` took 18 hits; CE-0183 retains its first causal
failure. The isolated threshold pass remains observed, but no stable baseline,
causal model correction, or NMNB survival claim follows from it.

Status: observed physical counterexample; isolated pass retained,
consecutive recovery rejected.

## CE-0183: Identical observer-off Stage-5 pass 2 returned to 18 hits

Date: 2026-07-29

Observed symptom:

- Accepted observer-off Lunatic Stage-5 control
  `lunatic_route2_stage5_unattended_20260729_161313` completed frames
  `2..45288`, 12,639 decisions, hard no-Bomb, route completion, retained
  artifacts, and cleanup. It took 18 native hits after the immediately
  preceding unchanged control took ten, so the two-consecutive-run gate
  failed and reset.
- Executable checkpoint `e4e266f`, physical-code checkpoint `3f02ff1`, target
  SHA-256, runtime patch, controller config, difficulty, stage, native
  backends, hard no-Bomb policy, and every optional observer/shadow flag are
  identical across the two controls. Repository changes between launches
  contain only the first run's compact evidence and documentation. Both
  started at Power 128, lives 8, and Bomb stock 3.
- Phase hits were nonspell/103/107/111/115 = `10/2/5/1/0`. The canonical
  fresh hit at frame 2,524 occurred in nonspell at player
  `(371.121,82.873)`, active `down_left`, Power 128, 267 bullets, and zero
  lasers. It is a modeled committed-prefix collision with pipeline clearance
  `-1.808`.
- Global viability was first exhausted at frame 2,284, 240 frames before
  contact. The robust action set was empty at frame 2,516, eight frames
  before contact. At the final alive decision, current pipeline clearance was
  still `+0.539`, but robust prefix clearance was `-4.027` over delay support
  `2..6`; usable pipeline warning arrived at contact.
- Every one of the 18 contacts followed global viability exhaustion. Causes
  split into 11 modeled committed-prefix collisions, five observed-bullet
  overlaps, one observed-enemy-body overlap, and one sensor/unmodeled case.

Invalid assumption:

- One ten-hit observer-off sample establishes a stable recovered baseline.
- Optional V6 observers or a changed action-authority path are necessary for
  an 18-hit Stage-5 result.
- The eight-hit aggregate delta identifies a rollback target. The two controls
  are workload/config matched but not same-seed, same-history, or
  same-first-hit-state paired trials.

Evidence or trace:

- compact artifacts and run note for
  `lunatic_route2_stage5_unattended_20260729_161313`;
- exact compact comparison against
  `lunatic_route2_stage5_unattended_20260729_154229`;
- ignored 550,969,744-byte raw JSONL SHA-256
  `50e8924ba23da9e703b887d8dfd320a025f044754625b66b2939c858ba357ba5`.

Root-cause class: physical viability preservation, world-model/planner
boundary, and baseline stability.

Correction:

- reset the consecutive Stage-5 sequence and do not advance to Stage 3,
  Stage 4A, Final B, or full-route characterization;
- do not roll back unchanged action-authority code or infer a necessary
  optional-observer cause;
- stop uncontrolled physical repetition and analyze the frame-2,284
  viability-loss root plus frame-2,516 committed-prefix boundary;
- proceed through the roadmap's versioned native-semantic corrections before
  the next physical promotion attempt, retaining the earliest falsifying
  state after each slice.

Regression test:

- replay the retained pre-hit history with exact position, hazards, action,
  held/pending input, delay support, float32 margins, and an independent
  collision/recurrence oracle;
- classify why all causal actions become globally losing 240 frames before
  contact and why the short prefix becomes unsafe only eight frames before
  contact;
- after a versioned correction, take the smallest Stage-5 causal
  falsification and then restart the two-consecutive observer-off gate.

Live verification: observed failure. The current stack does not satisfy the
consecutive Stage-5 baseline gate; the exact semantic or viability correction
needed to prevent the first loss remains unresolved.

Status: observed physical counterexample; `PHYS-BASE-RING` stopped and
correction required.

## CE-0184: SEM-TIMER Stage-5 falsifier exceeds the survival threshold before its live consumer appears

Date: 2026-07-29

Observed symptom:

- Accepted observer-off Lunatic Stage-5 run
  `lunatic_route2_stage5_unattended_20260729_173957`, executed with physical
  code checkpoint `e309c81`, completed frames `1..42021`, 11,597 decisions,
  route completion, exact cleanup, and hard no-Bomb. It took 12 native hits,
  so it fails the preregistered `<=10` SEM-TIMER physical threshold.
- Phase hits were nonspell/103/107/111/115 = `7/2/2/0/1`. The canonical
  fresh hit at frame 2,069 occurred in nonspell at player `(376,424)`,
  active input `up`, Power 128, 663 bullets, and zero lasers. It is a modeled
  committed-prefix collision with pipeline clearance `-2.222`.
- Global viability was first exhausted at frame 1,829, 240 frames before
  contact. The robust action set was empty at frame 2,063, six frames before
  contact; usable pipeline warning led by only two frames. All 12 contacts
  followed global viability exhaustion.
- The corrected timer-aware live consumer first appears only at frame 21,751.
  The canonical hit and the first six hits precede it. Across its 3,940 rows,
  every timer fraction is zero and every time scale is `1.0`; 2,794 rows
  publish complete schedules and 1,146 stop explicitly on unsupported control
  flow, with no lookahead error.

Invalid assumption:

- Passing the native/offline timer differential gate guarantees the fixed
  Stage-5 survival threshold.
- A failed aggregate physical threshold identifies the changed timer
  primitive as causal even when the canonical failure and six contacts occur
  before its live consumer exists.
- Runtime ECL PCs may be normalized with a base borrowed from another stage.
  A preliminary manual check using Stage-4A base `0x0B1D1430` falsely
  suggested past-time roots. Fresh inference selects Stage-5 base
  `0x0B1D0048`, mapping 33/33 unique roots; decoded relations are 3,835
  future, 105 equal, zero past, and zero unmapped.

Evidence or trace:

- compact artifacts and run note for
  `lunatic_route2_stage5_unattended_20260729_173957`;
- ignored 477,436,227-byte, 11,691-line raw JSONL SHA-256
  `e0b32ee0e042056079d7159f1055aa2ad289447d03efc078d2c2d73f98585367`;
- connected-IDB revalidation of equality helper `0x0040D3F0` and its
  `enemy_ecl_vm_step` call/false-exit path at `0x004185AF`;
- fresh affine inference against decoded `ecldata5.ecl` SHA-256
  `3148f45faf78bd8211a956edcdc353be73d2781995d3dadd36bdca8132f8fe19`.

Root-cause class: physical viability preservation and analysis/runtime-address
identity.

Correction:

- retain this as a failed physical survival gate and stop Stage-5 repetition
  and Stage-3/4A/Final-B expansion for this immutable version;
- do not roll back the component timer on this evidence; preserve the
  frame-1,829 viability-loss and frame-2,063 committed-prefix boundary for
  the next causal model correction;
- infer or capture runtime ECL base per stage/session before interpreting
  instruction headers;
- keep nonzero-fraction and nonunit-scale physical authority open, and
  continue the ordered roadmap at `SEM-SCALE`.

Regression test:

- future physical timer reports must bind stage/session, runtime base, decoded
  image digest, model versions, and component identity before comparing
  instruction time;
- the first physical scene that exercises nonunit scale must retain scale and
  fraction histories and compare native/product transition results;
- a later immutable corrected model may restart the Stage-5 gate only with
  canonical first-hit attribution and the same hard no-Bomb threshold.

Live verification: failed for survival (`12 > 10`) but does not causally
reject SEM-TIMER. The live consumer/version telemetry is integrated on the
unit-scale, zero-fraction slice; nonunit-scale physical validation remains
pending.

Status: observed physical counterexample; no timer rollback justified,
physical expansion stopped, `SEM-SCALE` next.

## CE-0185: Installed ECL callback fields were assigned to the enemy base instead of the selected VM

Date: 2026-07-29

Observed symptom:

- The inherited foundation note described opcode `0x89` state as enemy
  `+0x10/+0x14`.
- Revalidated dataflow at `enemy_ecl_vm_step` `0x0041E852` first loads the
  current VM pointer through enemy scratch field `+0x2CA0`, then reads the
  callback and retained record at that VM `+0x10/+0x14`.
- The main selection is enemy `+0x7F8`, making its actual fields enemy
  `+0x808/+0x80C`. Auxiliary selection changes the current VM to context
  `+0x08`, so each auxiliary VM has independent callback state.

Invalid assumption:

- Installed callback state is an enemy-base field shared by main and
  auxiliary execution.
- Reading enemy base `+0x10/+0x14` proves installed callback absence.

Evidence or trace:

- connected-IDB pseudocode/dataflow at `0x004184D3..0x004184E5`,
  `0x0041E852..0x0041E870`, and `0x0041EBBC..0x0041ECA7`;
- IDA comments at `0x0041E852` and `0x0041EBBC`;
- corrected foundation note and exact-offset regression in
  `test_th08_scale_source_trace.py`.

Root-cause class: world model and sensing layout.

Correction:

- capture main installed callback state from VM `+0x10/+0x14`;
- reject any non-null auxiliary context until its active VM state, including
  its independent installed callback fields, is captured and modeled;
- retain main VM -> auxiliary indices 0..3 -> enemy motion as the native
  scheduler order.

Regression test:

- a fixture places callback 18 at enemy base `+0x10` while leaving main VM
  `+0x10` zero and proves the decoder reports no installed main callback;
- separate fixtures prove main-VM callback and auxiliary-pointer states fail
  closed when scale authority is incomplete;
- complete Linux/Windows discovery passes 1,107 tests.

Live verification: observed in accepted native-replay physical capture
`finalb_scale_source_replay_20260729_215613.json`: one main VM, zero
auxiliary contexts, and installed callback zero are read from the corrected
selected-VM fields under one stable manager frame.

Status: fixed in source/IDA/foundation semantics and physically observed.

## CE-0186: First-spell trigger captured a unit-scale root across a manager-frame boundary

Date: 2026-07-29

Observed symptom:

- The first native-replay C4 attempt triggered on the first observation of
  spell 190 at manager frame 72987, before the phase reached quarter scale.
- The selected source had scale `1.0`, and the 480-slot/full-owner capture
  took 36.834 ms and crossed the manager-frame bracket.
- The strict report therefore failed
  `source_capture:manager_frame_changed`,
  `target_quarter_scale_not_observed`, and completeness checks.

Invalid assumption:

- Spell identity alone selects the scale-source root.
- Allocating and copying a full process-pool buffer inside a one-shot capture
  is harmless to the physical frame bracket.

Evidence or trace:

- `artifacts/runtime_reports/finalb_scale_source_replay_20260729_211715.json`,
  SHA-256
  `4686ee59f655345d9bf22c6b247418dc980e0e2298b0a843835e09488238c36c`;
- strict failed report SHA-256
  `a12635d359af79067c470787624a0b92badadf653c85d63fe09c537545502128`.

Root-cause class: physical trigger and capture transaction.

Correction:

- arm the one-shot only at spell 190, binary32 quarter scale, and Bomb zero;
- make the stable transaction's bracket frame the source root;
- preallocate and reuse the process-reader buffer and avoid copying the
  ordinary pool.

Regression test:

- unit-scale spell-190 polls do not consume the one-shot;
- quarter-scale polls do;
- stable-bracket source identity does not require equality with an earlier
  polling frame;
- Linux and Windows complete discovery pass after the correction.

Live verification: the corrected capture completes at manager frame 74787 in
7.734 ms without crossing the frame.

Status: fixed and physically verified.

## CE-0187: Replay death residue was conflated with a fresh semantic-root hit

Date: 2026-07-29

Observed symptom:

- The second native-replay attempt reached spell 190 and manager frame 74794
  but ended without triggering because the observer required predeath zero.
- Historical physical decision frame 74791 for the same phase has
  `player.phase=3`, `predeath_at_action=7`, no hit edge, no Bomb, and the last
  recorded hit thousands of frames earlier.
- The no-life-decrement patch changes the life update, not every native
  death/predeath residue field.

Invalid assumption:

- A source-semantic trace requires a zero predeath counter.
- Stable nonzero replay/no-life-patch residue is equivalent to a fresh hit at
  the captured scale root.

Evidence or trace:

- `artifacts/runtime_reports/finalb_scale_source_replay_20260729_213600.json`,
  SHA-256
  `53b7f0845e96c717ed289a7863cb8cb796443e2c195abe09ee1873e12677965c`;
- strict failed report SHA-256
  `6ada457ffae19a3c16aaabee5b48d57192d3f06ea55300ab6288def9482b0b25`;
- local retained raw session
  `lunatic_route2_stage6b_unattended_20260726_004142.jsonl`, decision frame
  74791; and
- `player_respawn_or_miss_handler` plus the no-life patch sites
  `0x0044D0FA/0x0044D100`.

Root-cause class: evidence-authority conflation.

Correction:

- require a stable quarter-scale and no-Bomb root for source semantics;
- retain the exact predeath value as contamination;
- state explicitly that this does not prove a clean zero-predeath root,
  survival, or NMNB.

Regression test:

- stable predeath residue is reported but does not block the source-semantic
  capture;
- Bomb or an unstable root still fails closed;
- the report's `not_proved` authority text includes clean zero-predeath,
  survival, and NMNB.

Live verification: accepted capture `20260729_215613` retains predeath 7 and
passes only the scale-source semantic checks.

Status: fixed; clean-player-root and survival evidence remain open.

## CE-0188: Normal-player-phase gating made the physical scale-source window unreachable after a prior hit

Date: 2026-07-29

Observed symptom:

- The original-game Lunatic Stage-6B C5 trial
  `lunatic_route2_stage6b_finalb_scale_delivery_20260729_233720`
  completed frames `2..74080` with 17,282 decisions, 19 native hit edges,
  and zero Bomb decisions or Bomb masks.
- The canonical fresh-attempt hit is frame 8,085 in a nonspell. The last hit
  is frame 73,477 in spell 190. It leaves the player at phase 3 with
  predeath residue 7 before the native root changes from unit to quarter.
- At frame 73,600 the controller observes spell 190 and binary32 quarter
  scale, but publishes `finalb_scale_source_wait` instead of capturing the
  source. The exact quarter-scale interval remains read-only through frame
  74,078. The next decision at frame 74,079 has player phase 0 and the native
  root has already restored to unit; the route unloads at frame 74,080.
- No new replay was created by this Practice Start run. The newest native
  replay file still predates the trial, so replay re-execution is unavailable
  for this witness.

Invalid assumption:

- Stable nonzero predeath residue may be admitted as contamination while
  requiring player phase 0 at the exact source root.
- A death-recovery phase necessarily ends before the native quarter-scale
  source window closes.
- A physical evidence run should terminate on a later source/root/horizon
  mismatch even when continuing without new writes would preserve stage and
  route evidence.

Evidence or trace:

- ignored 579,579,062-byte raw JSONL SHA-256
  `11ba10fb1ac771e138627cff0e6faf7855e0858c5cf70b88c0fa2c9966e52b8a`;
- retained strict C5 report, complete Stage-6B dossier, death ledger, session,
  summary, regressions, and run note for the run above;
- frame 73,596 decision: spell 190 active, player phase 3, predeath 7;
- frame 73,600 first quarter-scale `finalb_scale_source_wait`; and
- frame 74,079 decision: player phase 0, spell inactive, unit root.

Root-cause class: physical source-trigger/evidence-authority conflation.

Correction:

- capture and retain `player_phase` inside the same complete-source
  transaction;
- permit a coherent Bomb-zero quarter-scale source when the captured player
  phase and predeath value agree with the controller root, while labeling a
  nonzero phase as contaminated and granting no normal-player-phase survival
  authority;
- in the explicit C5 physical experiment, incomplete scale authority waits
  without a new input write and continues collecting evidence instead of
  terminating the game;
- keep focused Stage-6B restore auto-stop explicit, but disable it for the
  original Game Start full-route gate; and
- consume both native `summary` and legacy `run_summary` records in the
  strict report.

Regression test:

- phase-3/predeath-7 source and controller roots bind one exact schedule and
  retain `source_player_phase=3`;
- phase mismatch inside the source transaction still rejects capture;
- native `kind=summary` is consumed by the strict report;
- focused auto-stop accepts `finalb_scale_delivery_complete`, while a
  complete-route scope accepts `route_complete`; and
- Final-B authority can be carried from expected stage 0 for a complete
  Lunatic Game Start route or from stage 7 for the focused gate.

Live verification: complete for the corrected source trigger and exact
delivery scope. Full-route run
`lunatic_route2_fullrun_unattended_20260730_002115` physically exercises the
non-aborting continuation through route completion with zero Bomb masks, but
its failed route history never reaches spell 190. Whole-stage C5-2 run
`lunatic_route2_stage6b_finalb_scale_delivery_20260730_020015` later captures
phase-3/predeath-7 spell-190 source state and delivers its exact schedule
through the terminal restore with zero exact-scope hit/Bomb. The surrounding
stage still takes 22 hits and grants no survival authority.

Status: observed physical counterexample; corrected and physically
revalidated for exact delivery, not clean survival.

## CE-0189: A completed failed full route did not reach the C5 target or distinguish absent spells from clean spells

Date: 2026-07-30

Observed symptom:

- Original Game Start run
  `lunatic_route2_fullrun_unattended_20260730_002115` completed Lunatic
  Sakuya/Remilia from Power 0 through Final B with 60,877 decisions, 74 hit
  edges, zero Bomb masks, no foreground interruption, and normal
  `route_complete` cleanup.
- Stage hit counts were `3/4/6/21/17/23` for
  Stage 1/2/3/4A/5/Final B. The first canonical hit was Stage-1 nonspell frame
  2,022.
- The run physically entered Final-B spell 174 for 299 decisions and spell
  178 for 311 decisions, but entered spells 182, 186, and 190 for zero
  decisions. Every sampled root scale remained binary32 unit.
- The C5 observer therefore had no due quarter-scale source. The strict report
  has zero authority rows and correctly fails, while the controller continues
  through route completion.
- The old dossier table rendered both an observed zero-hit spell and an absent
  spell as `Runtime 0`, obscuring this causal distinction.
- No new native replay was created.

Invalid assumption:

- Any completed full Final-B route necessarily reaches statically reachable
  spell 190 and therefore exercises the C5 source gate.
- A zero per-spell hit count is sufficient evidence that the spell was
  physically entered and survived.
- A failed route's patched post-hit resource/time continuation is
  control-equivalent to the intended NMNB path for late-spell reachability.

Evidence or trace:

- ignored 2,009,399,974-byte raw JSONL SHA-256
  `ffe52f97e959a92ec0adb06e418c17e2a97c8e2209f0978a1249f1b66e8a69d0`;
- retained strict C5 report SHA-256
  `47337406a0aaa4ff6a819b2614f94907613b951d44ae058bf3472634517fe7d8`;
- retained dossier, comparison, summary, session, 74-row death ledger,
  regressions, and run note; and
- exact top-level live spell-decision counts for 174/178/182/186/190:
  `299/311/0/0/0`.

Root-cause class: physical gate coverage plus objective/resource history;
the exact native late-spell branch condition remains unknown.

Correction:

- retain exact active-spell decision count, first frame, last frame, and
  observed/absent status separately from per-spell hits in dossier schema v4;
- keep C5 exact delivery and full-route survival/resource conclusions
  separate;
- use the original-game complete Stage-6B workload for the corrected C5
  falsifier, without THPRAC, precise-spell selection, or operator activation;
- disable restore auto-stop so the physical research unit remains the whole
  stage; and
- continue evidence collection without new input writes on a C5 mismatch.

Regression test:

- an observed active spell with two decisions and zero hits is reported as
  observed, while a statically reachable absent spell is reported with zero
  decisions and `not_observed_in_trace`;
- dossier v4 is accepted by the full-route artifact materializer;
- the strict C5 report accepts `route_complete` when delivery auto-stop is
  false; and
- practice-supervisor tests retain whole-stage no-auto-stop configuration.

Live verification: the full-route non-aborting continuation and target
absence are observed. The subsequent complete Stage-6B C5-2 trial reaches
every late spell through 190, retains whole-stage evidence, and passes the
nested exact-delivery gate without auto-stop.

Status: observed physical counterexample; report ambiguity fixed offline;
C5 physical revalidation complete.

## CE-0190: Strict C5 v3 required unsampleable origin and active restore rows

Date: 2026-07-30

Observed symptom:

- Whole-stage run
  `lunatic_route2_stage6b_finalb_scale_delivery_20260730_020015` completed
  Stage 6B normally with 18,332 decisions, 22 hits, zero Bomb masks, and exact
  cleanup.
- One coherent spell-190 source transaction captured manager frame 75,811.
  The controller decision and expected source frame were 75,810, so the
  transaction completed one native manager update later.
- Causal authority therefore begins at sampled offset 1. It cannot publish an
  exact offset-zero row retroactively.
- 111 exact decisions consume quarter-scale roots through offset 238 with
  zero fallback, fresh hit, or Bomb. The accepted schedule writes unit scale
  at offset 239.
- Controller cadence does not sample offset 239. At offset 240 the native
  root is unit, spell context has ended, and a same-frame
  `scene_inactive/status=terminal_unload` record closes the stage.
- Strict report v3 rejected this trace because it required both an exact
  sampled offset-zero authority row and an active exact unit-scale restore
  row.

Invalid assumption:

- An asynchronous coherent source transaction must be observable as exact
  authority at the controller decision that triggered it.
- Physical restore evidence is valid only while the exact spell context is
  still active.
- A fixed legacy restore offset of 240 is authoritative across captures,
  rather than the callback-18 write and player/laser schedules retained by
  that exact source.

Evidence or trace:

- ignored raw JSONL SHA-256
  `cbad986f0bb627d88135e2a4ae31c48389b6e030657ad77e557b882585aedcfc`;
- original Windows-CRLF rejected v3 render SHA-256
  `b1aa5a0c27082303a19c7d90f7aba42661006c5892e351b68c3e0a80f65aa2b5`
  and tracked LF-normalized v3 artifact SHA-256
  `9d3652466596f3a49cd67d0ff317f31685c92d23a6640bdf5ed9d47bd9d5dca7`;
- retained corrected v4 report SHA-256
  `53cddd1162769010dbc467bf9d295e90e5389db3ffb4fce3d1c73c42076b08ec`;
- source manager frame 75,811, first authority offset 1, last sampled quarter
  offset 238, scheduled restore offset 239, and terminal unit-root observation
  offset 240; and
- same-frame `root_only_context_mismatch` plus
  `scene_inactive/status=terminal_unload` at source frame 76,051.

Root-cause class: sensing/report temporal contract.

Correction:

- derive the immutable origin from the accepted complete source schedule and
  permit only a coherent zero- or one-manager-frame capture lag;
- forbid negative offsets and require sampled authority to remain
  nonretroactive;
- derive the restore offset from the accepted callback-18/player/laser
  schedule instead of hard-coding 240;
- accept either an active exact unit-root row or a same-frame terminal-unload
  unit-root bracket at the restore or following manager frame;
- retain the origin, offset, baseline, and source phase on context mismatch
  so terminal evidence remains auditable without granting planner authority;
  and
- make the delivery-complete helper use the accepted schedule's observed unit
  root rather than a legacy fixed offset.

Regression test:

- a coherent one-frame capture lag with first exact offset 1, quarter offset
  238, scheduled restore 239, and terminal unit root 240 passes;
- the same trace without `scene_inactive/status=terminal_unload` fails;
- negative, late, mismatched, hit, Bomb, and fallback traces still fail;
- active-context C4-style offset-240 restore remains accepted; and
- a synthetic exact schedule restoring at offset 2 completes without a
  hard-coded 240 dependency.

Live verification: corrected strict schema v4 passes all 20 checks on the
retained physical C5-2 trace. The whole stage's 22 hits remain a separate
survival failure.

Status: observed physical counterexample; corrected and physically
revalidated.

## CE-0191: Root-only scale fail-close discarded the whole-stage mode observer

Date: 2026-07-30

Observed symptom:

- SEM-MODE-B attempt
  `lunatic_route2_stage5_unattended_20260730_035718` launched the exact shipped
  executable, verified the no-life-decrement patch, selected Lunatic
  Sakuya/Remilia Stage 5, armed the mode observer, and entered gameplay.
- At gameplay frame 1, before any decision record, the live controller emitted
  `time_scale_authority_unknown` with unit root, root-only coverage,
  `hard_authority=false`, and `fallback=terminate_and_release_keys`.
- The terminal summary has zero hits and
  `termination_reason=time_scale_authority_unknown`; therefore the attempt is
  not a physical SEM-MODE observation or survival sample.
- Supervisor cleanup released keys and left no TH08, controller, or
  supervisor process.

Invalid assumption:

- A physical diagnostic observer should use the hard-authority fail-close
  path even when its purpose requires a complete stage trace and its output
  is explicitly excluded from survival authority.
- Terminating before the first decision is preferable to retaining an
  explicitly unknown-direction diagnostic proxy.

Evidence or trace:

- ignored five-row JSONL SHA-256
  `da31073f043a6a615830d3410769174993ce30273becf44e1c1648117f0e5dad`;
- retained session SHA-256
  `560cce7dd93dea844c84bf673f4aa8ad5988c408d806fac6b42652d6b2083e24`;
- ignored launch log SHA-256
  `5a5017ec9d40869ed392505fc6c77727feb8b8626e55ae80dbca805400a6f736`;
- target SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`,
  patched byte `0x0044D0FA=0x00`, difficulty 3, route 2, selected stage 5;
  and
- native menu trace reaches stage cursor 5 before the agent's gameplay-frame
  fallback.

Root-cause class: control/runtime authority boundary.

Correction:

- keep the default hard-authority behavior unchanged;
- add a separate default-off continuation flag scoped to the whole-stage
  enemy-mode observer;
- at each root-only decision, expand only the currently observed root into a
  finite constant player/laser proxy, label it
  `unknown_direction_no_authority`, and retain the original root-only record;
- never combine this proxy with exact Final-B scale-source authority; and
- retain hard no-Bomb, `stop-after-hits=0`, whole-stage entry, and exact
  cleanup.

Regression test:

- root-only scale becomes a complete finite proxy with identical player and
  laser bits, immutable source frame, and explicit unknown-direction/no-
  authority provenance;
- complete schedules cannot enter the fallback;
- the launcher flag is default-off, requires the enemy-mode observer, and
  conflicts with exact Final-B scale-source authority; and
- hard no-Bomb and no stop-after-hit arguments remain present.

Live verification: original-game run
`lunatic_route2_stage5_unattended_20260730_041408` reached
`route_complete` at frame 42,463 with 11,879 decisions. Its compact
mode report passes integrity with 11,763 coherent captures, and cleanup left
no gameplay/control process. The correction therefore restores the intended
whole-stage diagnostic evidence unit.

Status: observed physical launch counterexample; corrected and physically
revalidated for diagnostic continuation only.

## CE-0192: Diagnostic Stage-5 mode run retained ten native hits

Date: 2026-07-30

Observed symptom:

- Original-game run
  `lunatic_route2_stage5_unattended_20260730_041408` selected Lunatic
  Sakuya/Remilia Stage 5, exercised code checkpoint `60ae5b9`, and completed
  frames `1..42463` with 11,879 decisions.
- It retained ten native hit edges at frames
  `[3519, 13472, 22550, 23262, 29253, 29733, 30133, 30508, 34814, 38385]`
  while every decision/input/action kept Bomb bit `0x02` clear.
- The canonical fresh-attempt hit is nonspell frame 3,519 at
  `(376.000, 431.585)` with 954 bullets, no laser, active input
  `left_fast`, and committed-prefix clearance `-2.766`.
- Seven contacts classify as `modeled_committed_prefix_collision` and three
  as `observed_bullet_overlap`. Nine have
  `robust_action_set_exhausted_before_hit`; frame 38,385 is
  `late_collision_after_positive_causal_margin`.
- Phase attribution is nonspell 3, spell 103 2, spell 107 4, spell 111 1,
  and spell 115 0. Later hits follow practice-mode respawn and are discovery
  evidence, not independent clean-route trials.

Invalid assumption:

- Completing the SEM-MODE-B occurrence gate also supplies survival evidence.
- A diagnostic run using an unknown-direction root-scale proxy and
  cadence-perturbing mode reads can be compared directly with an observer-off
  Stage-5 hit count.
- Later respawn-coupled contacts estimate a Power-0 or fresh-attempt clear
  rate.

Evidence or trace:

- retained run note:
  `notes/runs/lunatic_route2_stage5_unattended_20260730_041408.md`;
- ignored 482,944,752-byte raw JSONL SHA-256
  `773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`;
- ignored launch log SHA-256
  `895586a05926072d22148b17b839243d570121ada0728fc611ee4d8778af5668`;
- compact mode report SHA-256
  `545677cead65e312942408cbaa977694be6416b7afbaea663802462c0775dfd9`;
- target SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`
  with no-life-decrement byte `0x0044D0FA=0x00`; and
- retained dossier/regression/death ledger preserve the complete first-hit
  geometry and all ten phase-attributed contacts.

Root-cause class: planner/world-model authority boundary.

Correction:

- do not roll back or promote based on aggregate `10 versus 12` hits;
- complete SEM-MODE-C causal player/enemy mode recurrence and SEM-MODE-E
  independent body-set differential before live consumption;
- retain hard survival as the first objective and require observer-off
  whole-stage evidence only after an immutable corrected model; and
- continue treating the first hit as canonical while preserving later
  contacts as coupled geometry/cadence workloads.

Regression test:

- exact scalar/optimized histories must include active-input pickup,
  no-write/pending transitions, delayed `+5` mode state, and per-frame
  bit-`0x800` contact body sets;
- observation-equivalent hidden histories must merge before the next
  controller action;
- retained `10065 -> 10075` and Stage-5 native transition capsules must match
  projected body sets; and
- no diagnostic capture or constant-root proxy may claim action, hard, or
  physical-survival authority.

Live verification: pending a corrected immutable SEM-MODE-C–E model and the
subsequent whole-stage SEM-MODE-F gate. Do not repeat the unchanged
diagnostic run.

Status: observed physical counterexample; causal correction pending.

## CE-0193: Native input observed an ordered dispatch's partial mask

Date: 2026-07-30

Observed symptom:

- A source-hashed post-hoc audit reused complete original-game Lunatic
  Stage-5 trace `lunatic_route2_stage5_unattended_20260730_041408`.
- At trace line 297 / controller frame 386 / captured manager frame 384, the
  coherent native root was active and held mask `0x65`
  (`down_left` with Focus and Shot). The recorded issue targeted `0x41`
  (`left_fast` with Shot) through ordered key transitions
  `[(0x04, false), (0x20, false)]`, hence transaction masks
  `0x65 -> 0x61 -> 0x41`.
- At adjacent trace line 298 / controller frame 389 / captured manager frame
  387, native `input_current` and the coherent root's active mask are
  `0x61`, while held/pending target is `0x41` with remaining support `[1]`.
  The player mode counter advances `378 -> 381`; all four declared atomic
  delay branches reproduce the observed mode/body endpoint, but none can
  produce active mask `0x61`.
- Across the strict audit boundary, 381 eligible intervals directly observe
  an ordered dispatch intermediate mask that has no compatible successor
  under the atomic old-complete-mask/final-complete-mask action model. They
  cover 141 transaction/observed-mask signatures and intermediate positions
  after edges `1/2/3/4` in counts `307/62/10/2`; 148 directly involve Focus
  acquire/release timing.

Invalid assumption:

- A complete-mask issue is picked up atomically, so native active input
  between two decisions can only be the old complete mask, an older pending
  complete mask, or the new final complete mask.
- The Windows dispatcher's ordered per-key transitions are implementation
  detail invisible to the shipped game's input update.

Evidence or trace:

- compact deterministic report
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_041408.enemy_mode_bodyset_report.json`,
  SHA-256
  `db411650de15a050c5ffb815f0f075c538b4ec16cbc01f2fa0f47e403f678464`;
- ignored 482,944,752-byte source JSONL SHA-256
  `773cbdb322dc5e15f80da4800ce82bcd0f41c1e6f82826812087edc9a328dca9`;
- report scope: 11,708 adjacent coherent pairs, 7,162 strict eligible
  intervals, 3,835 compatible intervals, 381 ordered-partial-mask
  counterexamples, and 2,946 other diagnostic manager/player/issue phase
  decomposition mismatches; and
- canonical witness retains the exact dispatch order, both coherent
  pipeline roots, native active input, player mode tuple, and endpoint body
  compatibility.

Root-cause class: control/runtime and world-model boundary.

Correction:

- **Implemented offline:** one issued complete mask is an ordered
  key-transition transaction whose intermediate masks can be sampled by the
  native input update;
- **Implemented offline:** desired final mask, queued transition suffix,
  observed native active mask, and final-completion deadline are distinct
  actuator state;
- define pickup/order relative to a revalidated player/input update phase,
  not unconditional enemy-manager-frame delta;
- **Implemented offline:** complete-mask no-write emits no transition,
  samples no new delay, and preserves an older pending transaction;
- **Implemented offline:** hidden transaction histories merge only after
  active/held actuator observation and native mode state agree; and
- keep the existing atomic model and every consumer outside hard/live
  authority until independent scalar parity, adversarial multi-edge cases,
  exact-version differential, and physical post-hoc replay pass.

Regression test:

- the retained synthetic transaction
  `0x65 -> 0x61 -> 0x41` must admit the native `0x61` observation with final
  desired `0x41`;
- stop/resume, direction reversal, Focus release/acquire, Shot-only changes,
  older-pending overwrite, and complete-mask no-write must preserve ordered
  transition identity and observation-compatible support;
- exact branch membership may be a member of a later estimator belief
  without requiring singleton whole-root equality; and
- a diagnostic manager-frame decomposition must label other phase mismatches
  unknown-direction instead of misreporting them as independent recurrence
  counterexamples.

Offline regression: nine game-neutral and four TH08 ordered tests reproduce
the witness and cover deadline, no-write, overwrite, observation merging, and
priority-9-before-priority-17 composition. Complete Linux/Windows discovery
passes 1,202 tests in 14.611/30.759 seconds, with three Windows skips.

Live verification: pending asynchronous capture/issue phase closure,
physical completion-delay support, immutable optimized/native shadow
differential, exact future body/geometry versions, and a scoped whole-stage
falsifier. Do not launch an unchanged Stage-5 run.

Status: observed physical counterexample; independent offline transaction
correction implemented, physical phase/integration correction pending.

## CE-0194: Probe-only Stage-5 command stopped on root-only scale at frame 1

Date: 2026-07-30

Observed symptom:

- Original-game attempt
  `lunatic_route2_stage5_unattended_20260730_082428` verified the clean
  executable, no-life-decrement patch, Lunatic difficulty,
  Sakuya/Remilia, and Stage 5, then installed the priority-17 hook.
- The controller armed at gameplay frame 0 and established probe baseline
  serial 0. At frame 1 it emitted `time_scale_authority_unknown` because the
  ordinary root-only scale observation had no finite-horizon hard authority,
  released input, and terminated with zero decisions and zero hits.
- Supervisor cleanup terminated the exact target. The 4.9 KiB trace retains
  identity, installed-hook provenance, baseline, frame-1 scale failure,
  summary, and successful final drain/cleanup; no process remained.
- The controller had read callback serial 1 before the early scale exit and
  advanced its local cursor without emitting the batch. Final drain therefore
  reported `previous_serial=observed_serial=1` and could not retain event 1.

Invalid assumption:

- The new probe-only Stage-5 command could reach ordinary decisions without
  the already explicit diagnostic constant-root scale continuation.
- Reading and advancing a trace-ring cursor near observation time is safe even
  when later pre-issue authority checks can continue or terminate without a
  decision row.

Evidence or trace:

- ignored raw trace
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_082428.jsonl`,
  SHA-256
  `ab1c58c2a613df08e8d6b249d185608efcb1370c9bb5e0193239fc6fd38879c9`;
- ignored session
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_082428.session.json`,
  SHA-256
  `991f51e53441eab2a8fe40720b66a4f654728a60a46559786eeea2e840e41379`;
- target SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`;
- trace configuration reports hook status `installed`, common epilogue
  `0x00452480`, remote base `0x07230000`, and 256-event capacity; and
- terminal summary reports `time_scale_authority_unknown`, zero decisions,
  zero counter gaps, zero hits, and no cleanup error.

Root-cause class: control/runtime experiment contract and trace retention.

Correction:

- allow `--diagnostic-continue-root-only-scale` under either the whole-stage
  enemy-mode observer or the priority-17 publication observer, while
  preserving its explicit unknown-direction/no-authority provenance and
  conflict with exact Final-B scale authority;
- move the ring read after all pre-issue early exits;
- advance the retained serial cursor only after the decision row containing
  that batch is flushed; and
- if an exception occurs before publication, let final drain reread the
  uncommitted serial interval.

Regression test:

- argument construction must accept priority-17 tracing plus diagnostic
  root-only continuation, retain hard no-Bomb and `stop-after-hits=0`, and
  continue rejecting unscoped continuation;
- focused probe/report/issue/CLI/hotkey/supervisor suites must pass; and
- the repeated whole-stage trace must contain decisions, a contiguous
  baseline-to-final serial history or explicit unknown intervals, and no
  cleanup error.

Live verification: corrected run
`lunatic_route2_stage5_unattended_20260730_083416` completed all of Stage 5
with 12,109 retained decisions, explicit baseline-to-final batches, normal
route completion, and exact cleanup. Four ring overflows are retained as
unknown intervals instead of aborting the stage.

Status: observed physical preflight counterexample; code correction
implemented and physically revalidated.

## CE-0195: Publication report scanned older serials as backward intervals

Date: 2026-07-30

Observed symptom:

- Corrected Stage-5 run `083416` completed and retained a monotone probe
  baseline-to-final history, but automatic report materialization raised
  `ValueError: publication serial moved backward: 21 -> 1`.
- A streaming audit found no backward batch, issue bracket, or global serial
  sequence. The raw trace continued monotonically to serial 32,618.
- The failure occurred while analyzing a later transaction starting at serial
  21: interval selection applied a forward-only distance validator to every
  retained event, including old serial 1, before checking whether that event
  belonged to the requested interval.
- The first repaired report also exposed a second semantic issue: an observed
  target after a dropped serial range was labeled the *first* target
  publication even though the missing prefix could contain an earlier one.

Invalid assumption:

- A serial-distance helper that rejects backward movement can also serve as a
  predicate over a global event set containing legitimate older events.
- A positive target event after a retention gap is sufficient to claim its
  first-publication distance.

Evidence or trace:

- ignored 501,784,095-byte raw JSONL SHA-256
  `319b22f94dfdb2ce5322a0779839f94e6d03b6866c2d985e75e7c323473cae2f`;
- corrected compact report
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_083416.priority17_publication_report.json`,
  SHA-256
  `395ce5384e14e815afe6a5ce1977b165a94344906f3eb483a56c2484056be9b8`;
- monotone retained serial range `1..32618`; and
- four explicit gapped batches with dropped counts `46/296/520/852`.

Root-cause class: control/runtime evidence analysis and report integrity.

Correction:

- compute unsigned distance for membership and discard events outside the
  requested forward interval without treating old history as a rollback;
- retain the strict backward validator for actual ordered bounds and
  contiguous batches;
- publish first-final step counts only when every serial from the transaction
  boundary through that event is retained; and
- retain separate witnesses for a target observed after a gap and for a
  complete interval in which the transient target is not observed.

Regression test:

- an end-to-end multi-transaction fixture keeps events predating the later
  transaction and must report both final outcomes without raising;
- overflow with a retained target after the gap must not populate
  `first_final_publication_step_counts`; and
- the five focused priority-17 report tests pass; complete Linux/Windows
  discovery passes 1,227 tests in 14.858/30.896 seconds, with three existing
  Windows skips.

Live verification: deterministic regeneration from the same physical raw
source is structurally identical to the retained corrected report. Global
integrity remains false only for the four real overflow batches, not an
analyzer exception.

Status: observed physical-report counterexample; analyzer and retained report
fixed.

## CE-0196: Corrected priority-17 Stage-5 gate still took 11 native hits

Date: 2026-07-30

Observed symptom:

- Original-game Lunatic Sakuya/Remilia Stage-5 run `083416` completed frames
  `1..43376` with 12,109 decisions, normal `route_complete`, hard no-Bomb,
  no-save, and exact target cleanup.
- It took 11 native hit edges at
  `[4323, 10886, 11402, 12256, 23249, 25167, 30956, 31980, 41571, 42205,
  43073]`. Phase counts are nonspell/103/107/111/115 = `4/2/2/0/3`.
- The canonical fresh-attempt hit is nonspell frame 4,323 at
  `(84.751,432.000)`. The robust action set was exhausted at frame 4,314;
  minimum 240-frame pipeline clearance was `-5.043`, and the active input at
  contact was `up`.
- Across all contacts, eight classify as
  `modeled_committed_prefix_collision` and three as
  `observed_bullet_overlap`; ten of eleven have
  `robust_action_set_exhausted_before_hit`.
- Nine contacts involve fast mode, nine involve the playfield boundary, and
  six involve more than 1,000 active bullets. Bottom-eight-pixel occupancy is
  `0.800` during the 60 frames before hits versus `0.359` outside those
  windows.

Invalid assumption:

- Completing the publication observer gate implies Stage-5 survival has
  improved or entered an accepted band.
- Later post-respawn contacts are independent clean-route trials.
- This diagnostic run can be compared directly with observer-off runs despite
  the constant-root scale proxy and native callback hook.

Evidence or trace:

- run note
  `notes/runs/lunatic_route2_stage5_unattended_20260730_083416.md`;
- compact dossier SHA-256
  `898fdc03dfb09d5fb18f7715e99d9d31af135bacce685669347f28658c1c483b`;
- ignored raw source and SHA-256 retained in CE-0195; and
- control verification covers all 12,109 decisions with no Bomb mask,
  decision flag, or action violation.

Root-cause class: planner/world-model survival failure under a diagnostic
physical workload; exact causal split remains unresolved.

Correction:

- retain frame 4,323 as the canonical causal witness and later hits only as
  coupled geometry/phase discovery evidence;
- do not rollback or claim an aggregate regression from this
  non-control-equivalent diagnostic sample;
- first repair asynchronous issue/publication phase and censored completion
  support, then run scalar/optimized parity and bind exact future
  body/flag/geometry;
- preserve survival as hard, keep Bomb forbidden, and keep damage/Power/item
  objectives inside the viable set; and
- require a new immutable model version before another Stage-5 survival
  gate, followed by fresh Stage-3/4A/5/Final-B evidence before a Power-0 route.

Regression test:

- the retained first-hit window must preserve frame 4,314 action-set
  exhaustion, frame 4,323 contact, resource state, action/input masks, and
  no-Bomb proof;
- physical reports must keep fresh-attempt and post-respawn sample roles
  separate; and
- no stage result with any native hit can be promoted as NMNB.

Live verification: failed survival as observed. The publication observer
sub-gate supplies valid positive native-semantic evidence, but no live action,
hard-survival, or NMNB authority.

Status: observed physical survival counterexample; causal model correction
pending.

## CE-0197: Exogenous future schedules form impossible input/world Cartesian pairs

Date: 2026-07-30

Observed symptom:

- The first immutable future-body wrapper accepts one exogenous schedule set
  and independently enumerates asynchronous input histories.
- That product is valid for an explicitly action-independent fixture, but a
  physical producer must account for direction-dependent player position,
  aimed enemy motion, Focus mode gates, Shot/Power damage and despawn, and
  shared gameplay RNG consumption.
- Two deterministic adversarial cases contain eight exact actuator branches
  and three conditioned schedules apiece. An independent Cartesian product
  admits 24 input/schedule pairs; only eight are causally compatible. The
  other 16 pair an active-mask history with geometry produced for a different
  history.

Invalid assumption:

- Future enemy identity/flags/geometry can be produced independently of the
  delayed active-mask history and crossed with actuator branches afterward.
- The selected root action alone captures every physical effect of partial
  publication.

Evidence or trace:

- formal contract
  `notes/CAUSAL_ACTION_CONDITIONED_FUTURE_BODY_PRODUCER_CONTRACT_20260730.md`;
- deterministic report
  `artifacts/runtime_reports/th08_causal_future_body_schedule_differential_20260730.json`,
  SHA-256
  `d9f4c6202f87b2fd1515bb779284bb2cb4f51f37c08982faa092c1f43ba1898e`;
- direction reversal histories
  `(0x21,0x01)`, `(0x21,0x21)`, `(0x21,0x41)`; and
- asynchronous Focus histories
  `(0x01,0x01,0x01)`, `(0x01,0x01,0x05)`,
  `(0x01,0x05,0x05)`.

Root-cause class: noncausal world-model composition.

Correction:

- represent one selected root action as an immutable family
  `{active-mask history -> compatible schedule support}`;
- enumerate the exact reachable actuator-history set first and reject any
  missing or extra family member;
- carry one family version across all branches, but keep the conditioned
  history and schedule-branch digest hidden from the next controller key; and
- merge histories only after complete actuator/player/body observation
  converges.

Regression test:

- the two-edge direction and asynchronous Focus fixtures must retain only
  their eight compatible branches and zero mismatches;
- missing/extra histories, wrong action/mask, mutable members, wrong horizon,
  and cross-family observation merging fail closed;
- different observed body geometry cannot merge, while explicitly converged
  supplied observations can; and
- nine focused tests pass on Linux and Windows with byte-identical report
  generation; complete discovery passes 1,266/1,266 in 14.471/31.192
  seconds, with the three existing Windows skips.

Live verification: none. Both schedule representations remain offline-only,
and the repository still has no complete predictive native producer.

Status: observed finite-model counterexample; causal family representation
fixed offline, physical producer and survival integration pending.

## CE-0198: Frame-boundary active bits merge hidden same-update slot reuse

Date: 2026-07-30

Observed symptom:

- Native `enemy_spawn_from_timeline` selects the first inactive ordinary slot
  at `0x0042A54E`, copies the template, starts the main VM, and immediately
  executes it.
- If the initial VM returns `-1`, `0x0042A5F5` clears active bit 0 inside the
  same timeline spawn call. A later timeline record in the same manager update
  may allocate that slot again.
- In the canonical deterministic fixture, root-inactive slot 7 is allocated,
  retired, and reallocated in one physical update. Both root and endpoint
  snapshots expose only one inactive-to-active edge. The surviving body is
  generation 1, while the endpoint-only reconstruction reports generation 0.
- Across four independent cases, the ordered-event implementation matches a
  structurally separate oracle in all four. The boundary-only foil merges
  three hidden-reuse cases.

Invalid assumption:

- A pointer, pool slot, or adjacent active-bit edge uniquely identifies an
  enemy body lifetime.
- Matching endpoint active sets prove matching allocation histories.

Evidence or trace:

- formal contract
  `notes/GENERATIONAL_BODY_IDENTITY_AND_NATIVE_ROOT_SLICE_CONTRACT_20260730.md`;
- connected IDA comments at `0x0042A54E` and `0x0042A5F5`; and
- deterministic report
  `artifacts/runtime_reports/th08_future_body_generation_differential_20260730.json`,
  SHA-256
  `701f1ca778e6e20c94996061afec3cea68bf4233ba920af80de0fa63b4930334`.

Root-cause class: non-injective cross-time body identity and lossy lifecycle
observation.

Correction:

- use root-relative `(slot, allocation_generation)` identity at the predictive
  producer boundary;
- advance generation on every ordered native allocation after a seen
  lifetime, including allocate/retire/reallocate sequences within one update;
- use endpoint active slots only to reconcile a complete event stream; never
  synthesize missing lifecycle events from snapshots; and
- fail closed on skipped updates, invalid event order, endpoint disagreement,
  or generation overflow.

Regression test:

- generation zero retains legacy ordinary-slot IDs `0..479`;
- reused lifetimes have injective encodings;
- immediate-end/reallocation and root-active/reallocation cases match the
  independent oracle;
- a missing retirement/allocation event cannot be repaired by a matching
  endpoint; and
- Linux and Windows must render byte-identical differential output.

Live verification: none. The native order is statically revalidated, but the
allocation-event producer and complete physical root capture are not yet
connected.

Status: observed native-semantic/finite-model counterexample; generational
identity boundary fixed offline, physical producer pending.

## CE-0199: Canonical Stage-5 command fail-closes before its first decision

Date: 2026-07-30

Observed symptom:

- The preregistered `losing_control_reserve` Stage-5 launch at code checkpoint
  `22f002a` verified clean executable SHA-256
  `330fbdbf58a710829d65277b4f312cfbb38d5448b3df523e79350b879213d924`,
  patched runtime byte `0x0044D0FA` to `0x00`, selected Lunatic Route-2
  Stage 5, and entered native stage route index 5.
- At frame 1 the controller observed unit root scale but only `root_only`
  future coverage. Because `diagnostic_continue_root_only_scale=false`, it
  emitted `time_scale_authority_unknown`, released keys, and terminated.
- Run `lunatic_route2_stage5_unattended_20260730_124635` contains identity,
  controller config, wait-ready, failure, and summary records only: zero
  decisions and zero physically controlled interval. Its reported zero hits
  are not a survival result.
- The supervisor retained the failed session and cleaned up game/controller
  processes. No process remained after the attempt.
- The first corrected-command retry was rejected before process launch by a
  second stale supervisor restriction that allowed diagnostic continuation
  only when the enemy-mode or priority-17 trace observer was also enabled.
- After removing that outer restriction, the next prelaunch retry exposed the
  same stale rule duplicated in the shared hotkey/agent argument contract.
  Run `lunatic_route2_stage5_unattended_20260730_125502` did launch and patch
  the original game, then navigated to the verified Stage-5 selection. The
  nested contract rejected before final stage confirmation, agent arm, or any
  controlled decision; supervisor cleanup then removed the game.

Invalid assumption:

- The canonical no-extra-flag Stage-5 command can satisfy the explicit
  no-fail-close physical contract after the scale-authority gate was added.
- A launch that reaches a stage route necessarily reaches a controlled
  decision.

Evidence or trace:

- compact session
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_124635.session.json`;
- nested-contract session
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_125502.session.json`;
- ignored five-row raw trace
  `artifacts/runtime_reports/lunatic_route2_stage5_unattended_20260730_124635.jsonl`;
  and
- `time_scale_authority_unknown` at frame 1 with
  `fallback=terminate_and_release_keys`.

Root-cause class: control/runtime preflight and authority-contract mismatch.

Correction:

- retain the failed attempt as launch/preflight evidence only;
- remove the observer-coupling restriction from both the outer supervisor and
  nested shared agent contract: explicit scale continuation is a whole-stage
  diagnostic control in its own right and must not require an unrelated
  trace service;
- add `--diagnostic-continue-root-only-scale` to this explicitly diagnostic
  whole-stage falsifier so unit root scale is held constant over the finite
  horizon instead of aborting;
- preserve the continuation's `unknown_direction` and
  `hard_authority=false` labels; it cannot promote the scale model or prove a
  hard certificate; and
- keep hard no-Bomb, full-stage completion, exact rollback, no THPRAC, and
  no stop-after-hits unchanged.

Regression test:

- the corrected command must emit at least one decision;
- it must record the diagnostic constant-current-root fallback instead of
  `terminate_and_release_keys`; and
- any later unknown scale root may continue only under the same explicit
  diagnostic label; and
- focused Linux/Windows supervisor discovery must accept the no-observer
  Stage-5 argument vector, while complete suites remain green.

Live verification: focused supervisor discovery passes 33/33 and focused
agent/hotkey discovery passes 19/19 on Linux and Windows. Complete discovery
passes 1,298 tests in 14.387 seconds on Linux and 32.480 seconds on Windows,
with the three existing Windows skips. Corrected physical rerun remains
pending.

Status: observed physical preflight counterexample; command contract
and supervisor scope corrected, physical rerun pending.

## CE-0200: Stage-5 reserve falsifier completes without activating reserve

Date: 2026-07-30

Observed symptom:

- Run `lunatic_route2_stage5_unattended_20260730_130219` completes the
  original-game Lunatic Sakuya/Remilia Stage 5 at physical code checkpoint
  `9905d46`: frames `2..42835`, 12,281 decisions, 12 hits, hard no-Bomb, two
  automatic dialogue wall pulses, `route_complete`, and exact cleanup.
- Canonical first contact is frame 2,220, earlier than the 2,524-frame
  18-hit control and much earlier than the retained 10,740/11,504/12,324
  favorable band. It is a committed-prefix bullet collision at player
  `(8.000, 422.343)` after `up_left_fast`, with the player pinned to the left
  boundary.
- Nine of 12 contacts have a playfield-boundary contributor. Aggregate 12 is
  below the 18-hit control but above the `<=10` useful threshold and is not a
  significant favorable result.
- More importantly, the corridor emitted zero policy records/solutions/
  queries. Repair/recovery guidance was available/selected on `0/0`, and
  every selected control-reserve deficit was zero. The configured
  `losing_control_reserve` therefore changed no physical decision.

Invalid assumption:

- Enabling `losing_control_reserve` in the live `PlannerConfig` proves the
  physical workload can reach its empty-kernel repair/recovery ranking.
- Diagnostic constant-root scale continuation preserves the global policy
  publication/query path required by that intervention.
- A changed CLI/config bit is an exercised physical intervention even when
  all of its runtime activation counters are zero.

Evidence or trace:

- run note
  `notes/runs/lunatic_route2_stage5_unattended_20260730_130219.md`;
- compact summary/session/dossier/comparison/regression/death artifacts with
  the same run ID;
- ignored 461,720,403-byte raw trace SHA-256
  `fd54f277da5bc32b75c874af7dffae8c279d6f3cadfdb3848211477e76b602c1`;
  and
- runtime policy/reserve counters in the compact dossier.

Root-cause class: unreachable experiment activation and control/runtime
contract mismatch.

Correction:

- retain the 12-hit run as a complete physical workload and activation
  counterexample, not as evidence for or against the empty-kernel reserve;
- require every physical intervention contract to name a nonzero runtime
  activation counter and reject a completed-but-inert run;
- screen a separate **unguided boundary reserve** only when global guidance is
  unavailable, ranking only existing fresh-hard-equivalent endpoints by the
  same delay-scaled reversible boundary distance; and
- require retained first-hit/full-hit trace A/B to change a pre-contact action
  with zero hard-vector regressions before another original-game stage.

Regression test:

- an unguided row with no safe/repair/recovery/survival guidance must be
  eligible only for the explicit new flag;
- the exact rollback must preserve historical selection;
- first-hit and multi-hit retained screens must report action changes, reserve
  improvement, hard-vector parity, and timing separately; and
- physical compact evidence must report nonzero intervention activations.

Live verification: complete 12-hit Stage-5 workload retained; corrected
unguided intervention not yet implemented or run.

Status: observed physical experiment-activation counterexample; current
reserve result rejected, fixed-trace successor gate pending.
