# TH08 Research Log — 2026-07-29

## Pre-commit review closed an auxiliary-event version-boundary gap

- **Observed:** the runtime identity service already accepted only route 2,
  but the downstream auxiliary-event service independently compared static
  bytes, difficulty, stage, and gameplay epoch without comparing the retained
  route ID. A forged accepted-version value from another route could therefore
  cross that module boundary in isolation.
- Added an immutable `expected_route_id` to the event-program configuration,
  reject negative configuration, and require exact route equality before
  binding or lowering. The controller fixes it to route 2.
- Added a regression case showing route 1 fails closed as
  `runtime_identity_mismatch`. This correction does not grant physical or
  action authority; the focused spell-107 delivery trial remains pending.
- Focused delivery/replay tests and Ruff pass. Complete Linux discovery passes
  1,011 tests in 11.843 seconds; complete Windows UNC discovery passes 1,011
  tests in 19.660 seconds with three existing platform skips.

## Schema-v4 auxiliary-event physical delivery gate failed

- **Observed:** focused Lunatic Stage-5 run
  `lunatic_route2_stage5_unattended_20260729_014125` completed frames
  `2..41804`, 11,805 decisions, 11 hits, hard no-Bomb, accepted route
  completion, no-save selection, and exact cleanup.
- All 142 transactions were coherent and immutable-version matched. The
  independent raw-byte oracle accepted 3,830/3,830 requests over targets
  69/72/73 with zero unknown; total transaction and cadence gates passed.
- The fixed delivery gate still failed. Six initial spell-edge transactions
  had no native context and retained `no_usable_contexts`. Event derivation
  p95/p99/max was `0.879/1.058/4.871 ms`; replay compact maximum was
  `11.335 ms`; and prior synchronous emit p95/p99 was
  `2.871/3.495 ms`. The limits remain unchanged.
- The failed report regenerates byte-identically at SHA-256
  `780b8f62...b3e`; raw trace SHA-256 is `6d779046...c872`. CE-0168 records
  the falsified live-delivery assumptions.
- The next step is a separately contracted exact-version bounded cache,
  visible pre-bind, hash-addressed compact replay bundle, and explicit
  independently verified empty-prefix status. Schema v4 remains failed and
  has no delivery/action authority.

## Schema-v5 corrected delivery passes isolated preflight

- **Observed:** five replays of all 142 retained schema-v4 physical
  transactions pass the fixed corrected-delivery gates on both Linux and
  Windows. The first repeat has exactly 46 misses, 1,797 persistent hits,
  1,987 request-local hits, and zero eviction.
- Schema v5 uses one visible exact-version preparation, a bounded 512-entry
  exact LRU, an independently verified initial `empty_complete` prefix, and a
  hash-addressed zlib-level-1 replay bundle. The independent auditor decodes
  and hashes every raw blob, replays every request, and separately simulates
  the LRU transitions.
- Linux event-derive/replay-compact/no-write-JSON p95 is
  `0.371/0.329/0.585 ms`; Windows is `0.408/0.433/0.626 ms`. Both reports
  pass every fixed isolated gate. Report SHA-256 values are
  `bb9fb6e3...e4cb` and `805ac6df...fea9`.
- Ruff and all focused tests pass. Complete Linux discovery passes 1,020
  tests in 11.561 seconds; Windows UNC discovery passes 1,020 in 20.672
  seconds with three existing skips. The failed V1 report remains
  byte-identical.
- This is isolated replay, not physical contention or action authority. The
  unchanged focused Lunatic Stage-5 spell-107 schema-v5 gate remains next.

## Schema-v5 physical run exposed epoch/version conflation and 20 hits

- **Observed:** run `20260729_095849` from checkpoint `5ad2bb1` completed
  hard no-Bomb Stage 5 with 20 hits: 11 nonspell and 2/2/3/2 on spells
  103/107/111/115. This is physically unacceptable for handoff.
- The exact runtime image and V2 program were prepared at controller epoch 0,
  but all 142 selected batches occurred at epoch 3. Every event derivation
  failed closed as `runtime_identity_mismatch`; no request reached lowering,
  cache, or independent replay. CE-0169 records the model-version failure.
- The earlier accepted runtime-image contract defines program equivalence
  without controller gameplay epoch. Epoch resets invalidate observation and
  control continuity; they do not assert an ECL instruction-image write.
- The 20 hits cannot be attributed to cache contention: there was no
  lowering, the first ten hits preceded the batch window, cadence p50/p95
  remained `2/4`, prior complete Stage-5 runs span 8–23 hits, and the
  schema-v3 run `20260728_193820` also had 20. However, the frame-2 cold bind
  consumed `5.317 ms` total and cannot be declared physically side-effect
  free from one p95 statistic.
- Schema v5/event v2 is rejected. A new pre-implementation V3 contract
  separates program identity from observation epoch, moves exact target
  validation/materialization before gameplay, tightens online preparation to
  `1.000 ms`, and requires every run to be retained. Before handoff, two
  consecutive corrected Stage-5 runs must each have at most ten hits and
  other Lunatic stages must show no obvious regression.
- **Hypothesized combat follow-up:** inside an unchanged viable set and away
  from precision micro-dodging, release Shift to test whether Sakuya's wider
  unfocused coverage kills distributed nonspell enemies earlier. Native
  team/shot mode, damage, kill time, exposure, and survival margin must be
  observed before any live rule; reduced margin returns to focused control.

## Schema-v6 epoch-safe implementation passes isolated cross-epoch replay

- **Observed:** V3 byte-validates all 1,664 Stage-5 instructions before
  gameplay, proves the nine retained target instructions have no executable
  same-difficulty successor outside their owner closure, and binds only those
  nine descriptors after runtime-base acceptance.
- Five service instances per platform replay 142 retained physical
  transactions across accepted epoch 0 and observation epochs 1/2/3. Linux
  and Windows each pass all 710 samples with the exact first-repeat cache
  totals: 46 misses, 1,797 persistent hits, 1,987 request-local hits, and no
  eviction.
- Online preparation maximum falls to `0.044 ms` on Linux and `0.073 ms` on
  Windows. Event-derive p95 is `0.366/0.436 ms`; all fixed isolated timing
  gates pass. Static validation is pre-game and separately recorded.
- Ruff passes; complete Linux/Windows discovery passes 1,027 tests, with
  three existing Windows skips. The immutable failed V1 report regenerates
  byte-identically.
- This is isolated trace-only evidence. The next physical Stage-5 run is
  retained regardless of outcome, and two consecutive results at no more
  than ten hits remain required before handoff.

## Schema-v6 physical semantics pass but delivery and survival fail

- **Observed:** run `20260729_104222` completed hard no-Bomb Stage 5 with
  exact cleanup and 11 hits. Phase hits nonspell/103/107/111/115 were
  `4/0/1/4/2`; all followed global viability-kernel exhaustion.
- The epoch correction worked physically: all 127 selected batches crossed
  accepted program epoch 0 to observation epoch 2. All 3,384 requests
  independently replay complete with zero unknown; online preparation is
  `0.121 ms`.
- Physical delivery still fails event-derive, replay-compact, and previous
  emit p95 at `0.620/0.564/2.862 ms`. Batch JSON line p50/p95/max is
  `66080/92142/97166` bytes, and previous emit correlates `0.785` with the
  preceding line size.
- CE-0170 retains both the delivery and survival rejection. A V4 contract is
  fixed before implementation: preserve usable raw states, source indices,
  status proof, and exact independent replay while replacing proved-null
  record dictionaries and repeated derived JSON with narrow projections and
  canonical commitments. No timing or survival threshold is loosened.

## Schema-v7 compact delivery passes isolated Linux/Windows preflight

- **Corrected before implementation:** the independent byte oracle cannot
  rebuild every descriptive field of `result.record()`. V4 therefore commits
  exactly the oracle-compatible recurrence core, not the complete production
  dictionary. Full VM/ECL bytes remain retained; excluded descriptive
  arguments gain no authority.
- **Observed:** five repeats of all 142 retained physical transactions pass on
  Linux and Windows across accepted epoch 0 and observation epochs 1/2/3.
  Each platform classifies all 710 samples, with the unchanged first-repeat
  cache totals 46 misses, 1,797 persistent hits, 1,987 request-local hits,
  and zero eviction.
- The selected observation keeps its native full-count/status proof and raw
  replay bundle but projects only usable rows with exact source indices.
  Event output keeps narrow request rows and canonical result commitments.
- Linux/Windows maximum line size is `23495/23494` bytes. Event derive,
  replay compact, and no-write JSON p95 is `0.251/0.290/0.185 ms` on Linux
  and `0.277/0.387/0.191 ms` on Windows; every fixed isolated gate passes.
- Ruff passes. Complete Linux/Windows discovery passes 1,032 tests, with
  three Windows skips. The failed V1 report remains byte-identical at
  `780b8f62...b3e`.
- This is isolated trace-only evidence. A fresh retained Stage-5 physical gate
  is next; two consecutive corrected `<=10`-hit results and later cross-stage
  checks remain required before handoff.

## First schema-v7 physical attempt was foreground-contaminated

- **Observed:** attempt `20260729_112743` from checkpoint `5fae77a` armed with
  exact executable/patch/menu identity, then lost TH08 foreground at summary
  frame 2,736. The controller failed closed, released keys, and the supervisor
  completed exact process cleanup.
- The trace has 1,169 decisions and one early nonspell hit at frame 2,726,
  after global-kernel exhaustion. It ended before spell 107 and emitted zero
  auxiliary batches, so it supplies no V4 delivery evidence and is excluded
  from the consecutive survival count.
- CE-0171 retains the contamination and the invalid assumption that an armed
  unattended launch necessarily retains foreground. The unchanged checkpoint
  requires a fresh retained rerun.

## Schema-v7 physical semantics pass but compact delivery and survival fail

- **Observed:** uncontaminated Stage-5 run `20260729_113124` completed frames
  `1..42928`, 11,797 decisions, hard no-Bomb, accepted artifacts, route
  completion, and exact cleanup. It took 14 hits:
  nonspell/103/107/111/115 = `8/2/3/1/0`.
- All 184 batches and 5,124 requests independently replay exactly with zero
  unknown. Preparation, cache, event derive, replay compact, transaction
  total, cadence, no-Bomb, completion, and cleanup gates pass.
- Physical context cardinality exceeded the isolated sample. Batch-line
  p95/max was `24142/25296` bytes, failing the fixed 24,576-byte maximum.
  Previous synchronous emit p95 was `1.366 ms`, failing the fixed 1.000-ms
  limit. The strict failed report is byte-stable at
  `2d5ebcf8...d29`.
- CE-0172 rejects V4 delivery and survival authority while preserving its
  physical semantic evidence. The next fixed transport-only V5 contract uses
  versioned columnar usable-record and request rows to remove repeated JSON
  keys while retaining exact indices, status proof, hashes, raw replay bytes,
  epochs, program identity, and recurrence commitments. No threshold is
  loosened and no action changes in this correction.
- Eight hits were in nonspell and every hit followed global-kernel
  exhaustion. The separate survival line will test earlier nonspell
  kill/despawn and, only when survival margin permits, Sakuya unfocused shot
  coverage. This remains a hypothesis and is not part of V5 delivery.

## Schema-v8 columnar delivery passes isolated Linux/Windows preflight

- **Observed:** the fixed V5 transport replaces repeated usable-record and
  request keys with exact fourteen-/three-column schemas. Raw replay bytes,
  status proof, source order, hashes, epochs, program identity, cache records,
  and recurrence commitments remain unchanged.
- Linux and Windows each pass five repeats over 142 retained transactions:
  19,150/19,150 requests independently replay complete with zero unknown,
  target counts remain `950/965/1915` per first repeat, and independent cache
  parity retains 46 misses, 1,797 persistent hits, 1,987 request-local hits,
  and zero eviction.
- Maximum line size falls to `13743/13741` bytes. Derive/compact/JSON p95 is
  `0.190/0.236/0.100 ms` on Linux and `0.280/0.390/0.149 ms` on Windows.
  Every fixed isolated gate passes without relaxing a threshold.
- Pure column projection of the actual 34-record failed V4 physical trace
  yields a 14,038-byte maximum. This supports size margin but is not physical
  V5 timing.
- Ruff and complete 1,038-test Linux/Windows suites pass. V1 and physical V4
  failed reports retain exact SHA-256. This checkpoint remains trace-only;
  one fresh V5 Stage-5 delivery run is next, separate from combat changes.

## Schema-v8 physical semantics pass but separate publication and survival fail

- **Observed:** run `20260729_120859` from checkpoint `35d3163` completed
  hard no-Bomb Stage 5 with accepted artifacts, route completion, exact
  cleanup, and 14 hits. Phase hits nonspell/103/107/111/115 were
  `5/1/5/2/1`.
- All 154 columnar batches and 4,238 requests independently replay complete
  with zero unknown. Preparation, program identity, schema, cache, oracle,
  line size, event derive, transaction total, cadence, no-Bomb, completion,
  and cleanup pass.
- Physical line p95/max falls to `14899/15453` bytes, so V5 fixes V4's size
  failure. The still-separate synchronous emit p95 is `1.412 ms`, and replay
  compact p95/max is `0.507/8.452 ms`; those fixed gates fail.
- **Inferred:** rows with at least 16 records pay an approximately
  one-millisecond fixed write cost even after columnarization. The next
  correction must change publication composition, not keep removing proof
  fields: a separately contracted envelope should restore exact V5 bytes
  inside the same-iteration decision publication and eliminate the second
  OS write.
- CE-0173 retains the failure. No threshold is loosened. Survival remains
  separate: 13/14 hits follow global-kernel exhaustion, and spells 107/111
  contribute seven hits, so nonspell targeting alone is insufficient.
- The survival-filtered unfocused-Sakuya hypothesis remains proposed only:
  release Shift for wider coverage when native shot/damage evidence and hard
  margin allow, then return to focus before dense geometry. It was not
  exercised in this run.

## V6 coalesced-publication contract fixed before implementation

- The immutable inner evidence is one complete schema-v8 V5 object encoded as
  canonical sorted compact JSON with `previous_emit = null`. The outer
  envelope uses zlib level 6, strict base64, exact compressed/uncompressed
  lengths and SHA-256, and sequence/frame/epoch/snapshot/stage bindings.
- One envelope attaches only to the decision produced later in the same
  controller iteration. V6 forbids a standalone auxiliary batch row and
  reuses that decision's one existing synchronous flush. The following
  decision's existing `previous_trace` is the causal publication measurement;
  a missing measurement fails closed.
- Independent analysis must reconstruct canonical bytes, validate every
  binding/order field, and reuse the unchanged V5 byte/cache oracle.
  Corruption, truncation, zlib trailing data, noncanonical JSON,
  stale/cross-epoch attachment, duplicate/gapped sequence, and a standalone
  row are explicit falsifiers.
- Ten Linux diagnostic passes over all 154 physical V5 rows yield inner
  p95/max `13934/14448` bytes, base64 p95/max `7024/7196` bytes, and pack
  p95/p99/max `0.365/0.416/0.825 ms`. Fixed V6 bounds are wider and still
  falsifiable; this diagnostic is not Windows or physical contention
  evidence.
- V6 remains post-issue trace-only and action-neutral. Its first physical run
  must not include Focus/unfocused, targeting, Power, planner, or other
  survival changes.
